{
  "mcpServers": {
    "sif": {
      "command": "sif",
      "args": [
        "mcp",
        "stdio"
      ],
      "env": {
        "SIF_DB_PATH": "/Users/forrest/.sif/index.sqlite"
      },
      "description": "SIF MCP Server for document search and retrieval",
      "disabled": false
    }
  }
}
