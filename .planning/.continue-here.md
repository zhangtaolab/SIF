---
context: default
phase: e2e-verification
status: in_progress
last_updated: "2026-04-27T12:53:33Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] **CONSTRAINT: SQLite DDL triggers implicit commit** — Creating tables inside a transaction that later rolls back will NOT roll back the DDL. Structural mitigation: never assert DDL rollback in SQLite tests.
- [x] **CONSTRAINT: macOS load_extension("vec0") fails** — Use `sqlite_vec.load(conn)` from the Python package instead.
- [x] **CONSTRAINT: Qwen3-Reranker-0.6B is NOT a CrossEncoder** — It requires `AutoModelForCausalLM` with yes/no token logit prediction.
- [x] **CONSTRAINT: Stored document paths are full resolved paths** — FIXED: `get_by_filename()` added as fallback when path lookup fails.

<current_state>
All 3 E2E bugs fixed and verified. MPS (Apple GPU) support added to embedder. Automated E2E passed 13/13. User paused to run manual E2E test against real Obsidian vault. All changes uncommitted.
</current_state>

<completed_work>

1. Fixed ~300+ ruff issues (UP045, UP006, SIM108, B904, PLC0415, complexity, PERF401, etc.)
2. Fixed datetime.utcnow() → datetime.now(timezone.utc) across all src and test files
3. Fixed 9 PytestReturnNotNoneWarning in test_sif_complete.py
4. All 378 tests pass, 0 warnings
5. Fixed Bug 1 & 2: `get_by_filename()` fallback in get_cmd and multi_get_cmd
6. Fixed Bug 3: `_row_to_collection` now reads document_count/chunk_count from DB
7. Added `mark_indexed()` method to Collection dataclass
8. Added MPS (Apple GPU) device detection: CUDA > MPS > CPU
9. Automated E2E verification: 13/13 tests passed
10. Manual test plan written at `.planning/E2E-manual-test-plan.md`
</completed_work>

<remaining_work>

- **Commit all changes** — ~55 files modified, nothing committed yet
- **Run manual E2E test** — test plan at `.planning/E2E-manual-test-plan.md` against `/Users/forrest/SynologyDrive/Obsidian/ScholarCopilot-Dev/raw/markdown` (307 papers, 266MB)
- **Re-run embed with MPS** — `sif index embed -c scholar-test` should now use MPS instead of CPU
</remaining_work>

<decisions_made>

- Added noqa for Click-inherent complexity (structural, not fixable)
- Added noqa for intentional lazy imports (ML libraries)
- Used lambda wrappers for datetime in field default_factory
- MPS device detection: CUDA > MPS > CPU for both SentenceTransformerEmbedder and ModelScopeEmbedder
- Get by filename: path lookup first, then filename column fallback
- Collection stats: _row_to_collection must read all DB columns
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured machine-readable state
2. `.planning/E2E-manual-test-plan.md` — manual test plan for real Obsidian vault
3. `.planning/E2E-UAT.md` — automated E2E results (13/13 passed)
4. `.planning/debug/e2e-get-and-stats-bugs.md` — debug session with root cause analysis

## Infrastructure State
- SIF CLI installed and working (`sif --version` → 0.1.0)
- Test DB at `/tmp/sif-e2e-test.sqlite` cleaned up
- MPS available: confirmed (`torch.backends.mps.is_available()` → True)
- Model cached: Qwen/Qwen3-Embedding-0.6B at `/Users/forrest/Library/Caches/sif/`
- User may have a partial embed in progress for `scholar-test` collection

<next_action>
Commit all changes first. Then user runs manual E2E test plan: re-index and embed with MPS enabled for real Obsidian vault.
</next_action>
