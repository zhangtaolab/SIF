---
phase: 260917-bo2-typing-debt-batch-1-clear-search-rerank-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sif/search/rerank.py
autonomous: true
requirements: [PHASE03-DEFERRED-TYPING]  # phase-03 open deferred item: mypy strict-mode typing debt (~139 errors / 35 files); this is batch 1 of N
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py` exits 0 with zero errors for the file (all 22 current errors cleared; the `pyproject.toml: note: unused section(s): module = ['sif.cli.*', 'tests.*']` NOTE may still print — it is a config-level note expected under single-file invocation, not an error, and NOT in scope)"
    - "ZERO behavior change: `git diff` on src/sif/search/rerank.py shows ONLY (a) field type annotations, (b) TYPE_CHECKING import additions, (c) runtime-safe narrowing asserts after existing load guards, (d) removal of 4 stale `# type: ignore[...]` comments (lines 238, 374, 377, 380), (e) removal of the `# noqa: F401` on the TYPE_CHECKING CrossEncoder import, (f) the `settings: Settings` parameter annotation. No logic edits, no guard removals, no signature/value changes"
    - "The three `load()` early-exit guards (lines 108, 186, 278 `if self._model is not None: return`) are STILL PRESENT and now type-check as reachable — the typing was wrong, the code was right; no code removed"
    - "`create_reranker` valid-type set is byte-identical: gguf / sentence_transformers dispatch, qwen3-reranker name auto-detection, getattr/hasattr defensive reads, and the ValueError on unknown type are untouched (settings validator is load-bearing)"
    - "`env -u FORCE_COLOR NO_COLOR=1 python -m pytest` green at baseline (676 passed / 0 failed expected; if the 2 order-dependent caplog failures in tests/unit/embedding/test_openai_embedder.py appear they are pre-existing WINDOWS.md #3/#4 — verify by confirming they are exactly those 2)"
    - "`ruff check src tests` and `ruff format --check src tests` both clean (in particular NO new RUF100 unused-noqa from the removed `# noqa: F401`)"
  artifacts:
    - "src/sif/search/rerank.py — all lazy model/tokenizer fields properly annotated as `X | None` with TYPE_CHECKING third-party types; narrowing asserts in the three `rerank()` methods; `create_reranker(settings: Settings)` annotated"
  key_links:
    - "TYPE_CHECKING `from llama_cpp import Llama` <-> `LlamaCppReranker._model: Llama | None`"
    - "TYPE_CHECKING `from sentence_transformers import CrossEncoder` (existing, line 15) <-> `CrossEncoderReranker._model: CrossEncoder | None`"
    - "TYPE_CHECKING `from transformers import AutoModelForCausalLM, AutoTokenizer` <-> `Qwen3Reranker._model` / `Qwen3Reranker._tokenizer` annotations"
    - "TYPE_CHECKING `from sif.config.settings import Settings` <-> `create_reranker(settings: Settings)` (TYPE_CHECKING-only: no runtime import edge added, no circular-import risk; tests pass MagicMock and are NOT part of the `mypy src/sif` gate)"
---

<objective>
Clear all 22 mypy strict-mode errors in `src/sif/search/rerank.py` (batch 1 of the phase-03 deferred typing-debt series; `rerank.py` is the hottest file). Zero behavior change — annotations, TYPE_CHECKING imports, narrowing asserts, and stale-ignore removal only. The 676-test suite stays green.

Planner-verified error inventory (fresh run this session, `env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py`):
- 14x `"None" has no attribute/not callable` — root cause: `self._model = None` / `self._tokenizer = None` in `__init__` with NO annotation, so mypy infers the field type as literal `None`. Lines 140, 152 (LlamaCpp), 238 (CrossEncoder), 305/308/310/311/320/323/325/326 (Qwen3.load), 367/374/377 (Qwen3.rerank).
- 3x `Statement is unreachable` at 108, 186, 278 — the `if self._model is not None: return` early-exits in the three `load()` methods. These guards ARE reachable at runtime (second `load()` call no-ops after a successful load); the inferred `None` type makes mypy think otherwise. Fix the typing, do NOT remove the guards.
- 4x `Unused "type: ignore" comment` at 238, 374, 377, 380 — stale ignores (the codes they name never fire; the real errors are different codes). Remove them.
- 1x `no-untyped-def` at 391 — `create_reranker(settings)` missing param annotation.

Verified environment facts (deterministic fix, planner-tested with a scratch file under `--strict --warn-unreachable --ignore-missing-imports`):
- `llama_cpp`, `sentence_transformers`, `transformers`, `torch` are NOT installed in the uv env; with `ignore_missing_imports = true` their TYPE_CHECKING imports resolve to `Any`. numpy IS installed.
- Annotating `self._model: Llama | None = None` alone fixes the unreachable errors AND all errors inside `load()` (assignment of an `Any`-typed RHS narrows the field to `Any` for the rest of the method).
- mypy does NOT collapse `Any | None` to `Any` for member access: in each `rerank()`, after the existing `if self._model is None: self.load()` guard, an explicit `assert self._model is not None` is required (load() either assigns or raises, so the assert is runtime-safe and passes trivially).
- Test safety verified: tests/unit/inference/test_reranker.py (only file touching load()/rerank()) patches the `Llama`/`CrossEncoder` constructors at module level, so `load()` genuinely assigns a MagicMock — the asserts can never fire in the suite.
- ruff has no S rules selected (asserts fine in src) but RUF100 (unused-noqa) IS active: the `# noqa: F401` on line 15 MUST be dropped once the CrossEncoder import is referenced by an annotation.
- The `pyproject.toml: note: unused section(s): module = ['sif.cli.*', 'tests.*']` note is expected under single-file invocation (those overrides serve full-project runs). Do NOT touch pyproject.toml — explicitly out of scope for batch 1.

Purpose: chip the mypy strict-mode debt down file-by-file with provable zero behavior change; `rerank.py` at 0 errors unblocks later batches (~117 errors remain in other files — expected, out of scope).
Output: mypy-clean `src/sif/search/rerank.py`, green suite, clean ruff, two pathspec-scoped commits.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/sif/search/rerank.py
@src/sif/config/settings.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Type LlamaCppReranker and CrossEncoderReranker lazy model fields (22 errors -> 16)</name>
  <files>src/sif/search/rerank.py</files>
  <action>
Edits confined to the module header, LlamaCppReranker, and CrossEncoderReranker:

1. Extend the existing `if TYPE_CHECKING:` block (lines 14-15) to import `Llama` alongside CrossEncoder, isort-alphabetical, and REMOVE the now-obsolete `# noqa: F401` from the CrossEncoder import (it becomes used by an annotation; keeping the noqa would trip RUF100):
   `from llama_cpp import Llama` then `from sentence_transformers import CrossEncoder`.

2. LlamaCppReranker.__init__ (line 103): `self._model = None` -> `self._model: Llama | None = None`.

3. LlamaCppReranker.rerank(): immediately after the existing guard `if self._model is None: self.load()` (lines 134-135), add `assert self._model is not None` on its own line. Touch nothing else in the method — `self._model.embed(...)` at 140/152 then type-checks.

4. CrossEncoderReranker.__init__ (line 181): `self._model = None` -> `self._model: CrossEncoder | None = None`.

5. CrossEncoderReranker.rerank(): same pattern — after the existing guard (lines 224-225) add `assert self._model is not None`, then DELETE the trailing `# type: ignore[union-attr]` from line 238 (`raw_scores = self._model.predict(pairs, batch_size=self._batch_size)`).

6. Do NOT touch the `load()` methods' logic (the `if self._model is not None: return` guards at 107-108 and 185-186 become reachable once the fields are annotated — that IS the fix for the two unreachable errors). Do NOT touch `import numpy as np` or any `# noqa: PLC0415` runtime imports.

Then run the verification command; expected result is exactly 16 remaining errors, ALL of them in Qwen3Reranker (278+) or the factory (391) — zero errors at lines <= 238. Commit with pathspec only: `git commit -m "fix(260917-bo2): type lazy model fields in LlamaCpp/CrossEncoder rerankers" -- src/sif/search/rerank.py` (leaves the staged `mypy.ini` deletion and untracked runtime dirs alone).
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py 2>&1 | grep -v "unused section" | grep -c "error:" | grep -qx "16" && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/unit/inference/test_rerank.py -q && ruff check src/sif/search/rerank.py && ruff format --check src/sif/search/rerank.py</automated>
  </verify>
  <done>mypy reports exactly 16 errors, all at lines >= 278 (Qwen3Reranker + factory); test_rerank.py fully green; ruff clean on the file; change is annotations/assert/stale-ignore-removal only; pathspec commit created.</done>
</task>

<task type="auto">
  <name>Task 2: Type Qwen3Reranker model/tokenizer fields and create_reranker settings param (16 errors -> 0)</name>
  <files>src/sif/search/rerank.py</files>
  <action>
Edits confined to Qwen3Reranker and create_reranker:

1. Add to the TYPE_CHECKING block: `from transformers import AutoModelForCausalLM, AutoTokenizer` (third-party group, alphabetized after sentence_transformers) and `from sif.config.settings import Settings` (first-party group, separated by a blank line from the third-party imports, matching how lines 10-11 order sif imports). If ruff isort disagrees with the layout, run `ruff check --fix src/sif/search/rerank.py` and let it settle the ordering — scoped to this file only.

2. Qwen3Reranker.__init__: line 270 `self._model = None` -> `self._model: AutoModelForCausalLM | None = None`; line 271 `self._tokenizer = None` -> `self._tokenizer: AutoTokenizer | None = None`. Leave `_token_true_id`/`_token_false_id` (`int | None`) exactly as typed — indexing with them at 381-382 is on an `Any`-typed logits and needs no change.

3. Qwen3Reranker.load(): NO edits needed. With the annotations in place, the assignments at 298-302 / 313-317 (AutoTokenizer/AutoModelForCausalLM resolve to Any with the packages absent) narrow the fields, so the `.to(...)` chains at 305/308/320/323 and `convert_tokens_to_ids` at 310-311/325-326 type-check, and the guard at 277-278 becomes reachable. If any error survives here, fix with the minimal field-annotation/narrowing change — never a blanket ignore and never a logic edit.

4. Qwen3Reranker.rerank(): after the existing guard `if self._model is None: self.load()` (lines 348-349) add two asserts on their own lines: `assert self._model is not None` and `assert self._tokenizer is not None`. Then remove the three stale ignores: line 374 drop `# type: ignore[union-attr]`, line 377 drop `# type: ignore[operator]`, line 380 drop `# type: ignore[union-attr]`. The `_token_true_id`/`_token_false_id` indexing needs NO assert (verified: numpy/mypy accept it).

5. create_reranker (line 391-392): annotate the parameter — `def create_reranker(settings: Settings) -> LlamaCppReranker | CrossEncoderReranker | Qwen3Reranker:` — adding `from sif.config.settings import Settings` to the TYPE_CHECKING block (`from __future__ import annotations` is already line 3, so the forward reference is safe; TYPE_CHECKING-only means zero runtime import edge). Do NOT change the body: the getattr/hasattr defensive reads stay byte-identical (tests call this with MagicMock; the gguf / sentence_transformers / qwen3-name-detection dispatch and the unknown-type ValueError are load-bearing — see settings validator `reranker_model_type`).

6. Run the full quality suite per CLAUDE.md, then commit pathspec-only: `git commit -m "fix(260917-bo2): type Qwen3Reranker fields and create_reranker param; rerank.py mypy-clean" -- src/sif/search/rerank.py`.

Scope guards: full-project `uv run mypy src/sif` will still show ~117 errors in OTHER files — expected, batch-1 scope is rerank.py only. Do NOT edit pyproject.toml (the `unused section(s)` note and `sif.cli.*`/`tests.*` overrides are for full-project runs). Do NOT delete any guard, ignore-suite changes beyond the 4 named lines, or `# noqa: PLC0415` markers.
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py 2>&1 | grep -v "unused section" | grep -q "^Success" ; env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py > /dev/null 2>&1 ; [ $? -eq 0 ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest -q && ruff check src tests && ruff format --check src tests</automated>
  </verify>
  <done>`uv run mypy src/sif/search/rerank.py` exits 0 with "Success" (the pyproject unused-section NOTE may print — not an error); full pytest suite green at 676 passed / 0 failed (or only the 2 pre-existing WINDOWS.md #3/#4 caplog flakers); ruff check + format --check clean across src and tests; diff review confirms annotations/TYPE_CHECKING imports/4 stale ignores removed/narrowing asserts/Settings param only — zero behavior change; pathspec commit created.</done>
</task>

</tasks>

<verification>
1. Gate command (the definition of done for the typing work): `env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif/search/rerank.py` — exit code 0, "Success: no issues found in 1 source file". The `pyproject.toml: note: unused section(s)` note is acceptable output; any `error:` line is a failure.
2. Full quality suite (CLAUDE.md): `env -u FORCE_COLOR NO_COLOR=1 python -m pytest` (676 passed / 0 failed expected; 2 order-dependent caplog failures in tests/unit/embedding/test_openai_embedder.py are pre-existing WINDOWS.md #3/#4 if they appear), `ruff check src tests`, `ruff format --check src tests`.
3. Zero-behavior-change audit: `git diff HEAD~2 -- src/sif/search/rerank.py` — every hunk is one of: field annotations, TYPE_CHECKING imports, narrowing asserts directly after existing guards, removal of the 4 stale type-ignores (238/374/377/380), removal of the line-15 noqa F401, the `settings: Settings` annotation. No guard removals, no logic changes, no signature/value changes.
4. Regression confirmation for the runtime-reachable "unreachable" guards: `grep -c "if self._model is not None:" src/sif/search/rerank.py` returns 3 (guards at 108/186/278 all retained).
5. Commits are pathspec-scoped to src/sif/search/rerank.py only; `git status` still shows untracked runtime dirs (.gsd/, .planning/state.json, .planning/tmp/, .planning/ui-reviews/) and the pre-staged mypy.ini deletion untouched by this task's commits.
</verification>

<success_criteria>
- All 22 mypy errors in src/sif/search/rerank.py cleared; file is mypy strict-clean in this environment.
- Test suite green at baseline; ruff check + ruff format --check clean.
- Zero behavior change proven by diff audit; the three load() early-exit guards retained and now reachable per mypy.
- Two atomic pathspec commits landed; no unrelated files swept in.
- Deferred-item progress recorded in SUMMARY for the next batches (~117 errors remain in other files).
</success_criteria>

<output>
Create `.planning/quick/260917-bo2-typing-debt-batch-1-clear-search-rerank-/260917-bo2-SUMMARY.md` when done
</output>
