---
phase: 260914-g63-audit-follow-up-fix-deferred-items-doc-f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sif/cli/main.py
  - .planning/phases/05-agent-context-experience/deferred-items.md
  - .planning/phases/03-embedding-vector-search/deferred-items.md
autonomous: true
requirements: [AUDIT-G63]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "node ~/.claude/gsd-core/bin/gsd-tools.cjs query audit-uat --raw reports EXACTLY ONE unresolved deferred item: summary.total_items == 1, by_phase {\"03\": 1}, results[0].phase == \"03\", and that item's name contains both \"2026-09-14\" and \"typing debt\" (the re-filed mypy strict-mode debt)"
    - "Phase 05 deferred-items.md: the single item still carries its full original strikethrough description and the full resolution explanation (root cause: Rich console.print word-wrapping; fixed by quick task 260910-kps, commits 2f7cbb4 and 4d4bb02), and now parses as resolved"
    - "Phase 03 deferred-items.md: the original \"mypy cannot run\" item under the existing '## 2026-09-03 — plan 03-07 execution' heading parses as resolved with its root-cause explanation preserved (mypy.ini pinning python_version 3.9 shadowed pyproject.toml; consolidated overrides, deleted mypy.ini)"
    - "Phase 03 deferred-items.md: a NEW open deferred item under its own '## 2026-09-14 — audit re-file: mypy strict-mode typing debt' leaf heading records 140 errors in 36 files (76 checked) as of 2026-09-14, top error codes type-arg/no-any-return/attr-defined, hottest files search/rerank.py, indexing/watcher.py, mcp/handlers.py, embedding/manager.py, disposition = tracked backlog with /gsd-phase as the scheduling path; it carries NO status field so it stays open and surfaced"
    - "src/sif/cli/main.py imports sqlite3 and the cleanup_cmd OperationalError handler at former line 166 no longer carries '# noqa: F821'; env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif reports 139 errors (down from 140) with zero errors in src/sif/cli/main.py"
    - "Full quality suite green: ruff check src tests; ruff format --check src tests; env -u FORCE_COLOR NO_COLOR=1 uv run pytest passes"
    - "Every commit in this task uses an explicit file pathspec; the pre-existing staged mypy.ini deletion and other unrelated dirty-tree changes (.planning/WINDOWS.md, .planning/config.json, uv.lock, untracked .gsd/, .planning/state.json, .planning/tmp/, .planning/ui-reviews/) are never swept into this task's commits"
  artifacts:
    - ".planning/phases/05-agent-context-experience/deferred-items.md — restructured: description bullet line, then a '**Status:** resolved' line, then a 'Resolved 2026-09-10 — …' explanation line"
    - ".planning/phases/03-embedding-vector-search/deferred-items.md — original item closed with parseable status; new '## 2026-09-14 — audit re-file: mypy strict-mode typing debt' section with open item"
    - "src/sif/cli/main.py — 'import sqlite3' in the stdlib import block; '# noqa: F821' removed from the cleanup_cmd except clause"
  key_links:
    - "deferred-items status line format <-> gsd-core uat.cjs field parser: the line must reduce to key 'status' with value EXACTLY 'resolved' — the parser takes the full remainder of the line as the value, so any date or prose appended to the status line itself breaks resolution"
    - "new phase-03 leaf heading '## 2026-09-14 — …' <-> uat.cjs splitDeferredHeadingEntriesDetailed: one entry per LEAF heading (all sibling bullets under one ## heading form a SINGLE entry sharing one status), so the open typing-debt item must live under its own heading or the resolved status of the sibling item would silently hide it"
    - "src/sif/cli/main.py sqlite3 import <-> cleanup_cmd's 'except sqlite3.OperationalError' fallback on the document_embeddings DELETE: without the import the fallback raises NameError instead of degrading to embeddings_removed = 0"
---

<objective>
Audit follow-up (quick task 260914-g63): make two already-resolved deferred-items entries parseable by the GSD audit, re-file the mypy strict-mode typing debt as a tracked open deferred item, and fix the latent sqlite3 NameError in cleanup_cmd.

Purpose: today's `audit-uat` run flags 2 unresolved deferred items that are both resolved in substance but written in a format the parser cannot see, hiding real state; and the phase-03 file buries an untracked 140-error typing debt inside prose that claims resolution. Separately, src/sif/cli/main.py references sqlite3 without importing it, so the cleanup_cmd fallback path raises NameError exactly when it should degrade gracefully.

Output: two corrected deferred-items.md files (audit then reports exactly one open item — the typing debt), a fixed import in src/sif/cli/main.py (mypy 140 -> 139 errors, name-defined gone), and pathspec-only commits that leave the pre-existing dirty working tree untouched.

Parser ground truth (empirically verified this session against ~/.claude/gsd-core/bin/lib/uat.cjs via its exported parseDeferredItems — this refines the background notes' prose example, which does NOT parse):
- A deferred entry is resolved only when one of its lines, after bullet-marker/indent trimming, yields field key `status` (lowercase `status:` or bold `**Status:**` — a bare capitalized `Status:` stores under the wrong key and is ignored) with value EXACTLY `resolved`. The parser takes the FULL remainder of the line as the value: `**Status:** resolved 2026-09-10 — …` on one line does NOT resolve (tested: unresolved). The date and explanation MUST go on a separate following continuation line.
- Phase 03's file has no level-2 `## Deferred Items` heading, so the whole file is scanned with heading-delimited entry splitting: every LEAF `##` heading opens one entry and ALL sibling bullets under it share ONE status. A resolved line on one bullet resolves the entire heading-section — so the new typing-debt item must be its own `## …` leaf heading section, not a second bullet under the 2026-09-03 heading.
- The exact target file contents below were run through the real parser this session: phase 05 -> 0 unresolved; phase 03 -> exactly 1 unresolved (the new typing-debt item).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/sif/cli/main.py
@.planning/phases/05-agent-context-experience/deferred-items.md
@.planning/phases/03-embedding-vector-search/deferred-items.md

Environment notes:
- Tests and type-checking in this repo fail spuriously under FORCE_COLOR: always run `env -u FORCE_COLOR NO_COLOR=1 uv run pytest` and `env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif` (unsetting the var, not setting it to "0").
- Current baseline (verified 2026-09-14): audit-uat --raw gives summary.total_items 2, by_phase {"03":1,"05":1}; mypy reports "Found 140 errors in 36 files (checked 76 source files)" with exactly one error in src/sif/cli/main.py (line 166, code name-defined); pytest 667 passed.
- The audit command's --raw stdout is pure JSON (verified parseable via require()).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import sqlite3 in cli/main.py so the cleanup fallback cannot raise NameError</name>
  <files>src/sif/cli/main.py</files>
  <precondition>Working tree still has the pre-existing unrelated changes (staged mypy.ini deletion, modified .planning/WINDOWS.md, .planning/config.json, uv.lock) — verify with git status and leave every one of them exactly as found.</precondition>
  <action>
Fix the latent NameError in cleanup_cmd: the document_embeddings DELETE guard reads `except sqlite3.OperationalError:  # noqa: F821` (currently around line 166) but the module never imports sqlite3, so an OperationalError on an older DB would raise NameError instead of falling back to embeddings_removed = 0.

Add `import sqlite3` to the stdlib import block after `from __future__ import annotations`, placed BEFORE `from pathlib import Path` (ruff isort ordering — verified this session: straight stdlib import precedes the from-import in this project's config), so the block reads: `from __future__ import annotations`, blank line, `import sqlite3`, `from pathlib import Path`. Remove the now-unneeded `# noqa: F821` comment from the except line, leaving bare `except sqlite3.OperationalError:` — RUF100 (RUF is selected in pyproject) would flag the noqa as unused if left. Do not touch any other code in the file.

Commit with an explicit pathspec so the pre-existing staged mypy.ini deletion is not swept in: `git commit -m "fix(260914-g63): import sqlite3 so cleanup's OperationalError fallback can't raise NameError" -- src/sif/cli/main.py` — never `git commit -a`, never a bare `git add -A`.
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif 2>&1 | tee /tmp/g63-mypy.txt | tail -1 && ! grep -q "cli/main.py" /tmp/g63-mypy.txt && ruff check src tests && ruff format --check src tests</automated>
  </verify>
  <done>tail -1 of mypy output reads "Found 139 errors in 36 files (checked 76 source files)" (down from 140), zero error lines mention src/sif/cli/main.py (the name-defined error is gone), and ruff check + ruff format --check are clean. The commit contains only src/sif/cli/main.py.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure the phase-05 deferred item so its resolution parses</name>
  <files>.planning/phases/05-agent-context-experience/deferred-items.md</files>
  <action>
The file has one bullet under `## Deferred Items`. Its `status: resolved 2026-09-10 — …` sits mid-line after the strikethrough text, which the audit parser cannot see (verified baseline: phase 05 reports 1 unresolved item). Restructure the single bullet into three lines while keeping ALL original text verbatim:

Line 1 — the bullet line: keep exactly the existing strikethrough description starting `- ~~\`sif search search <term> --json\` emits raw control characters` and ending `unrelated to context add/prune changes.~~`, with the trailing ` status: resolved …` tail removed from this line.

Line 2 — an indented continuation line (two spaces) containing exactly: `**Status:** resolved` and nothing else. This is load-bearing: the bold key lowercases to `status` and the value must be EXACTLY `resolved` — any date or prose appended to this line breaks resolution because the parser takes the full line remainder as the field value (empirically tested this session).

Line 3 — an indented continuation line (two spaces) starting `Resolved 2026-09-10 — root cause was Rich \`console.print\` word-wrapping the JSON string at terminal width` and continuing with the full existing explanation verbatim (fixed by quick task 260910-kps, commits `2f7cbb4` and `4d4bb02`; all machine-format branches `--json`/`--csv`/`--md`/`--xml`/`--files` in search/vsearch/query and `--json` in bench now emit via `click.echo`; regression tests under forced `COLUMNS=80`). Note line 3 must not begin with a `key:`-shaped token that could parse as a status field — starting it with the word `Resolved` is safe.

Leave the leading blank line and the `## Deferred Items` heading untouched.

Commit with explicit pathspec: `git commit -m "docs(260914-g63): make phase-05 deferred-item resolution parseable by audit" -- .planning/phases/05-agent-context-experience/deferred-items.md`.
  </action>
  <verify>
    <automated>grep -c '^  \*\*Status:\*\* resolved$' .planning/phases/05-agent-context-experience/deferred-items.md && node ~/.claude/gsd-core/bin/gsd-tools.cjs query audit-uat --raw > /tmp/g63-audit.json && node -e "const a=require('/tmp/g63-audit.json'); const p5=(a.summary.by_phase&&a.summary.by_phase['05'])||0; if(p5!==0){console.error('FAIL: phase 05 still reports '+p5+' open deferred item(s): '+JSON.stringify(a.results));process.exit(1)} console.log('OK: phase 05 reports 0 open deferred items')"</automated>
  </verify>
  <done>The file contains exactly one line matching the bold-status pattern with value exactly `resolved` (grep count 1), the strikethrough description and full 260910-kps explanation are preserved, and audit-uat reports zero open items for phase 05 (phase 03 still reports its 1 pre-split item at this point — expected). The commit contains only the phase-05 deferred-items.md.
  </done>
</task>

<task type="auto">
  <name>Task 3: Close the original phase-03 item parseably and re-file the typing debt as its own open item</name>
  <files>.planning/phases/03-embedding-vector-search/deferred-items.md</files>
  <action>
Rewrite the file's single section into TWO leaf-heading sections. Critical structural rule (verified against the parser this session): all sibling bullets under one `##` heading form ONE entry sharing one status — a resolved status anywhere in the 2026-09-03 section would resolve the whole section, so the typing-debt item MUST get its own `##` heading.

Section 1 — keep the existing `## 2026-09-03 — plan 03-07 execution` heading and restructure its single bullet into three lines, preserving the substance of the original text:
- Line 1, the bullet line: the original item with the leading `- [x] RESOLVED 2026-09-04 —` marker and trailing `(verify-work close-out follow-up).**` fragment cleaned up — start it `- ~~**mypy cannot run in this environment (pre-existing).**~~ (verify-work close-out follow-up).` then keep the root-cause explanation verbatim: CR-03 (bc441d0) bumped `pyproject.toml [tool.mypy]` `python_version` to 3.10 but legacy `mypy.ini` (phase 08-06) pinned `python_version = 3.9` and by mypy's config precedence completely shadowed pyproject, so mypy kept dying in third-party packages using 3.10+ syntax (sentence_transformers, then mcp); fix consolidated all per-module overrides into `pyproject.toml [[tool.mypy.overrides]]` (sif.cli.* click-decorator relaxation, sentence_transformers/numpy ignores, tests.* relaxations) and deleted `mypy.ini` — single source of truth; quality suite re-confirmed green (ruff clean, format clean, pytest 572 passed / 11 skipped).
- Line 2, indented continuation (two spaces): exactly `**Status:** resolved` and nothing else — same load-bearing constraint as Task 2 (value must be exactly `resolved`).
- Line 3, indented continuation (two spaces): starts `Resolved 2026-09-04 — \`mypy src/sif\` runs to completion after the fix (re-confirmed 2026-09-14).` and ends by pointing at the re-file: the strict-mode typing debt that remained (142 errors in 36 files at the time) is re-filed below as its own open deferred item rather than buried inside this resolved entry.

Section 2 — a NEW leaf heading `## 2026-09-14 — audit re-file: mypy strict-mode typing debt` followed by a two-line bullet item:
- Line 1, the bullet line: states the debt is untracked anywhere else in `.planning/` and replaces the resolved "mypy cannot run" item above; as of 2026-09-14 `uv run mypy src/sif` reports 140 errors in 36 files (76 checked); top error codes type-arg, no-any-return, attr-defined; hottest files search/rerank.py, indexing/watcher.py, mcp/handlers.py, embedding/manager.py; pre-existing strict-mode debt, unrelated to phase 03 scope.
- Line 2, indented continuation: `Disposition: tracked backlog — scheduling a dedicated typing-hardening phase is a future \`/gsd-phase\` decision; until then this item stays open by design so the audit keeps surfacing it.`
- This item must carry NO status field of any kind (no line beginning `status:` or `**Status:**`), which is what keeps it open and surfaced.

Run the definitive audit gate, then commit with explicit pathspec: `git commit -m "docs(260914-g63): close original phase-03 deferred item parseably; re-file mypy typing debt as open item" -- .planning/phases/03-embedding-vector-search/deferred-items.md`.
  </action>
  <verify>
    <automated>grep -c '^  \*\*Status:\*\* resolved$' .planning/phases/03-embedding-vector-search/deferred-items.md && grep -c '^## 2026-09-14 — audit re-file: mypy strict-mode typing debt$' .planning/phases/03-embedding-vector-search/deferred-items.md && node ~/.claude/gsd-core/bin/gsd-tools.cjs query audit-uat --raw > /tmp/g63-audit.json && node -e "const a=require('/tmp/g63-audit.json'); const ok=a.summary.total_items===1&&a.results.length===1&&a.results[0].phase==='03'&&a.results[0].items.length===1&&/2026-09-14/.test(a.results[0].items[0].name)&&/typing debt/i.test(a.results[0].items[0].name); if(!ok){console.error('AUDIT GATE FAILED — want exactly one open item (phase 03 typing debt), got: '+JSON.stringify(a.summary));process.exit(1)} console.log('AUDIT OK: exactly one open deferred item — phase 03 mypy typing debt (2026-09-14)')"</automated>
  </verify>
  <done>The file has exactly one parseable resolved-status line (grep count 1), the new 2026-09-14 leaf heading exists (grep count 1), and the definitive audit gate passes: summary.total_items == 1, by_phase {"03": 1}, the single reported item is the re-filed typing debt (name contains 2026-09-14 and typing debt), and phases 05 and 03-original no longer appear. The commit contains only the phase-03 deferred-items.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer planning docs -> GSD audit tooling | deferred-items.md is developer-authored input consumed by gsd-tools; no runtime product surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-G63-01 | Tampering | deferred-items.md edits | low | accept | Planning docs are developer-authored and git-tracked; no untrusted input crosses into them |
| T-G63-02 | Denial of Service | src/sif/cli/main.py cleanup_cmd | low | mitigate | Task 1 restores the intended sqlite3.OperationalError fallback so a legacy DB schema degrades to embeddings_removed = 0 instead of crashing with NameError |
</threat_model>

<verification>
After all three tasks, run the full gate set from the repo root:

1. Definitive audit gate (from Task 3) still green: `node ~/.claude/gsd-core/bin/gsd-tools.cjs query audit-uat --raw` reports exactly one item — the phase-03 typing debt.
2. `ruff check src tests` — clean.
3. `ruff format --check src tests` — clean.
4. `env -u FORCE_COLOR NO_COLOR=1 uv run mypy src/sif` — "Found 139 errors in 36 files (checked 76 source files)", zero errors mentioning src/sif/cli/main.py.
5. `env -u FORCE_COLOR NO_COLOR=1 uv run pytest` — green (exit 0; 667 passed expected per STATE.md baseline).
6. `git log --stat` for this task's commits: each commit touches ONLY its pathspec'd file(s) — the pre-existing staged mypy.ini deletion and all other dirty-tree changes remain outside these commits, still present in the working tree/index afterwards.
</verification>

<success_criteria>
- audit-uat reports exactly one open deferred item: phase 03 mypy strict-mode typing debt, re-filed 2026-09-14.
- Both historical resolutions (05 JSON output fix via 260910-kps; 03 mypy.ini consolidation) are parseable by the audit while preserving their full explanation text.
- The mypy typing debt is now tracked as a first-class open deferred item with concrete numbers (140 errors / 36 files / 2026-09-14) and a disposition, instead of being buried in resolved prose.
- cleanup_cmd's OperationalError fallback works as designed; mypy name-defined eliminated (140 -> 139 errors).
- Quality suite green; three pathspec-only commits, dirty tree otherwise untouched.
</success_criteria>

<output>
Create `.planning/quick/260914-g63-audit-follow-up-fix-deferred-items-doc-f/260914-g63-SUMMARY.md` when done
</output>
