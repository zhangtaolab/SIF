---
phase: 260914-g63-audit-follow-up-fix-deferred-items-doc-f
plan: 01
type: execute
subsystem: planning-audit
tags: [audit-uat, deferred-items, mypy, sqlite3, quick-task]
requires:
  - "gsd-core uat.cjs deferred-items parser (leaf-heading entries, bold status field)"
provides:
  - "audit-uat ground truth: exactly one open deferred item — phase 03 mypy strict-mode typing debt (2026-09-14)"
  - "cleanup_cmd OperationalError fallback that degrades to embeddings_removed = 0 instead of NameError"
affects:
  - ".planning/phases/03-embedding-vector-search/deferred-items.md"
  - ".planning/phases/05-agent-context-experience/deferred-items.md"
  - "src/sif/cli/main.py"
tech-stack:
  added: []
  patterns:
    - "deferred-item resolution format: description line / '**Status:** resolved' alone on its line / 'Resolved <date> — ...' continuation line (parser takes full line remainder as field value)"
    - "one leaf '##' heading per deferred entry — sibling bullets share one status, so an open item must never live under a resolved sibling's heading"
key-files:
  created: []
  modified:
    - src/sif/cli/main.py
    - .planning/phases/05-agent-context-experience/deferred-items.md
    - .planning/phases/03-embedding-vector-search/deferred-items.md
decisions:
  - "Status value must sit alone on its '**Status:** resolved' line — any date/prose on that line breaks parsing because uat.cjs takes the full line remainder as the value"
  - "Typing debt re-filed under its own '## 2026-09-14' leaf heading with NO status field, keeping it open and audit-surfaced by design (tracked backlog; /gsd-phase is the scheduling path)"
metrics:
  duration: 8min
  completed: "2026-09-14T04:12:00Z"
plan_head_before: c91d293facacf9a0e1be55a40a0366a512937017
commits: 3
actuals:
  tokens: 1455   # chars/4 over realized diff (5818 diff chars, git diff c91d293..HEAD)
  tasks: 3
  commits: 3     # measured: git rev-list --count c91d293..HEAD
status: complete
---

# Quick Task 260914-g63: Audit Follow-up — Fix Deferred-items Doc Format + sqlite3 Import Summary

**One-liner:** Made two substantively-resolved deferred items parseable by audit-uat (bold `**Status:** resolved` on its own line), re-filed the 140-error mypy strict-mode typing debt as a tracked open item under its own 2026-09-14 leaf heading, and fixed the latent `sqlite3` NameError in `cleanup_cmd` (mypy 140 → 139).

## What Was Done

### Task 1 — `import sqlite3` in cli/main.py (commit 0ab1658)

- Added `import sqlite3` to the stdlib block after `from __future__ import annotations`, before `from pathlib import Path` (ruff isort ordering).
- Removed the now-unused `# noqa: F821` from `cleanup_cmd`'s `except sqlite3.OperationalError:` (RUF100 would flag it).
- Effect: an OperationalError on the `document_embeddings` DELETE (legacy DB without the table) now degrades to `embeddings_removed = 0` as designed instead of raising `NameError` (threat T-G63-02 mitigated).
- Verified: mypy `Found 139 errors ...`, zero error lines mention `src/sif/cli/main.py`; ruff check + format clean.

### Task 2 — Phase-05 deferred item restructured (commit 95e7ceb)

- Split the single bullet into: strikethrough description line / `  **Status:** resolved` (alone on its line) / `  Resolved 2026-09-10 — root cause was Rich console.print word-wrapping ...` continuation.
- All original description and 260910-kps explanation text preserved verbatim.
- Verified: grep count 1 for the bold-status pattern; audit-uat reports 0 open items for phase 05.

### Task 3 — Phase-03 item closed parseably + typing debt re-filed (commit ae2cb6d)

- `## 2026-09-03 — plan 03-07 execution` section: bullet restructured to description line / `  **Status:** resolved` / `  Resolved 2026-09-04 — ...` continuation; mypy.ini-shadowing root cause and fix preserved.
- New leaf heading `## 2026-09-14 — audit re-file: mypy strict-mode typing debt` with an open item: 140 errors / 36 files / 76 checked as of 2026-09-14; top codes type-arg, no-any-return, attr-defined; hottest files search/rerank.py, indexing/watcher.py, mcp/handlers.py, embedding/manager.py; `Disposition: tracked backlog — ... future /gsd-phase decision ...` and deliberately NO status field.
- Own leaf heading is load-bearing: sibling bullets under one `##` share one status, so the open item must not live under the resolved 2026-09-03 heading.
- Verified (definitive audit gate): `summary.total_items == 1`, `by_phase {"03": 1}`, item name contains "2026-09-14" and "typing debt".

## Verification Results

1. **Audit gate: PASS** — `node ~/.claude/gsd-core/bin/gsd-tools.cjs query audit-uat --raw`: exactly one open item, phase 03, the re-filed typing debt (baseline before task: 2 items, {"03":1,"05":1}).
2. **`ruff check src tests`: clean.**
3. **`ruff format --check src tests`: clean** (131 files already formatted).
4. **mypy: `Found 139 errors in 35 files (checked 76 source files)`** — down from 140; zero errors in `src/sif/cli/main.py`. (35 files, not the plan-done's "36": removing main.py's single error also removes main.py from the errored-file set — the plan's done-prose arithmetic slip; the must_have "139 errors, down from 140, zero in cli/main.py" is met exactly.)
5. **pytest: 665 passed, 2 failed** — see Pre-existing Test Failures below. Identical result with this task's changes fully reverted.
6. **Commit hygiene: PASS** — each commit touches exactly its one pathspec'd file (git log --stat); the pre-existing dirty state (staged `mypy.ini` deletion, modified `.planning/WINDOWS.md`, `.planning/config.json`, `uv.lock`, deleted `.continue-here.md`, untracked `.gsd/`, `.planning/state.json`, `.planning/tmp/`, `.planning/ui-reviews/`) remains exactly as found.

## Deviations from Plan

None — plan executed as written. Two documented observations, no action taken:

- **Plan-done prose said mypy would report "139 errors in 36 files"**; actual is 139 errors in 35 files. Arithmetic slip in the plan (36 − 1 file that no longer errors); the substantive must_have (139, down from 140, zero in cli/main.py) is met.
- **Plan expected "pytest green (667 passed)"**; actual is 665 passed / 2 failed. Proven pre-existing (below), so out of scope per the plan's scope boundary.

## Pre-existing Test Failures (out of scope, proven not caused by this task)

`tests/unit/embedding/test_openai_embedder.py::TestOpenAIEmbedderBehavior::test_import_error_logs_install_hint` and `::TestOpenAIEmbedderDimensionCache::test_corrupt_cache_treated_as_miss` fail in the full suite with empty `caplog.text` (they pass in isolation — 18/18 file-local).

- **Mechanism:** `setup_logging` (src/sif/utils/logging.py lines 56 and 130) sets `propagate = False`; any earlier test invoking a CLI command (per WINDOWS.md entry 3, `test_docs.py`) poisons caplog capture for every later test.
- **Causality proof:** temporarily reverted this task's entire main.py edit and re-ran the full suite — identical `2 failed, 665 passed`. The two deferred-items.md edits cannot affect tests (no test references `.planning`). Total test count unchanged (667).
- **Already tracked:** STATE.md "Next expected action" lists "caplog test pollution" as a 04-UAT deferred follow-up; WINDOWS.md entry 3 (2026-09-05, quick-260905-tax) records the same two failures with the polluting test identified. This task appended WINDOWS.md entry 4 (kind `unmet-truth`) recording today's re-confirmation.

## Auth Gates

None.

## Known Stubs

None — no code stubs introduced. The new open deferred item (typing debt) is intentional tracking, not a stub.

## Self-Check: PASSED

- Files exist: src/sif/cli/main.py, .planning/phases/05-agent-context-experience/deferred-items.md, .planning/phases/03-embedding-vector-search/deferred-items.md — all FOUND.
- Commits exist on main: 0ab1658, 95e7ceb, ae2cb6d — all FOUND (git log).
- Audit ground truth re-confirmed after all tasks: total_items == 1, phase 03 typing debt.
