---
phase: 06-documentation-audit-refresh
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/quickstart.md
  - README.md
  - docs/models.md
  - docs/mcp-server.md
autonomous: true
requirements: [DOC-03, DOC-04, DOC-05]
gap_closure: true
gap_ids: [G-06-3, G-06-4, G-06-6, G-06-7]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The quickstart jq piping example returns document paths against the CLI's real --json output shape (G-06-3 truth)"
    - "README.md example flags exist on the live CLI — the collection remove example uses the real confirmation-skip flag (G-06-4 truth)"
    - "docs/models.md EmbeddingConfig default model_type matches src/sif/models/embedding.py (G-06-6 truth)"
    - "docs/mcp-server.md documents all three mcp subcommands (stdio/http/daemon, with daemon's not-yet-implemented status stated honestly) and the real default database path (G-06-7 truth)"
  artifacts:
    - docs/quickstart.md
    - README.md
    - docs/models.md
    - docs/mcp-server.md
  key_links:
    - "quickstart jq filter -> search --json top-level array with path keys (src/sif/cli/commands/search.py:44-48, core/models.py SearchResult.to_dict)"
    - "README remove example -> live collection remove flag surface"
    - "mcp-server.md db default -> src/sif/config/constants.py:11 + Settings.get_db_path (settings.py:195-204)"
    - "mcp-server.md daemon section -> src/sif/cli/commands/mcp.py:75-91 actual behavior"
---

<objective>
Hand-sync the four documentation spots the generators cannot reach (G-06-3, G-06-4, G-06-6, G-06-7).

Purpose: `make docs-generate` only rewrites cli-reference.md and configuration.md (plan 06-08). quickstart.md, README.md, models.md, and mcp-server.md are hand-maintained, and each carries one diagnosed defect: a jq filter targeting an output shape the CLI never emits, a nonexistent confirmation-skip flag, a stale dataclass default, and an undocumented subcommand plus a wrong db-path default.

Output: Four docs corrected against live code, docs validator suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-audit-refresh/06-UAT.md

@docs/quickstart.md
@README.md
@docs/models.md
@docs/mcp-server.md
@src/sif/cli/commands/mcp.py
@src/sif/config/constants.py
@src/sif/models/embedding.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix quickstart jq filter, README remove flag, models.md EmbeddingConfig default</name>
  <files>docs/quickstart.md, README.md, docs/models.md</files>
  <precondition>Baseline suite green (675 passed / 0 failed); working tree clean for the three files.</precondition>
  <action>
Three single-line corrections, each verified against live code:

1. docs/quickstart.md:359 (Export Results section): the jq filter targets a wrapper-object shape the CLI does not emit. Live behavior: `sif search query "..." --json` prints a top-level JSON array of result dicts whose path key is `path` (src/sif/cli/commands/search.py:44-48 builds `json.dumps([r.to_dict(), ...])`; `SearchResult.to_dict` in src/sif/core/models.py:223-228 emits `path`). Replace the filter with one that iterates the top-level array and selects the path key — the currently documented filter silently returns nothing (G-06-3).

2. README.md:89 (collection management example block): the collection remove example passes a confirmation-skip flag that does not exist on the live command; the real flag is `--yes` (confirm with `sif collection remove --help`; docs/cli-reference.md:201-205 already documents it correctly). Swap the flag on that one example (G-06-4). Do not touch other README content — the UAT verified everything else in README and docs/index.md as accurate.

3. docs/models.md:283 (EmbeddingConfig pydantic model block): the `model_type` field default is shown as the sentence-transformers enum member; the live default in src/sif/models/embedding.py:21 is the modelscope member. Sync the default in the example block; all other dataclass field listings were verified accurate by the UAT and must not change (G-06-6).

After the edits, run the docs validator — these files are all in its DOCS_FILES list, and the python block in models.md must stay syntactically valid under its AST check.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && grep -cF "jq '.[].path'" docs/quickstart.md && [ "$(grep -cF '.results[].document_path' docs/quickstart.md)" = "0" ] && grep -c 'collection remove old-collection --yes' README.md && [ "$(grep -cF 'old-collection --force' README.md)" = "0" ] && grep -c 'Field(ModelType.MODELSCOPE' docs/models.md && [ "$(grep -c 'Field(ModelType.SENTENCE_TRANSFORMERS' docs/models.md)" = "0" ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v</automated>
  </verify>
  <done>quickstart's jq example returns paths from the real --json array; README's remove example uses the live confirmation-skip flag; models.md shows the live EmbeddingConfig model_type default; docs validator green.</done>
</task>

<task type="auto">
  <name>Task 2: mcp-server.md — document the daemon subcommand honestly and fix the default db path</name>
  <files>docs/mcp-server.md</files>
  <action>
1. Transport Types section (mcp-server.md:30-64 currently covers stdio and http only): add a third subsection for the daemon transport, matching the live command's actual behavior — the subcommand exists (`sif mcp daemon`, defined at src/sif/cli/commands/mcp.py:75-91) but currently exits with a not-yet-implemented error directing users to `mcp http`. Document it honestly as reserved/not yet functional with that guidance; do not advertise capabilities it lacks (G-06-7). Keep the example bash block consistent with the existing stdio/http subsections' format.

2. Default database path: every occurrence of the stale default (the pre-rename path shown at mcp-server.md:185 and :197 — check for additional occurrences with a repo grep) must be replaced with the real default `~/.local/share/sif/sif.db`, sourced from src/sif/config/constants.py:11 (`DEFAULT_DB_PATH`) and the platformdirs-based computation in `Settings.get_db_path` (src/sif/config/settings.py:195-204). Where prose allows, note that the path is computed in the platform data directory on first run (G-06-7).

The validator already knows the daemon path exists (`_SUBCOMMANDS` in tests/test_docs.py includes it) and skips executing it (`SKIP_COMMAND_PATTERNS` includes it), so the new example block validates without execution.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && grep -c 'sif mcp daemon' docs/mcp-server.md && grep -c '~/.local/share/sif/sif.db' docs/mcp-server.md && [ "$(grep -cF '~/.sif/index.sqlite' docs/mcp-server.md)" = "0" ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v</automated>
  </verify>
  <done>mcp-server.md documents all three mcp subcommands with daemon's unimplemented status stated truthfully, and both the env-var table and .env example carry the real computed default db path; docs validator green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs readers -> CLI behavior | documentation examples must not promise flags/paths/commands that differ from live behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Repudiation | docs/mcp-server.md daemon section | medium | mitigate | Daemon documented with its exact live failure mode (raises not-yet-implemented, directs to http) rather than aspirational behavior, so users cannot be misled into relying on it |
| T-06-06 | Information Disclosure | docs/mcp-server.md db path | low | mitigate | Correct documented default removes confusion about where the index (user content) actually lives |
</threat_model>

<verification>
1. All four gap-specific greps in the task verifies pass (positive greps find the corrected content, negative greps return 0).
2. `env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v` green (all four files are in DOCS_FILES).
3. `env -u FORCE_COLOR NO_COLOR=1 python -m pytest -q` full suite at the 675 baseline (docs-only change; no source touched).
</verification>

<success_criteria>
- G-06-3, G-06-4, G-06-6, G-06-7 truths from 06-UAT.md all demonstrably true via the greps and validator above.
- No other content in the four files changed beyond the diagnosed defects.
</success_criteria>

<output>
Create `.planning/phases/06-documentation-audit-refresh/06-09-SUMMARY.md` when done
</output>
