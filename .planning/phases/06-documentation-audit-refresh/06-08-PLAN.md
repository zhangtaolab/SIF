---
phase: 06-documentation-audit-refresh
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_cli_ref.py
  - scripts/generate_config_ref.py
  - docs/cli-reference.md
  - docs/configuration.md
  - tests/test_docs.py
  - .github/workflows/docs.yml
autonomous: true
requirements: [DOC-01, DOC-02, DOC-06, DOC-07]
gap_closure: true
gap_ids: [G-06-1, G-06-2, G-06-8]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "make docs-generate output passes make docs-test with zero failures (G-06-8 truth)"
    - "docs/cli-reference.md option tables carry the post-phase-06 CLI surface: --quiet on the three search commands, --cors-origins on mcp http, Choice-typed --chunk-strategy with its choices listed, and a tilde-relative --config default (G-06-1 truth)"
    - "docs/configuration.md defaults match live Settings (SIF_MODEL_TYPE=modelscope, SIF_RERANKER_MODEL_TYPE=sentence_transformers) and its validation table matches the live validator's valid set with no phantom env vars (G-06-2 truth)"
    - "Running the generators a second time produces no diff against the committed docs (machine-independent output)"
    - "The CI drift check fails the docs job when committed reference docs diverge from regeneration"
  artifacts:
    - scripts/generate_cli_ref.py
    - scripts/generate_config_ref.py
    - docs/cli-reference.md
    - docs/configuration.md
    - tests/test_docs.py
    - .github/workflows/docs.yml
  key_links:
    - "generate_cli_ref.py -> sif.cli.main cli param introspection -> docs/cli-reference.md option tables"
    - "generate_config_ref.py -> Settings.model_fields + validate_model_type source scan -> docs/configuration.md tables and validation rules"
    - ".github/workflows/docs.yml drift step -> regenerated committed docs (blocking gate)"
---

<objective>
Repair the documentation generation pipeline (G-06-8, the root-cause gap), then regenerate both reference docs so they match live code (G-06-1, G-06-2).

Purpose: The two generator scripts were never updated after phase 06-06's hand fixes, so `make docs-generate` currently emits docs that FAIL the project's own validator (3 failures), and the non-blocking CI drift gate let live-code drift accumulate unchecked. Until the regeneration loop is trustworthy, every other docs fix is a hand-patch waiting to be reverted.

Output: Fixed `scripts/generate_cli_ref.py` and `scripts/generate_config_ref.py`, regenerated `docs/cli-reference.md` and `docs/configuration.md`, regression locks in `tests/test_docs.py`, and a blocking drift gate in `.github/workflows/docs.yml`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-audit-refresh/06-UAT.md

@scripts/generate_cli_ref.py
@scripts/generate_config_ref.py
@src/sif/config/settings.py
@src/sif/cli/main.py
@tests/test_docs.py
.github/workflows/docs.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix generate_cli_ref.py — portable defaults, Choice types, valid workflow examples</name>
  <files>scripts/generate_cli_ref.py</files>
  <precondition>Working tree is clean for docs/cli-reference.md (regeneration overwrites it; nothing uncommitted may be clobbered). Baseline suite: 675 passed / 0 failed.</precondition>
  <action>
Fix the three defects diagnosed in G-06-1 and G-06-8, keeping Click introspection as the single source of truth per D-01/D-02 (no hardcoded option rows):

1. Portable path defaults (G-06-1(d)): in `_format_default`, when a str/Path default renders with the current user's home directory as prefix (compare against `str(Path.home())`), rewrite that prefix to `~`. Root cause: the global `--config` option's default is `str(DEFAULT_CONFIG_PATH)` where main.py:24 builds it from `Path.home()` at import, so the generator currently bakes a machine-absolute home path under the author's username into the committed table. The rewritten form (`~/.config/sif`) matches `DEFAULT_CONFIG_PATH` in src/sif/config/constants.py:13 and is identical on every machine — which is also the prerequisite for the blocking CI gate in Task 3.

2. Choice-typed options (G-06-1(c)): in `_format_type`, render `click.Choice` params as `choice [a|b|c]` from `param.type.choices` instead of the bare type name, so the Choice-converted `--chunk-strategy` (WR-06, commit bda12a2) shows its real type and allowed values like the live `--help` does.

3. Workflow example commands the validator rejects (G-06-8): in the Common Workflows section of `generate_cli_reference()` (script lines ~251-265), fix the two invalid examples: the index-update example currently passes the collection as a positional but the live command takes no positional — emit it with the `--collection` flag; and the search example currently invokes the bare `search` group — emit it through the `query` leaf subcommand. These are what trip `test_no_positional_args_for_option_only_commands` and `test_no_bare_search_command` on regeneration.

Then run the generator and confirm the output passes the docs validator. The options tables should now automatically pick up `-q, --quiet` on the search trio (added in 2f7cbb4) and `--cors-origins` on `mcp http` (phase 09) because they come from live param introspection.

Keep ruff clean: `ruff check scripts/generate_cli_ref.py` and `ruff format --check scripts/generate_cli_ref.py`.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && python scripts/generate_cli_ref.py && grep -c '`--quiet`' docs/cli-reference.md && grep -c 'cors-origins' docs/cli-reference.md && grep -c 'auto|fixed|markdown|code' docs/cli-reference.md && grep -c '~/.config/sif' docs/cli-reference.md && [ "$(grep -c '/Users/' docs/cli-reference.md)" = "0" ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v</automated>
  </verify>
  <done>Regenerated docs/cli-reference.md contains the quiet flag rows on all three search commands, the mcp http CORS option, a Choice-typed chunk-strategy row with its choices, and a tilde-relative --config default with zero machine-absolute home paths; tests/test_docs.py passes against the regenerated file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix generate_config_ref.py — live-derived validation row, real defaults, truthful sections</name>
  <files>scripts/generate_config_ref.py</files>
  <action>
Fix the stale baked content diagnosed in G-06-2 and G-06-8. The options tables already derive from `Settings.model_fields` introspection (they will self-correct on regeneration); the defects are in the hand-written sections:

1. Validation Rules table (script ~:191-192) and the validation error example (~:278-281): both hardcode a valid-model_type list that still names a backend removed by WR-07 (commit 60a2c08) — the live validator at src/sif/config/settings.py:161 (`valid_types` inside `validate_model_type`) rejects it. Replace both hardcoded lists with values derived from the live source: the script already scans `inspect.getsource(Settings)` in `get_validators()`, so regex-extract the `valid_types = {...}` set literal from the `validate_model_type` body and render the rule row and the sorted-list error message (matching the format of settings.py:163's message) from that set. Fail the generator loudly (raise) if the extraction finds no set, so future refactors cannot silently emit an empty list.

2. Complete .env example (script ~:232, :237): the embedding and reranker model-type lines are hardcoded to pre-WR-07 values. Derive both values from `Settings.model_fields["model_type"].default` and `Settings.model_fields["reranker_model_type"].default` so they track the live defaults (currently modelscope / sentence_transformers) automatically.

3. Viewing Current Configuration section (script ~:286-298): it emits a nonexistent config subcommand (twice). Phase 06-06 hand-fixed the committed doc to use `sif status`; make the generator emit the same real command so regeneration cannot revert that fix (G-06-8). Do not add a verbose variant to the status invocation unless the flag is placed at group level.

4. Troubleshooting section (script ~:327-344): remove the speculative env-file override export line — pydantic-settings reads a fixed relative `.env` (Settings.model_config `env_file=".env"`, settings.py:19-24) and honors no override variable. Replace with truthful guidance: the file must be named `.env` and sit in the working directory where `sif` is invoked.

5. Configuration Methods / Precedence (script ~:104-142): drop the home-directory `.env` lookup claim in both the section text and the numbered precedence list — live precedence is environment variables, then working-directory `.env`, then defaults.

Keep the computed db_path/cache_dir defaults hardcoded in `get_computed_default` per the phase 06-02 decision (calling the real getters creates directories as a side effect). Keep ruff clean on the script.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && python scripts/generate_config_ref.py && grep -c '`modelscope`' docs/configuration.md && grep -c '`sentence_transformers`' docs/configuration.md && [ "$(grep -ci 'huggingface' docs/configuration.md)" = "0" ] && [ "$(grep -c 'SIF_ENV_FILE' docs/configuration.md)" = "0" ] && [ "$(grep -c 'sif config show' docs/configuration.md)" = "0" ] && [ "$(grep -c 'home directory' docs/configuration.md)" = "0" ] && grep -c 'sif status' docs/configuration.md && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v</automated>
  </verify>
  <done>Regenerated docs/configuration.md shows the live defaults (model_type=modelscope in table and .env example, reranker_model_type=sentence_transformers), a validation row and error example matching the live validator's valid set with the removed backend absent everywhere, the status command in the viewing section, no phantom env-file override variable, and no home-directory .env precedence claim; tests/test_docs.py passes against the regenerated file.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate reference docs, lock regressions in the validator, make the CI drift gate blocking</name>
  <files>docs/cli-reference.md, docs/configuration.md, tests/test_docs.py, .github/workflows/docs.yml</files>
  <precondition>Tasks 1 and 2 completed (generators fixed); regenerated working-tree docs already pass tests/test_docs.py.</precondition>
  <reversibility rating="reversible">The CI gate flip is a one-line revert; the validator additions are additive tests.</reversibility>
  <action>
1. Regenerate and commit both reference docs via `make docs-generate`. The committed docs become the output of the fixed generators (this closes the option-level and default-level drift of G-06-1/G-06-2).

2. Regression locks in tests/test_docs.py:
   a. Extend the phantoms list in `test_configuration_no_phantom_fields` with the phantom env-file override variable named in UAT gap G-06-2 (docs/configuration.md:206 pre-fix) so the generator can never reintroduce it.
   b. Add one test method (e.g. `test_configuration_model_type_validation_is_truthful`) asserting both directions of the WR-07 drift: `Settings.model_validate({"model_type": <backend removed by WR-07, named in G-06-2>})` raises a pydantic ValidationError, AND the regenerated configuration.md does not list that backend as a valid model_type anywhere. Import `Settings` from `sif.config.settings` and `ValidationError` from pydantic in tests/test_docs.py.

3. Blocking CI drift gate (.github/workflows/docs.yml, ~:38): drop the always-succeed suffix from the `git diff --exit-code docs/cli-reference.md docs/configuration.md` step so divergence between committed and regenerated docs fails the Docs Validation job. Phase 06-07 deliberately chose the non-blocking form to avoid blocking PRs; UAT G-06-8's root cause names that non-blocking gate as the reason the regeneration loop rotted silently, and Task 1's tilde-rewrite made generator output machine-independent, so the false-positive risk (user-specific home paths baked into committed docs) is gone. Keep the docs-test step before the drift step and change nothing else in the workflow (same triggers, same setup, no new permissions).

4. Full quality suite per CLAUDE.md, plus explicit lint on the changed script/test files (the CLAUDE.md suite covers src and tests only): `ruff check src tests scripts/generate_cli_ref.py scripts/generate_config_ref.py`, `ruff format --check src tests scripts/generate_cli_ref.py scripts/generate_config_ref.py`, then the full pytest run with the FORCE_COLOR hygiene from project memory. Baseline is 675 passed / 0 failed plus the one new docs test; the 2 order-dependent caplog failures in tests/unit/embedding/test_openai_embedder.py are pre-existing and known.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && make docs-generate && [ -z "$(git status --porcelain docs/cli-reference.md docs/configuration.md)" ] && make docs-generate && [ -z "$(git status --porcelain docs/cli-reference.md docs/configuration.md)" ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v && python -c "import yaml; yaml.safe_load(open('.github/workflows/docs.yml')); print('docs.yml valid')" && [ "$(grep -c '|| true' .github/workflows/docs.yml)" = "0" ]</automated>
  </verify>
  <done>Committed reference docs are byte-identical to a fresh regeneration (generate-twice determinism verified), docs validator is green (13 tests incl. the new model_type truthfulness lock and extended phantom list), docs.yml parses, drift check is blocking, and the full quality suite passes at or above the 675 baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> generator scripts | scripts/generate_*.py read local source and write docs/ files |
| CI runner -> workflow file | .github/workflows/docs.yml edited in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | .github/workflows/docs.yml | high | mitigate | Gate is being strengthened, not weakened: same triggers/setup, no new permissions, secrets, or external actions; the only change makes an existing check able to fail the job |
| T-06-02 | Tampering | scripts/generate_cli_ref.py, scripts/generate_config_ref.py | medium | mitigate | Generators write only the two explicit docs paths via Path.write_text; no shell-out, no network; run through the reviewed make target |
| T-06-03 | Information Disclosure | docs/cli-reference.md | low | mitigate | Task 1 tilde-rewrite removes user-specific absolute home paths from generated output (current committed doc leaks the author's username) |
| T-06-04 | Denial of Service | CI drift gate (blocking) | low | accept | A future CLI change without doc regeneration now fails CI loudly by design; the fix (run make docs-generate) is documented in the workflow itself and the Makefile help |
</threat_model>

<verification>
1. `make docs-generate && make docs-test` — regenerated output passes the project's own validator (the G-06-8 truth, directly).
2. Generate-twice determinism: second `make docs-generate` leaves `git status --porcelain` empty for both reference docs.
3. `env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v` — 13 passed (12 baseline + 1 new regression lock).
4. `env -u FORCE_COLOR NO_COLOR=1 python -m pytest -q` — full suite green vs 675 baseline (known pre-existing order-dependent caplog failures in tests/unit/embedding/test_openai_embedder.py excluded).
5. `ruff check src tests scripts/generate_cli_ref.py scripts/generate_config_ref.py` and `ruff format --check` on the same paths.
6. `grep -c '|| true' .github/workflows/docs.yml` returns 0 and the YAML parses.
</verification>

<success_criteria>
- G-06-8 closed: generator scripts emit validator-passing docs; CI drift check is blocking.
- G-06-1 closed: cli-reference.md option tables match live Click params including --quiet, --cors-origins, Choice-typed --chunk-strategy, portable --config default.
- G-06-2 closed: configuration.md defaults and validation rules match live Settings with no phantom env vars.
- All three gaps' truths in 06-UAT.md are demonstrably true via the commands above.
</success_criteria>

<output>
Create `.planning/phases/06-documentation-audit-refresh/06-08-SUMMARY.md` when done
</output>
