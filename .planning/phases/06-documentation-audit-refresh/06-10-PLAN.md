---
phase: 06-documentation-audit-refresh
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_arch_diagram.py
  - docs/architecture.md
autonomous: true
requirements: [DOC-05]
gap_closure: true
gap_ids: [G-06-5]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "docs/architecture.md module structure enumerates the live src/sif tree — the unified MCP package with its real files, the real embedding/search/utils/indexing/cli module sets — and lists no file that does not exist (G-06-5 truth)"
    - "The Mermaid dependency graph is the actual output of scripts/generate_arch_diagram.py, which derives nodes and edges from scanned imports (D-10 honored for real)"
    - "The factory Supported Models list matches implemented backends only; the indexing flow uses the live collection-add argument order; the Future Enhancements list contains only unshipped items"
  artifacts:
    - scripts/generate_arch_diagram.py
    - docs/architecture.md
  key_links:
    - "generate_arch_diagram.py AST import scan -> package dependency graph -> Mermaid block in architecture.md"
    - "architecture.md module tree -> live `find src/sif -name '*.py'` listing"
    - "architecture.md indexing flow -> live `collection add PATH --name` signature"
---

<objective>
Rewrite docs/architecture.md against the live source tree and make the Mermaid diagram genuinely generated (G-06-5).

Purpose: architecture.md was last content-updated in phase 06 and still describes a directory layout that phase 09 deleted, mislabels the unified MCP package as legacy, lists phantom files while omitting eight real ones, advertises an unimplemented factory backend, shows a pre-phase-05 argument order, and files two implemented features under Future Enhancements. Its diagram script computes a dependency graph from imports and then throws it away, emitting hardcoded edges — so the "generated from source" claim is false and the diagram cannot track reality.

Output: A truthful architecture.md and an actually-derived diagram script; docs validator green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation-audit-refresh/06-UAT.md

@docs/architecture.md
@scripts/generate_arch_diagram.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make generate_arch_diagram.py emit the dependency graph it computes</name>
  <files>scripts/generate_arch_diagram.py</files>
  <precondition>Working tree clean for the script and docs/architecture.md.</precondition>
  <action>
The script already scans first-party imports (`extract_imports`, `scan_modules`) and builds a package-level dependency graph (`build_dependency_graph` returns `pkg_deps`) — and then discards it: `generate_mermaid()` emits a hardcoded label map and hardcoded edge list instead. That hardcoded output is exactly what drifted (it still contains a node for the package deleted in phase 09, mislabeled as the refactored one, and labels the surviving unified package as legacy — see UAT G-06-5 reason).

Rewrite `generate_mermaid()` so the emitted graph IS the computation:
1. Emit one node per package key actually present in the computed `pkg_deps`/modules mapping (do not enumerate packages by hand). Keep a label map for friendly display names, updated so the MCP node reads as the unified server (no legacy/refactored qualifiers); any discovered package without a label falls back to the package name.
2. Emit edges from the computed `pkg_deps` only — delete the hardcoded `edge_defs` list entirely.
3. Sort node and edge emission deterministically (sorted package names, sorted destination lists) so repeated runs produce byte-identical output.
4. Keep the module-name construction and import extraction as-is; they are correct for the current tree.

This honors phase decision D-10 (Mermaid in architecture.md is scripted from source imports) for real. Keep ruff clean: `ruff check scripts/generate_arch_diagram.py`, `ruff format --check scripts/generate_arch_diagram.py`.
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && python scripts/generate_arch_diagram.py > /tmp/arch1.mmd && python scripts/generate_arch_diagram.py > /tmp/arch2.mmd && diff /tmp/arch1.mmd /tmp/arch2.mmd && [ "$(grep -c 'mcp_server' /tmp/arch1.mmd)" = "0" ] && grep -c 'graph TD' /tmp/arch1.mmd && ruff check scripts/generate_arch_diagram.py && ruff format --check scripts/generate_arch_diagram.py</automated>
  </verify>
  <done>Script runs cleanly, output is deterministic across runs, contains no node for the deleted package, and its edges come from the computed import graph rather than a hardcoded list.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite architecture.md module tree, factory list, flows, Mermaid, and future list against the live tree</name>
  <files>docs/architecture.md</files>
  <precondition>Task 1 completed (deterministic diagram output available).</precondition>
  <action>
Rework the stale sections; leave the sections the UAT verified as accurate (layer descriptions, database schema DDL, key interfaces, search flow) intact unless they contradict the live tree. Ground everything in `find src/sif -name '*.py' | sort` run at execution time.

1. Module Structure tree (~:227-310): rebuild the listing from the live tree. Required corrections, all enumerated with line references in UAT G-06-5 reason:
   - Replace the MCP section: the package deleted in phase 09 is gone; the live unified package is `src/sif/mcp/` with backend.py, cli.py, handlers.py, protocol.py, server.py, and a transports/ subpackage (http.py, stdio.py). The phantom legacy filenames listed in the current tree do not exist and must not appear.
   - Fix the embedding section to the real module set: manager.py, factory.py, model.py, cache.py, embedder.py (the download helper the old tree misplaced here actually lives at models/download.py).
   - Add the omitted real files: indexing/watcher.py; search/context_attach.py and search/term_match.py; utils/progress.py and utils/text.py; cli/config.py; models/download.py; and the missing cli command modules (get.py is the get group, not a single multi-get command).
   - database/repository.py exists; describe it accurately (kept alongside repositories.py for indexer compatibility) rather than as part of the deleted-package story.

2. Factory Supported Models list (~:132-137): remove the unimplemented hub backend (the factory raises NotImplementedError for it — G-06-5 reason names it); keep GGUF, sentence-transformers, OpenAI-compatible API, and ModelScope. The rewritten doc must not mention that backend anywhere.

3. Indexing Flow step 1 (~:161): use the live argument order — path positional first, collection name via its flag: `sif collection add ~/notes --name my-collection` (phase 05 reordering).

4. Module Dependency Graph (~:314-346): replace the Mermaid block with the exact output of `python scripts/generate_arch_diagram.py` from Task 1. The italic note below the block (~:348) claiming generation from actual source imports becomes true again — keep it.

5. Future Enhancements (~:512-520): remove the two implemented items named in G-06-5 reason (checksum-based incremental indexing shipped with the idempotent embed work; cross-encoder-style reranking shipped with the Qwen3 reranker). Keep genuinely unshipped items: the real-time watcher (watcher.py exists but is wired to no CLI/MCP surface), plugin system, web UI, query suggestions.

All python code blocks must stay AST-parseable and all shell examples must name real commands (the docs validator checks both — architecture.md is in its DOCS_FILES list).
  </action>
  <verify>
    <automated>cd /Users/forrest/GitHub/SIF && [ "$(grep -c 'mcp_server' docs/architecture.md)" = "0" ] && [ "$(grep -cE 'server_http|transport_stdio|transport_http' docs/architecture.md)" = "0" ] && [ "$(grep -ci 'huggingface' docs/architecture.md)" = "0" ] && [ "$(grep -cF 'collection add my-collection ~/notes' docs/architecture.md)" = "0" ] && [ "$(grep -cF 'Only index changed documents' docs/architecture.md)" = "0" ] && [ "$(grep -cF 'Cross-encoder reranking' docs/architecture.md)" = "0" ] && grep -cE 'watcher\.py|term_match\.py|context_attach\.py' docs/architecture.md && grep -c 'collection add ~/notes --name my-collection' docs/architecture.md && python scripts/generate_arch_diagram.py > /tmp/gen.mmd && awk '/```mermaid/,/^```$/' docs/architecture.md > /tmp/doc.mmd && diff /tmp/doc.mmd /tmp/gen.mmd && [ -z "$(awk '/## Module Structure/,/## Module Dependency Graph/' docs/architecture.md | grep -oE '[a-z_]+\.py' | sort -u | while read -r p; do find src/sif -name "$p" | grep -q . || echo "MISSING: $p"; done)" ] && env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v</automated>
  </verify>
  <done>The module tree lists only files that exist under src/sif (existence-checked programmatically), the Mermaid block is byte-identical to the script output, no deleted package/phantom file/unimplemented backend appears, the indexing flow shows the live argument order, the future list contains only unshipped items, and the docs validator is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> diagram script | scripts/generate_arch_diagram.py reads local source files (AST parse) and prints to stdout |
| docs readers -> system behavior | architecture.md must not describe modules or features that do not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Tampering | scripts/generate_arch_diagram.py | low | mitigate | Script is AST-parse-only, reads src/sif, writes nothing (stdout); no network, no shell-out |
| T-06-08 | Repudiation | docs/architecture.md | low | mitigate | Phantom-module and unimplemented-backend claims removed; tree entries are programmatically existence-checked in the task verify |
</threat_model>

<verification>
1. All negative greps in Task 2's verify return 0 (deleted package name, phantom legacy filenames, unimplemented backend, old argument order, implemented-in-future phrases).
2. Mermaid block in the doc diffs clean against a fresh script run; script output is deterministic.
3. Every .py filename in the Module Structure tree resolves to a real file under src/sif.
4. `env -u FORCE_COLOR NO_COLOR=1 python -m pytest tests/test_docs.py -v` green; full suite at the 675 baseline (docs/script-only change).
</verification>

<success_criteria>
- G-06-5 truth from 06-UAT.md demonstrably true: architecture.md module structure and diagrams match the current src/sif tree with the unified mcp package and no deleted-directory references.
- The diagram script's output is derived from imports (D-10), deterministic, and matches the committed block.
</success_criteria>

<output>
Create `.planning/phases/06-documentation-audit-refresh/06-10-SUMMARY.md` when done
</output>
