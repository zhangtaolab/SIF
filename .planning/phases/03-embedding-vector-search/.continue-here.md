---
context: phase
phase: 03-embedding-vector-search
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-04-16T12:34:06.611Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_None identified._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree base drift | Executor worktrees were created from stale base commits, causing merge issues | advisory | Always verify `ACTUAL_BASE == EXPECTED_BASE` before agent work; reset --hard if mismatch |
| Agent getting stuck in Read loops | Background executor repeatedly re-read the same file instead of making progress | advisory | Kill stuck agents promptly and reassign; verify tool results are actionable |
| Import mismatch in tests | `docsift.models.search.SearchOptions` differs from `docsift.core.models.SearchOptions` | advisory | Always import SearchOptions from `docsift.core.models` when testing VectorSearcher |

<current_state>
Phase 03 — embedding-vector-search. Waves 1 and 2 are fully complete. 4 of 6 plans are done.
Current HEAD: 81595aa (docs(03-04): add plan summary)
Next up: Wave 3 (03-05) — EmbeddingManager refactor.
</current_state>

<completed_work>

Completed Tasks:
- Plan 03-01: Added `model_type`, `api_base`, `api_key` to Settings with tests — Done
- Plan 03-02: Implemented OpenAIEmbedder, wired ModelScopeEmbedder into factory, fixed return types — Done
- Plan 03-03: Made SchemaManager dimension-aware with fail-fast mismatch detection — Done
- Plan 03-04: Added `add_embeddings_batch` to VectorSearcher using `json.dumps` + `executemany` — Done
</completed_work>

<remaining_work>

- Plan 03-05: Refactor EmbeddingManager to work with Embedder protocol instances (Wave 3)
- Plan 03-06: Integrate EmbeddingManager into CLI search/index commands, fix indexer.py stale imports (Wave 4)
</remaining_work>

<decisions_made>

- SchemaManager introspects `sqlite_master` to detect dimension mismatches and fails fast at startup.
- VectorSearcher uses `json.dumps` for embedding serialization to sqlite-vec.
- Fallback regular table for embeddings was removed; vector search requires sqlite-vec.
</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)

1. `.planning/phases/03-embedding-vector-search/03-05-PLAN.md` — next plan to execute
2. `.planning/phases/03-embedding-vector-search/03-06-PLAN.md` — final plan to execute
3. `.planning/phases/03-embedding-vector-search/03-RESEARCH.md` — technical research and pitfalls
4. `CLAUDE.md` — project coding conventions and architecture

## Infrastructure State

- Python 3.13.9, uv-managed venv at `.venv`
- No running background processes
- Test runner: `uv run pytest`
</continue-here>
