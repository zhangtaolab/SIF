---
phase: 03-embedding-vector-search
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sif/embedding/embedder.py
  - src/sif/embedding/factory.py
  - pyproject.toml
  - tests/unit/embedding/test_openai_embedder.py
  - tests/unit/embedding/test_factory.py
  - tests/unit/cli/test_index.py
autonomous: true
gap_closure: true
requirements: [VEC-01]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "SIF_MODEL_TYPE=openai produces embeddings end-to-end through the real dispatch path — Settings → EmbeddingManager.from_settings → EmbeddingModelFactory.create_model → OpenAIEmbedder.embed/embed_batch — with the endpoint mocked in tests; this exact path dead-ends at factory load today (03-VERIFICATION.md gap, VEC-01 / SC1)"
    - "--model-type openai on sif vsearch/query/embed and the SIF_MODEL_TYPE=openai env override no longer terminate in a load-time ClickException from an unimplemented backend; the api_key/api_base settings that currently dead-end in the factory flow through to the OpenAI client constructor (03-CONTEXT D-04)"
    - "OpenAI-compatible endpoints are addressed as {api_base}/embeddings via the openai SDK base_url parameter; without api_base the SDK default endpoint is used"
    - "First load with the openai backend resolves the embedding dimension from the API (one minimal probe input) and caches it to openai_dim_cache.json in the configured cache dir with a 7-day TTL, keyed by model name; subsequent loads read the cache and do not re-probe (03-CONTEXT D-05)"
    - "A caller-supplied embedding_dim that disagrees with the API-detected dimension fails fast at load time with a RuntimeError naming both values and the SIF_EMBEDDING_DIM setting, mirroring the D-09 dimension-mismatch fail-fast philosophy"
    - "When the openai package is absent, constructing the backend raises ImportError after a logger.error naming the install command pip install sif[openai], matching the SentenceTransformerEmbedder optional-dependency pattern (embedder.py lines 36-43)"
    - "Backend configuration is parallel-safe and interruption-safe (VEC-01 edge probe, concurrency): two SIF processes configured with model_type=openai construct independent OpenAIEmbedder instances with no shared mutable state, and a failed or interrupted embedding run persists no partial embeddings — vectors reach document_embeddings only after a complete batch response, because embed_batch validates response length and raises before any write"
    - statement: "An interrupted or parallel ModelScope download resumes from the local snapshot cache instead of restarting or corrupting it — the guarantee rests with modelscope snapshot_download's caching/resume semantics and is not unit-verifiable offline (VEC-04 edge probe, concurrency)"
      verification: backstop
  flagged_assumptions:
    - "VEC-02 edge probe returned unclassified: edge behavior beyond the verified sqlite-vec-only, fail-fast vector search path was not enumerated by the probe; carried as a flagged assumption, never auto-resolved"
    - "VEC-03 edge probe returned unclassified: edge behavior beyond the verified executemany batch insert was not enumerated (includes the advisory NULL-chunk_id reliance recorded in 03-VERIFICATION.md coincidental_reliance_items); carried as a flagged assumption, never auto-resolved"
  artifacts:
    - "src/sif/embedding/embedder.py defines class OpenAIEmbedder(Embedder) with embed, embed_batch, and dimension"
    - "src/sif/embedding/factory.py _create_openai_model constructs OpenAIEmbedder and no longer raises for the openai branch"
    - "tests/unit/embedding/test_openai_embedder.py exists (Wave-0 artifact declared in 03-VALIDATION.md, missing per 03-VERIFICATION.md)"
    - "tests/unit/embedding/test_factory.py exists (same Wave-0 declaration, missing per 03-VERIFICATION.md)"
    - "pyproject.toml declares an openai optional extra and includes it in the all extra"
  key_links:
    - "Settings.model_type=openai → EmbeddingManager.from_settings → factory kwargs (api_key, api_base, embedding_dim, cache_dir) → _create_openai_model → OpenAIEmbedder → OpenAI(api_key=..., base_url=...).embeddings.create — currently NOT_WIRED per the 03-VERIFICATION.md key-link table; must be WIRED"
    - "SIF_MODEL_TYPE env var → Settings.model_type → ModelType.OPENAI → factory dispatch (mechanism verified green, currently dead-ends at the factory)"
    - "API-detected dimension → OpenAIEmbedder.dimension → EmbeddingManager EmbeddingResponse.dimensions, validated fail-fast against settings.embedding_dim"
  prohibitions:
    - "MUST NOT transmit document or chunk text to any remote endpoint unless model_type=openai is explicitly configured — local backends (sentence_transformers, gguf, modelscope) keep all user content on-device per the local-first trust model; model-file downloads are the only permitted network traffic for local backends"
    - "MUST NOT silently fall back to a different embedding backend when the configured backend fails to load or errors — the failure must surface as an explicit error to the user (extends the Phase 1 fail-fast decision D-03/FND-07 to backend selection)"
---

<objective>
Close the single Phase 03 verification gap (03-VERIFICATION.md): the OpenAI-compatible API embedding
backend named in Success Criterion 1 / VEC-01 was never implemented. Settings, env vars, and three CLI
--model-type options advertise the openai backend, but EmbeddingModelFactory._create_openai_model
raises before doing anything and the OpenAIEmbedder class never existed in any reachable commit. The
api_key/api_base settings currently dead-end in the factory. The two Wave-0 test files declared in
03-VALIDATION.md (tests/unit/embedding/test_factory.py, test_openai_embedder.py) are also missing.

This plan delivers: OpenAIEmbedder(Embedder) with embed/embed_batch/dimension, generic
OpenAI-compatible endpoint support via api_base (D-04), dimension auto-detection with local caching
(D-05), the factory wiring, the openai optional extra in pyproject.toml, and both missing test files.
Because the plan touches factory.py, it also completes the honest-signature fix that plan 03-02's
title promised ("refactor factory to Embedder protocol") and that 03-REVIEW.md WR-01/WR-02 flag: the
create_model/_create_* annotations return Embedder (not the EmbeddingModel ABC none of them build)
and kwargs are typed Any (not the builtin any).

Purpose: make every backend Settings advertises actually loadable — VEC-01 is PARTIAL solely because
of this leg; VEC-02/03/04 are verified satisfied and untouched.
Output: a working, tested openai backend; zero changes to the six executed plans' other deliverables.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-embedding-vector-search/03-VERIFICATION.md
@.planning/phases/03-embedding-vector-search/03-CONTEXT.md
@.planning/phases/03-embedding-vector-search/COVERAGE.md

# Source files being modified (read fully before editing)
@src/sif/embedding/embedder.py
@src/sif/embedding/factory.py
@src/sif/embedding/manager.py
@src/sif/core/models.py
@src/sif/models/embedding.py
@src/sif/config/settings.py
@pyproject.toml

# Test patterns to replicate
@tests/unit/embedding/test_embedder_impl.py
@tests/unit/embedding/test_manager.py
@tests/unit/cli/test_index.py
</context>

<assumption_delta_decision>
The assumption-delta detector fired on the Phase 03 scope (pluralization signals: "alternative",
"fallback"; "chosen" signal: "configurable"). The identity-model question — does the primary
abstraction still name the right noun now that a fourth embedding backend lands?

- Noun now primary: the embedding backend variant, already generalized as the Embedder protocol
  (src/sif/core/models.py:278-292) + the ModelType enum + EmbeddingModelFactory dispatch.
- Decision: no-change.
- Rationale: the plural seam predates this gap — three Embedder implementations and a five-member
  ModelType already exist, and ModelType.OPENAI has been a (dead) member since before this phase.
  OpenAIEmbedder slots into the existing protocol/enum/factory without promoting or demoting any
  variant. No accepted debt.
- Companion invariant test (accepted): Task 3's factory dispatch matrix encodes "every VEC-01-named
  backend (sentence_transformers, gguf, openai) constructs a working Embedder through the factory" —
  it goes red the instant a future change reintroduces a Settings-advertised but unimplemented
  backend, which is precisely the defect this gap closure repairs.
</assumption_delta_decision>

<probe_provenance>
Spec-less probe fallback (no 03-SPEC.md sections; EDGE_ABSENT=1, PROHIB_ABSENT=1). Source report:
.planning/tmp/03-edge-coverage.json — 4 applicable, 4 unresolved. Disposition, per the --auto rules:

- VEC-01 (concurrency probe) → resolved, verification: explicit — authored as the parallel-safe /
  interruption-safe truth in must_haves.truths (unit-testable: ragged batch raises before any write).
- VEC-04 (concurrency probe) → resolved, verification: backstop — authored as the structured
  { statement, verification: backstop } marker (guarantee rests with the modelscope SDK).
- VEC-02, VEC-03 (unclassified) → stay unresolved as explicit flagged assumptions in
  must_haves.flagged_assumptions — never auto-resolved.

No-silent-drop equality: 4 probe-surfaced == 2 authored + 2 flagged. ✓

Prohibition recall (two-stage, in-prompt): ~10 raw candidates per requirement; Stage 2 dropped
routine-engineering items (input mutation, empty-list handling, timeout hygiene — owned by code
review). Canon drop with breadcrumb: secret-in-logs for api_key is canon security (OWASP A09) —
covered by threat-model mitigation T-03-01 and /gsd-secure-phase; not minted as a prohibition.
Kept: the two values-level prohibitions in must_haves.prohibitions (remote-content-egress opt-in;
no silent backend fallback). VEC-02/VEC-03/VEC-04 yielded zero kept prohibitions (clean-utility
outcome — local infrastructure with no product-intent surface beyond the two already kept).
</probe_provenance>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end openai backend — Settings → EmbeddingManager → factory → OpenAIEmbedder → embeddings, one path only</name>
  <files>src/sif/embedding/embedder.py, src/sif/embedding/factory.py, pyproject.toml, tests/unit/embedding/test_openai_embedder.py</files>
  <read_first>
  - src/sif/embedding/embedder.py — the SentenceTransformerEmbedder lazy-import pattern (try/except ImportError around a function-local import, logger.error naming the install command, re-raise; lines 36-43), the LlamaCppEmbedder numpy L2-normalization pattern (lines 156-163), and the class layout (each embedder subclasses Embedder and exposes embed, embed_batch, dimension)
  - src/sif/core/models.py — the Embedder protocol definition (lines 278-292): embed(text) -> list[float], embed_batch(texts) -> list[list[float]], dimension property
  - src/sif/embedding/factory.py — create_model dispatch, the _create_openai_model stub (lines 66-72) to be replaced, and the _create_modelscope_model wiring as the reference shape (lines 82-92)
  - src/sif/embedding/manager.py — from_settings (lines 44-72) and load_model (lines 74-94): which kwargs reach the factory (embedding_dim, max_tokens, n_gpu_layers, n_ctx, api_key, api_base, cache_dir) and in what form
  - src/sif/models/embedding.py — EmbeddingConfig fields api_key/api_base (lines 35-36) and ModelType.OPENAI
  - tests/unit/embedding/test_embedder_impl.py — the patch.dict("sys.modules", {...}) mock-module pattern used for optional heavy deps (e.g. lines 150-200)
  - pyproject.toml — [project.optional-dependencies] extras structure (embed, all)
  </read_first>
  <behavior>
  - Test 1 (end-to-end tracer, must be written first and fail before implementation): with a fake openai module injected via patch.dict("sys.modules", ...) whose OpenAI(...).embeddings.create(input, model) returns data=[SimpleNamespace(embedding=[0.1, ...])] of a fixed length, constructing Settings(model_type="openai", api_key="test-key", api_base="https://api.example.com/v1", cache_embeddings=False, embedding_dim=8) with the fake vectors of length 8, then EmbeddingManager.from_settings(settings).embed(["hello world"]) returns exactly one embedding, matching the fake response content, and the recorded client was constructed once with api_key="test-key" and base_url="https://api.example.com/v1"
  - Test 2: embed_batch of 150 texts with batch_size=64 results in exactly 3 embeddings.create calls and 150 vectors returned in input order
  - Test 3 (ragged response): a mocked response whose data list is shorter than the input slice raises RuntimeError naming the model and the expected-vs-actual count
  - Test 4 (dependency absent): with the openai import blocked (patch builtins.__import__ to fail only for the openai module, per test_embedder_impl.py lines 230-237), constructing OpenAIEmbedder raises ImportError after logging an install hint containing sif[openai]
  - Test 5: each returned vector is L2-normalized (numpy norm approximately 1.0), consistent with the other backends
  - Test 6 (no partial persistence, edge probe): when the fake client raises a mid-batch exception (first slice succeeds, second raises), embed_batch propagates the error and returns nothing — no partial list is produced
  </behavior>
  <action>
  Create class OpenAIEmbedder(Embedder) in src/sif/embedding/embedder.py, placed after ModelScopeEmbedder. Constructor signature: __init__(self, model_name: str = "text-embedding-3-small", api_key: str | None = None, api_base: str | None = None, embedding_dim: int | None = None, cache_dir: str | None = None, batch_size: int = 64) -> None. Import openai lazily inside __init__ exactly per the SentenceTransformerEmbedder pattern: try/except ImportError with logger.error naming the install command pip install sif[openai], then re-raise. Construct the client once in __init__ as OpenAI(api_key=api_key, base_url=api_base) — the SDK appends the /embeddings path to base_url, so a custom OpenAI-compatible endpoint given as api_base is addressed as {api_base}/embeddings per D-04; api_base=None means the SDK default endpoint. Never log or interpolate api_key into any message, exception, or repr (threat T-03-01).

  Dimension resolution in this task (provisional; Task 2 upgrades it): if the embedding_dim kwarg is not None use it directly; otherwise probe once via client.embeddings.create(input=["."], model=model_name) and take the length of the first data item's embedding. Store the result for the dimension property.

  Implement embed(text: str) -> list[float]: one embeddings.create call with input=[text] and model=self.model_name; take data[0].embedding; L2-normalize with numpy the same way LlamaCppEmbedder.embed does; return as a plain list. Implement embed_batch(texts: list[str]) -> list[list[float]]: return an empty list for empty input; otherwise iterate slices of size self._batch_size, call embeddings.create once per slice with the slice as input, validate that the response data length equals the slice length (else raise RuntimeError naming model_name and both counts — this is the interruption-safety edge probe guarantee), normalize each vector, and return the concatenation in input order. The dimension property returns the resolved dimension. The class holds no mutable state shared across instances beyond the immutable client — parallel processes are independent (edge probe, VEC-01 concurrency).

  Wire the factory: replace the body of _create_openai_model (which currently only raises before using any kwargs) with a return of OpenAIEmbedder(model_name=model_name, api_key=kwargs.get("api_key"), api_base=kwargs.get("api_base"), embedding_dim=kwargs.get("embedding_dim"), cache_dir=kwargs.get("cache_dir")) — exactly the construction listed in 03-VERIFICATION.md gap.missing. Delete the raise stub entirely; the sibling HuggingFace stub in _create_huggingface_model stays untouched (out of scope per 03-VERIFICATION.md anti-pattern table).

  Honest-signature fix on the same file (completes what plan 03-02's title claimed; 03-REVIEW.md WR-01/WR-02): annotate create_model and every _create_* helper as returning Embedder (import Embedder from sif.core.models; stop importing EmbeddingModel from sif.embedding.model — import ModelType from sif.models.embedding, the canonical source manager.py already uses), and change every occurrence of kwargs typed as the builtin any to **kwargs: Any with from typing import Any. Nothing else imports EmbeddingModel from factory.py (verified: only src/sif/embedding/__init__.py re-exports it from sif.embedding.model directly), so this is a contained change.

  Add the dependency declaration to pyproject.toml under [project.optional-dependencies]: a new extra openai = ["openai>=2.0.0"] (heavy ML libs stay optional extras per project decision D-05/STATE.md), and add "openai>=2.0.0" to the existing all extra. Package legitimacy was verified at plan time via pypi.org/project/openai (official OpenAI publisher, Trusted Publishing, Apache-2.0; research table marks it VERIFIED: PyPI) — no blocking checkpoint needed; version floor >=2.0.0 accommodates the installed 2.36.0 and current 3.x.

  Create tests/unit/embedding/test_openai_embedder.py implementing the six behaviors above. Mock pattern: build a fake module object (types.ModuleType) whose OpenAI attribute is a factory capturing constructor kwargs and returning an object whose embeddings.create records calls and returns SimpleNamespace(data=[SimpleNamespace(embedding=[...])]); inject with patch.dict("sys.modules", {"openai": fake_module}). Construct Settings with explicit kwargs and cache_embeddings=False so no cache database is created under the user home (03-REVIEW.md WR-10); monkeypatch-delenv SIF_MODEL_TYPE/SIF_API_KEY/SIF_API_BASE when asserting defaults so tests stay hermetic (03-REVIEW.md IN-06). In the end-to-end test, make the fake vectors' length equal the settings embedding_dim (8) so Task 2's always-probe upgrade cannot break this test.
  </action>
  <verify>
    <automated>pytest tests/unit/embedding/test_openai_embedder.py -x</automated>
    <fails_when>pytest exits non-zero, reports any FAILED or ERROR, or collects 0 tests from the file</fails_when>
    <automated>grep -q "OpenAI models not yet implemented" src/sif/embedding/factory.py &amp;&amp; exit 1 || exit 0</automated>
    <fails_when>grep exits 0 — the stub message quoted in 03-VERIFICATION.md artifact notes is still present in factory.py (the distinct HuggingFace stub message must remain and is a different literal)</fails_when>
    <automated>python -c 'import inspect; from sif.embedding.factory import EmbeddingModelFactory; s = inspect.getsource(EmbeddingModelFactory); assert "OpenAIEmbedder" in s and "-> EmbeddingModel" not in s and "dict[str, any]" not in s'</automated>
    <fails_when>python exits non-zero (AssertionError or ImportError) — the openai branch is not wired, or the old ABC return annotation / builtin-any kwargs annotation survives in factory.py</fails_when>
  </verify>
  <done>The exact path that failed verification (Settings → manager → factory → backend) returns embeddings from a mocked OpenAI-compatible endpoint; the load-time dead end for model_type=openai is gone; the openai extra is declared; the tracer test, ragged-batch guard, ImportError hint, and no-partial-persistence tests are green and committed.</done>
  <acceptance_criteria>
  - src/sif/embedding/embedder.py contains class OpenAIEmbedder(Embedder) defining embed, embed_batch, and a dimension property
  - src/sif/embedding/factory.py _create_openai_model returns an OpenAIEmbedder constructed from the api_key/api_base/embedding_dim/cache_dir kwargs; the stub message documented in 03-VERIFICATION.md appears nowhere in the file
  - Every create_model/_create_* signature in factory.py reads -> Embedder and **kwargs: Any
  - pyproject.toml [project.optional-dependencies] contains an openai extra with openai>=2.0.0, and the all extra lists it too
  - pytest tests/unit/embedding/test_openai_embedder.py -x exits 0 with at least 6 collected tests
  - python -c 'from sif.models.embedding import ModelType; from sif.embedding.factory import EmbeddingModelFactory; print(ModelType.OPENAI)' exits 0 and prints ModelType.OPENAI
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dimension auto-detection with local cache and fail-fast mismatch validation (D-05)</name>
  <files>src/sif/embedding/embedder.py, tests/unit/embedding/test_openai_embedder.py</files>
  <read_first>
  - src/sif/embedding/embedder.py — the OpenAIEmbedder built in Task 1 (its provisional dimension resolution is superseded by this task)
  - src/sif/config/settings.py — get_cache_dir() (lines 200-209) and the cache_dir field; this is where the factory's cache_dir kwarg originates via EmbeddingManager.from_settings
  - src/sif/embedding/cache.py — the existing file-based cache convention in the cache dir (embedding cache uses the same directory; follow its file layout conventions)
  - .planning/phases/03-embedding-vector-search/03-CONTEXT.md — D-05 (auto-detect on first load, cache locally, avoid repeated probing); cache file location and TTL are Claude's discretion per the decisions section
  - .planning/phases/03-embedding-vector-search/03-RESEARCH.md — resolved open question 1 (JSON file, 7-day TTL) and assumption A2 (endpoints may ignore the dimensions parameter — hence detect, never request truncation)
  </read_first>
  <behavior>
  - Test 1 (probe once, then cache hit): with a fresh tmp_path cache_dir, the first OpenAIEmbedder(embedding_dim=None, cache_dir=tmp_path) triggers exactly one embeddings.create probe call; a second instance with the same model_name and cache_dir performs zero probe calls; both report the probed dimension; the file tmp_path/openai_dim_cache.json exists after the first instance
  - Test 2 (cache content): the cache file is valid JSON keyed by model name, storing the detected dimension and an ISO-8601 detected_at timestamp for the model used
  - Test 3 (TTL expiry): a cache entry whose detected_at is older than 7 days (pre-write the file in the test) is ignored — the instance re-probes and rewrites the entry
  - Test 4 (no cache dir): cache_dir=None produces no file operations and no crash; the dimension still resolves via one probe for that instance
  - Test 5 (per-model keys): two different model_name values sharing one cache_dir maintain separate entries — switching models re-probes rather than serving the other model's dimension
  - Test 6 (fail-fast mismatch, supersedes the trust-me kwarg): with settings embedding_dim=8 and a fake endpoint returning vectors of length 6, construction raises RuntimeError whose message contains both numbers and the string SIF_EMBEDDING_DIM
  - Test 7 (corrupt cache): a malformed JSON cache file is treated as a miss — a warning is logged, the probe runs, and the file is rewritten
  - Test 8 (Task 1 regression): the Task 1 end-to-end manager-path test still passes — probe fires, matches settings embedding_dim, no exception
  </behavior>
  <action>
  Upgrade OpenAIEmbedder's dimension resolution in src/sif/embedding/embedder.py to the final D-05 semantics (superseding Task 1's provisional rule): the API is the source of truth. On construction, resolve the dimension as follows. If cache_dir is not None, read the file Path(cache_dir) / "openai_dim_cache.json" (create parent directories with parents=True, exist_ok=True); its schema is a JSON object mapping each model name to an object with dimension (int) and detected_at (ISO-8601 UTC string via datetime.now(timezone.utc).isoformat()). A fresh entry (detected_at within the last 7 days — 604800 seconds) for the exact model_name is used directly with no API call. Otherwise probe once via client.embeddings.create(input=["."], model=model_name) — the minimal input keeps probe cost at a single token — record len of the first data item's embedding, and rewrite the cache file. A missing, corrupt, or unread-mtime file is a cache miss: log a warning and re-probe (never crash on cache I/O; threat posture: cache is an optimization, correctness comes from the probe). With cache_dir=None, keep only the in-instance probe result (one probe per instance, documented behavior).

  After resolution, if the caller-supplied embedding_dim is not None and differs from the resolved dimension, raise RuntimeError at load time naming both values (resolved vs configured) and instructing the user to align SIF_EMBEDDING_DIM with the endpoint model's dimension — this mirrors the D-09 schema-mismatch fail-fast: a wrong dimension otherwise surfaces later as an opaque sqlite-vec wrong-number-of-entries insert error. When they agree, or when embedding_dim is None, the resolved dimension becomes the dimension property value. Write the eight behavior tests into tests/unit/embedding/test_openai_embedder.py (extend the file; use the tmp_path fixture for cache dirs so nothing is written outside the test tree).
  </action>
  <verify>
    <automated>pytest tests/unit/embedding/test_openai_embedder.py -x</automated>
    <fails_when>pytest exits non-zero, reports any FAILED or ERROR, or collects 0 tests</fails_when>
    <automated>ruff check src/sif/embedding/embedder.py &amp;&amp; ruff format --check src/sif/embedding/embedder.py</automated>
    <fails_when>either ruff command exits non-zero — a lint violation or a formatting diff in the modified source file</fails_when>
  </verify>
  <done>First openai-backend load probes the endpoint dimension once and caches it per model for 7 days; subsequent loads skip the probe; a settings dimension that disagrees with the endpoint fails fast at load with both values named; Task 1's tests still pass.</done>
  <acceptance_criteria>
  - src/sif/embedding/embedder.py references the file name openai_dim_cache.json and a 604800-second (7-day) freshness bound
  - pytest tests/unit/embedding/test_openai_embedder.py -x exits 0 with at least 14 collected tests (6 from Task 1 + 8 from Task 2)
  - A double-construction test asserts the second instance issues zero probe calls (cache hit)
  - A mismatch test asserts RuntimeError is raised and its message contains SIF_EMBEDDING_DIM
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Factory dispatch matrix test file, settings→factory env path, and CLI openai path</name>
  <files>tests/unit/embedding/test_factory.py, tests/unit/cli/test_index.py</files>
  <read_first>
  - src/sif/embedding/factory.py — the post-Task-1 factory: dispatch order, GGUF model_path ValueError, the unsupported-type ValueError, and the openai/modelscope/sentence_transformers branches
  - tests/unit/embedding/test_embedder_impl.py — the sys.modules mock-module pattern for sentence_transformers, llama_cpp, and modelscope dependencies
  - tests/unit/embedding/test_manager.py — Settings construction with api_key/api_base propagation (from_settings test)
  - tests/unit/cli/test_index.py — the test_embed_cmd_respects_model_type_override scaffolding (lines ~168-200): _make_collection helper, MagicMock collection/document repos, settings model_copy capture, CliRunner invocation of the embed command
  - .planning/phases/03-embedding-vector-search/COVERAGE.md — the decided capability matrix this surface implements
  </read_first>
  <behavior>
  - Test 1: create_model(ModelType.SENTENCE_TRANSFORMERS, None, name) with a mocked sentence_transformers module returns a SentenceTransformerEmbedder exposing embed/embed_batch/dimension
  - Test 2: create_model(ModelType.OPENAI, None, name, api_key="k", api_base="https://api.example.com/v1", embedding_dim=8) with a mocked openai module returns an OpenAIEmbedder, and the captured OpenAI client constructor kwargs include api_key="k" and base_url="https://api.example.com/v1"
  - Test 3: create_model(ModelType.GGUF, None, name) without a model_path raises ValueError whose message names model_path; with a model_path and a mocked llama_cpp module it returns a LlamaCppEmbedder
  - Test 4: create_model(ModelType.MODELSCOPE, None, name) with ModelDownloader patched at sif.embedding.embedder.ModelDownloader and a mocked sentence_transformers module returns a ModelScopeEmbedder
  - Test 5: an unknown model_type value raises ValueError containing the phrase Unsupported model type
  - Test 6 (env path): with monkeypatch.setenv SIF_MODEL_TYPE=openai (and the other SIF_* model vars cleared), Settings() yields model_type "openai", ModelType(settings.model_type) is ModelType.OPENAI, and the factory dispatches to OpenAIEmbedder with the openai module mocked
  - Test 7 (assumption-delta invariant, the companion test from the decision block): for each VEC-01-named backend (sentence_transformers, gguf, openai) with its dependency mocked, EmbeddingModelFactory.create_model returns an object that has callable embed and embed_batch attributes and an integer dimension — no Settings-advertised backend may dead-end at load
  - Test 8 (CLI path, in tests/unit/cli/test_index.py): invoking the embed command with --model-type openai against the existing test scaffolding's mocked repos — but with the real EmbeddingManager and a fake openai module injected via patch.dict — exits 0 and the fake client recorded at least one embeddings.create call whose model kwarg equals the settings model_name in effect; no backend-level failure text appears in the output because the exit code is 0
  </behavior>
  <action>
  Create tests/unit/embedding/test_factory.py as the consolidated dispatch matrix (the Wave-0 artifact declared in 03-VALIDATION.md and missing per 03-VERIFICATION.md): implement behaviors 1-7. Reuse the mock-module injection pattern from tests/unit/embedding/test_embedder_impl.py — a types.ModuleType with the needed attribute, patched over sys.modules for the duration of each test. For the modelscope branch, patch sif.embedding.embedder.ModelDownloader (the name ModelScopeEmbedder actually imports) rather than the download implementation. For the env-path test, clear SIF_MODEL_NAME/SIF_API_KEY/SIF_API_BASE/SIF_CACHE_EMBEDDINGS via monkeypatch.delenv so the assertion is hermetic against a developer shell that exports SIF_* variables.

  Extend tests/unit/cli/test_index.py with the CLI-path test (behavior 8): adapt the scaffolding of test_embed_cmd_respects_model_type_override — same _make_collection helper, same MagicMock repos for collection/document/chunk lookups — but do not mock EmbeddingManager: let the command construct the real manager from the overridden settings (model_type openai, a model_name matching the fake endpoint, api_key/api_base test values, cache_embeddings False, embedding_dim equal to the fake vector length), and inject the fake openai module so load_model dispatches to OpenAIEmbedder and embed/embed_batch run against the fake client. The fake vectors' length must equal the settings embedding_dim so Task 2's mismatch validation passes. Assert exit_code == 0 and that the recorded embeddings.create calls include one whose model kwarg is the configured model_name. If the embed command's DB writes make a fully-mocked connection awkward, keep the repo mocks from the existing test and assert only the two named outcomes — do not weaken the exit-code or recorded-call assertions.

  Do not modify any production file in this task — it is test surface only (the factory and embedder are final after Task 2). If a test exposes a genuine production defect, fix the defect in the owning file and re-run Tasks 1-2 verifies.
  </action>
  <verify>
    <automated>pytest tests/unit/embedding/test_factory.py tests/unit/cli/test_index.py -x</automated>
    <fails_when>pytest exits non-zero, reports any FAILED or ERROR, or either file collects 0 tests</fails_when>
    <automated>ruff check src tests &amp;&amp; ruff format --check src tests &amp;&amp; pytest</automated>
    <fails_when>any of the three commands exits non-zero — a lint violation, a formatting drift, or any test failure anywhere in the full suite (baseline at HEAD: 505 passed, 11 skipped)</fails_when>
  </verify>
  <done>The missing Wave-0 test files exist and pass; every VEC-01-named backend is proven constructible through the factory; the SIF_MODEL_TYPE=openai env path dispatches end-to-end; the CLI embed command with --model-type openai reaches the (fake) endpoint; the full quality suite from CLAUDE.md is green.</done>
  <acceptance_criteria>
  - tests/unit/embedding/test_factory.py exists and collects at least 7 tests covering dispatch for sentence_transformers, gguf, openai, modelscope, the two ValueError guards, and the env-var path
  - tests/unit/cli/test_index.py contains a test invoking the embed command with --model-type openai that asserts exit_code == 0 and a recorded embeddings.create call
  - pytest tests/unit/embedding/test_factory.py tests/unit/cli/test_index.py -x exits 0
  - ruff check src tests, ruff format --check src tests, and the full pytest suite all exit 0
  </acceptance_criteria>
  <precondition>The full test suite passes at HEAD before this task's changes (run pytest once at task start; baseline is 505 passed, 11 skipped — halt and report if the baseline itself is red).</precondition>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user environment → Settings | SIF_API_KEY (secret) and SIF_API_BASE cross from env/.env into process memory |
| SIF process → remote OpenAI-compatible endpoint | user document/chunk text crosses to a third-party service, only when model_type=openai is explicitly configured |
| remote endpoint → SIF process | endpoint responses (embedding arrays, dimensions) are external input consumed by the index path |
| pip supply chain → venv | installing the new openai optional extra executes third-party package installation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | OpenAIEmbedder api_key handling (embedder.py, factory.py) | medium | mitigate | Never log, interpolate, or repr api_key; pass it solely as the OpenAI(api_key=...) constructor argument; Settings.api_key is already repr=False (settings.py:72-76); no exception message may embed the key |
| T-03-02 | Spoofing | Settings.api_base → remote endpoint | low | mitigate | Existing validate_api_base rejects any non-HTTP(S) scheme (settings.py:160-169); OpenAIEmbedder consumes only that validated value via the SDK base_url parameter; no additional URL assembly is performed |
| T-03-03 | Tampering | remote endpoint response (ragged batches, dimension drift) | medium | mitigate | embed_batch validates response length per slice and raises RuntimeError before any write; Task 2 validates the detected dimension against settings.embedding_dim fail-fast at load |
| T-03-04 | Information Disclosure | document text egress to remote endpoint | high | mitigate | Egress is strictly opt-in: OpenAIEmbedder is reachable only via explicit model_type=openai in settings/env/CLI; prohibition P-1 forbids content-bearing network calls from local backends; enforced by design and covered by the dispatch matrix test |
| T-03-05 | Denial of Service | hung/absent endpoint stalls CLI | low | accept | The openai SDK applies default timeouts/retries; a hard failure surfaces as an error (fail-fast, no silent fallback per prohibition P-2); a tunable timeout is not part of the gap scope |
| T-03-SC | Tampering | pip install of the openai optional extra | high | mitigate | Package legitimacy verified at plan time via pypi.org/project/openai — official OpenAI publisher, Trusted Publishing from openai/openai-python, Apache-2.0, substantial release history; version floor openai>=2.0.0 declared in extras (03-RESEARCH.md marks it VERIFIED: PyPI); no unverified packages are introduced |
</threat_model>

<verification>
Plan-level checks (in addition to per-task verifies):

1. The exact failure from 03-VERIFICATION.md behavioral spot-check is inverted: pytest tests/unit/embedding/test_openai_embedder.py -x exits 0 (the spot-check "OpenAI backend creation → NotImplementedError" is now a green construction test).
2. The two missing Wave-0 artifacts exist: tests/unit/embedding/test_factory.py and tests/unit/embedding/test_openai_embedder.py (git ls-files shows both after commit).
3. The dead-end key link is closed: api_key/api_base flow from Settings through EmbeddingManager.from_settings into the OpenAI client constructor (Task 3 Test 2/6 assert the captured constructor kwargs).
4. Full quality suite per CLAUDE.md: ruff check src tests; ruff format --check src tests; pytest — all exit 0.
5. Human verification (post-fix, from 03-VERIFICATION.md section "Live OpenAI-compatible endpoint"): set SIF_MODEL_TYPE=openai, SIF_API_BASE=https://<compatible-endpoint>/v1, SIF_API_KEY=..., SIF_MODEL_NAME=<endpoint-model-id>, run sif vsearch <query> against a real endpoint. Requires a live key — cannot be automated. Note for the operator: with model_type=openai, settings.model_name must be set to the endpoint's model id (the shared default Qwen/Qwen3-Embedding-0.6B is a local-model id, not an API id; an endpoint that rejects it surfaces an explicit SDK error naming the model).
</verification>

<success_criteria>
- The single verification gap is closed: SIF_MODEL_TYPE=openai and --model-type openai load a real OpenAIEmbedder through EmbeddingModelFactory and produce embeddings (mocked endpoint in automated tests; live endpoint via the human check above).
- VEC-01 moves from PARTIAL to satisfied: all three named backends (sentence-transformers, llama-cpp-python, OpenAI-compatible API) configurable via Settings and CLI.
- Both missing Wave-0 test files exist and pass; the full suite stays green (505+ passed, 0 failed).
- api_key never appears in logs, reprs, or exception messages (threat T-03-01 mitigation).
- No production behavior changes outside the openai backend and the factory annotations (VEC-02/03/04 surfaces untouched).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (gap closure)

Classes and methods:
- OpenAIEmbedder(Embedder) — src/sif/embedding/embedder.py: __init__(model_name="text-embedding-3-small", api_key=None, api_base=None, embedding_dim=None, cache_dir=None, batch_size=64), embed(text) -> list[float], embed_batch(texts) -> list[list[float]], dimension property, plus private dimension-probe/cache helpers (cache read/write, TTL check, mismatch validation)
- EmbeddingModelFactory._create_openai_model — wired to construct OpenAIEmbedder (no longer raises)
- Honest signatures across EmbeddingModelFactory.create_model and all _create_* helpers: -> Embedder, **kwargs: Any (imports: Embedder from sif.core.models; ModelType from sif.models.embedding; the sif.embedding.model import of EmbeddingModel removed from factory.py)

Settings / CLI / env (no new surface — existing advertised surface becomes functional):
- SIF_MODEL_TYPE=openai, SIF_API_KEY, SIF_API_BASE, --model-type openai on sif vsearch/query/embed, and Settings.model_type/api_key/api_base now reach a working backend
- SIF_EMBEDDING_DIM referenced in the new fail-fast mismatch error message

Packaging:
- pyproject.toml [project.optional-dependencies]: new openai extra (openai>=2.0.0), also added to the all extra

Files (new):
- tests/unit/embedding/test_openai_embedder.py (≥14 tests: end-to-end manager path, batching, ragged guard, ImportError hint, normalization, no-partial-persistence, dimension probe/cache/TTL/per-model/corrupt/mismatch)
- tests/unit/embedding/test_factory.py (≥7 tests: dispatch matrix, ValueError guards, env path, advertised-backend invariant)

Files (modified):
- tests/unit/cli/test_index.py (+1 CLI openai-path test)
- src/sif/embedding/embedder.py, src/sif/embedding/factory.py, pyproject.toml (as above)

Runtime artifacts:
- openai_dim_cache.json in the configured cache dir (default via Settings.get_cache_dir()), JSON map of model name → {dimension, detected_at}

Planner-authored (not executor deliverables):
- .planning/phases/03-embedding-vector-search/COVERAGE.md — the decided API coverage matrix (api-coverage gate input)
</artifacts_produced>

<output>
Create `.planning/phases/03-embedding-vector-search/03-07-SUMMARY.md` when done
</output>
