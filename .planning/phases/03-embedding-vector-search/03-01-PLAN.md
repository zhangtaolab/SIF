---
phase: 03-embedding-vector-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/docsift/config/settings.py
  - tests/unit/config/test_settings.py
autonomous: true
requirements:
  - VEC-01
user_setup: []
must_haves:
  truths:
    - Settings accepts model_type, api_base, and api_key fields
    - DOCSIFT_MODEL_TYPE defaults to sentence_transformers
    - api_base is validated as HTTP/HTTPS URL
    - api_key is excluded from logging/representation
  artifacts:
    - path: src/docsift/config/settings.py
      provides: Pydantic Settings with embedding backend configuration
      contains: "model_type: str = Field(default=\"sentence_transformers\""
    - path: tests/unit/config/test_settings.py
      provides: Unit tests for new settings fields
      min_lines: 40
  key_links:
    - from: Settings.model_type
      to: DOCSIFT_MODEL_TYPE env var
      via: "model_config = SettingsConfigDict(env_prefix=\"DOCSIFT_\""
---

<objective>
Add embedding backend configuration fields to Settings and create unit tests.

Purpose: Enable users to configure embedding backends (sentence-transformers, gguf, openai, modelscope) via environment variables and CLI options.
Output: Updated settings.py with model_type, api_base, api_key fields and passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-embedding-vector-search/03-CONTEXT.md
@.planning/phases/03-embedding-vector-search/03-RESEARCH.md
@.planning/phases/03-embedding-vector-search/03-PATTERNS.md
@.planning/phases/03-embedding-vector-search/03-VALIDATION.md

<interfaces>
From src/docsift/config/settings.py (existing pattern):
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_prefix="DOCSIFT_",
        env_file=".env",
        env_file_encoding="utf-8",
        extra="ignore",
    )
    model_name: str = Field(default="all-MiniLM-L6-v2", description="Embedding model name")
    embedding_dim: int = Field(default=384, ge=1, description="Embedding dimension")
```

From 03-CONTEXT.md D-01, D-02, D-03, D-04:
- model_type: valid values are "sentence_transformers", "gguf", "openai", "modelscope"
- default model_type: "sentence_transformers"
- api_base: configurable OpenAI-compatible endpoint base URL
- api_key: API key for remote embedding endpoints
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add embedding backend fields to Settings</name>
  <files>src/docsift/config/settings.py</files>
  <read_first>
    - src/docsift/config/settings.py
    - src/docsift/config/constants.py
  </read_first>
  <action>
    Add the following fields to the Settings class in src/docsift/config/settings.py, placed after the existing `model_path` field and before `embedding_dim`:

    1. `model_type: str = Field(default="sentence_transformers", description="Embedding model type (sentence_transformers, gguf, openai, modelscope)")`
    2. `api_base: str | None = Field(default=None, description="Base URL for OpenAI-compatible API endpoints")`
    3. `api_key: str | None = Field(default=None, description="API key for remote embedding endpoints", exclude=True)`

    Add a field validator `validate_api_base` that:
    - Accepts `v: str | None`
    - If v is None, returns None
    - If v does not start with "http://" or "https://", raises ValueError with message: "api_base must be an HTTP or HTTPS URL"
    - Returns v

    Add a field validator `validate_model_type` that:
    - Accepts `v: str`
    - Converts to lowercase
    - Validates against valid types: {"sentence_transformers", "gguf", "openai", "modelscope"}
    - Raises ValueError with message: "Invalid model_type: {v}. Must be one of: sentence_transformers, gguf, openai, modelscope"
    - Returns the normalized value

    Ensure imports include `Any` if not already present. Maintain 100-character line length.
  </action>
  <verify>
    <automated>pytest tests/unit/config/test_settings.py -x</automated>
  </verify>
  <done>
    Settings class includes model_type, api_base, api_key fields with correct defaults and validators.
  </done>
  <acceptance_criteria>
    - `grep -n 'model_type: str = Field(default="sentence_transformers"' src/docsift/config/settings.py` returns a match
    - `grep -n 'api_base: str | None = Field(default=None' src/docsift/config/settings.py` returns a match
    - `grep -n 'api_key: str | None = Field(default=None' src/docsift/config/settings.py` returns a match
    - `grep -n 'def validate_api_base' src/docsift/config/settings.py` returns a match
    - `grep -n 'def validate_model_type' src/docsift/config/settings.py` returns a match
    - `ruff check src/docsift/config/settings.py` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for Settings embedding configuration</name>
  <files>tests/unit/config/test_settings.py</files>
  <read_first>
    - src/docsift/config/settings.py
    - tests/conftest.py
  </read_first>
  <action>
    Create tests/unit/config/test_settings.py with the following test cases:

    1. `test_default_model_type_is_sentence_transformers`:
       - `settings = Settings()`
       - Assert `settings.model_type == "sentence_transformers"`

    2. `test_model_type_validation_accepts_valid_values`:
       - For each valid type in ["sentence_transformers", "gguf", "openai", "modelscope"]:
         - `settings = Settings(model_type=type_value)`
         - Assert `settings.model_type == type_value`

    3. `test_model_type_validation_rejects_invalid_values`:
       - `with pytest.raises(ValueError, match="Invalid model_type"): Settings(model_type="invalid")`

    4. `test_api_base_validation_accepts_https_url`:
       - `settings = Settings(api_base="https://api.openai.com/v1")`
       - Assert `settings.api_base == "https://api.openai.com/v1"`

    5. `test_api_base_validation_rejects_non_http_url`:
       - `with pytest.raises(ValueError, match="api_base must be an HTTP or HTTPS URL"): Settings(api_base="file:///etc/passwd")`

    6. `test_api_key_is_excluded_from_repr`:
       - `settings = Settings(api_key="sk-secret")`
       - Assert `settings.api_key == "sk-secret"`
       - Assert `"sk-secret"` does not appear in `repr(settings)`

    7. `test_model_type_env_var_override`:
       - `with monkeypatch.context() as m: m.setenv("DOCSIFT_MODEL_TYPE", "openai"); settings = Settings(); assert settings.model_type == "openai"`

    Use pytest. Include `from docsift.config.settings import Settings` at the top.
  </action>
  <verify>
    <automated>pytest tests/unit/config/test_settings.py -x</automated>
  </verify>
  <done>
    All 7 unit tests pass and cover settings validation for embedding backends.
  </done>
  <acceptance_criteria>
    - `pytest tests/unit/config/test_settings.py -x` exits 0
    - File contains at least 7 test functions
    - Tests cover model_type default, validation (valid + invalid), api_base validation (valid + invalid), api_key exclusion, and env var override
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Fix stale test imports across existing test files</name>
  <files>
    - tests/unit/cli/test_index_commands.py
    - tests/unit/cli/test_search_commands.py
    - tests/unit/cli/test_collection_commands.py
    - tests/unit/search/test_bm25.py
    - tests/unit/search/test_hybrid.py
    - tests/unit/search/test_vector.py
    - tests/unit/indexing/test_chunker.py
    - tests/unit/indexing/test_parser.py
    - tests/integration/test_index_and_search.py
    - tests/integration/test_search_pipeline.py
  </files>
  <read_first>
    - src/docsift/cli/commands/index.py
    - src/docsift/cli/commands/search.py
    - src/docsift/cli/commands/collection.py
    - src/docsift/search/bm25.py
    - src/docsift/search/hybrid.py
    - src/docsift/search/vector.py
    - src/docsift/indexing/chunker.py
    - src/docsift/indexing/parser.py
    - src/docsift/core/models.py
  </read_first>
  <action>
    Fix all broken imports in the listed test files so they match the current source API. Update imports and referenced names as needed. Do not rewrite entire test files—only fix imports and any directly related name references.

    Key mappings (verify against actual source before applying):
    - `docsift.models.search` -> `docsift.core.models` for SearchOptions, SearchResult, SearchType
    - `BM25SearchStrategy` -> `BM25Searcher` (in docsift.search.bm25)
    - `VectorSearchStrategy` -> `VectorSearcher` (in docsift.search.vector)
    - `HybridSearchStrategy` -> `HybridSearcher` (in docsift.search.hybrid)
    - `docsift.search.strategy.SearchContext` -> remove/replace if it does not exist; use query string directly where APIs accept it
    - `docsift.search.rerank.Reranker` -> check if exists; if not, mock or remove
    - `docsift.search.expansion.QueryExpansion` -> check if exists; if not, mock or remove
    - `docsift.core.collection.Collection` -> `docsift.core.models.Collection`
    - `docsift.core.document.Document` -> `docsift.core.models.Document`
    - `docsift.indexing.chunker.Chunk, ChunkingStrategy` -> verify current exports; `ChunkingStrategy` may not exist (use string "fixed" etc. or check actual module)
    - `docsift.core.document.DocumentMetadata` -> check if exists in parser module or core.models
    - CLI commands: `index_add`, `index_update`, etc. do not exist in index.py; replace with actual command names like `update_cmd`, `embed_cmd`, `index_status_cmd`, `index_group` or remove tests for missing commands
    - CLI commands: `search_query`, `search_interactive`, `search_similar`, `search_explain` do not exist in search.py; replace with actual names like `search_cmd`, `vsearch_cmd`, `query_cmd`, `search_group` or remove tests for missing commands
    - CLI commands: `collection_create`, `collection_delete`, `collection_add_path`, `collection_remove_path` do not exist in collection.py; replace with actual names like `collection_add`, `collection_remove`, `collection_rename`, `collection_list`, `collection_show`, `collection_group` or remove tests for missing commands

    After fixing imports, run `pytest` on each file. If a test file still fails due to missing fixtures or unrelated issues, ensure at minimum the import errors are resolved.
  </action>
  <verify>
    <automated>pytest tests/unit/cli/test_index_commands.py tests/unit/cli/test_search_commands.py tests/unit/cli/test_collection_commands.py tests/unit/search/test_bm25.py tests/unit/search/test_hybrid.py tests/unit/search/test_vector.py tests/unit/indexing/test_chunker.py tests/unit/indexing/test_parser.py tests/integration/test_index_and_search.py tests/integration/test_search_pipeline.py -x</automated>
  </verify>
  <done>
    All listed test files have valid imports and pytest can collect them without ImportError.
  </done>
  <acceptance_criteria>
    - `pytest --collect-only` succeeds for each of the 10 test files
    - No `ModuleNotFoundError` or `ImportError` from stale names
    - `ruff check tests/unit/cli tests/unit/search tests/unit/indexing tests/integration` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment variables -> Settings | Untrusted user input crosses into application config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | Settings.api_key | mitigate | Field uses `exclude=True` so Pydantic repr excludes it; never log api_key |
| T-03-01-02 | Spoofing | Settings.api_base | mitigate | Validator rejects non-HTTP/HTTPS schemes to prevent SSRF |
</threat_model>

<verification>
- Run `pytest tests/unit/config/test_settings.py -x` — must pass
- Run `ruff check src/docsift/config/settings.py tests/unit/config/test_settings.py` — must pass
- Run `ruff format --check src/docsift/config/settings.py tests/unit/config/test_settings.py` — must pass
- Run `pytest tests/unit/cli/test_index_commands.py tests/unit/cli/test_search_commands.py tests/unit/cli/test_collection_commands.py tests/unit/search/test_bm25.py tests/unit/search/test_hybrid.py tests/unit/search/test_vector.py tests/unit/indexing/test_chunker.py tests/unit/indexing/test_parser.py tests/integration/test_index_and_search.py tests/integration/test_search_pipeline.py -x` — must pass (import errors fixed)
</verification>

<success_criteria>
- Settings accepts model_type, api_base, api_key with correct defaults and validation
- Unit tests cover all new fields and validators
- Code passes ruff lint and format checks
- Stale test imports are fixed across all listed test files
</success_criteria>

<output>
After completion, create `.planning/phases/03-embedding-vector-search/03-01-SUMMARY.md`
</output>
