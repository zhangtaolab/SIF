---
phase: 08-project-rename-from-docsift-to-sif
plan: 07
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - .claude/skills/docsift-search/SKILL.md (renamed to .claude/skills/sif-search/SKILL.md)
  - .claude/skills/docsift-get/SKILL.md (renamed to .claude/skills/sif-get/SKILL.md)
  - .claude/skills/sif-search/SKILL.md
  - .claude/skills/sif-get/SKILL.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Skill directories renamed from docsift-search/docsift-get to sif-search/sif-get"
    - "SKILL.md files reference 'sif' CLI commands (not 'docsift')"
    - "SKILL.md name fields are 'sif-search' and 'sif-get'"
    - "SKILL.md descriptions reference SIF (not DocSift)"
    - "No 'docsift' references remain in skill files"
  artifacts:
    - path: ".claude/skills/sif-search/SKILL.md"
      provides: "Updated search skill with sif commands"
    - path: ".claude/skills/sif-get/SKILL.md"
      provides: "Updated get skill with sif commands"
  key_links:
    - from: "skill files"
      to: "sif CLI"
      via: "bash commands like 'sif search query'"
      pattern: "sif search query|sif get get"
---

<objective>
Rename Claude Skill directories and update skill files to use sif CLI commands and SIF branding.

Purpose: Claude Skills are user-facing tools that invoke the CLI; they must use the new command name per D-07.
Output: Skills renamed to sif-search and sif-get, all command references updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-project-rename-from-docsift-to-sif/08-CONTEXT.md

## Current skill files
- .claude/skills/docsift-search/SKILL.md — search skill
- .claude/skills/docsift-get/SKILL.md — get skill

## Changes needed
1. Rename directories: docsift-search → sif-search, docsift-get → sif-get
2. Update SKILL.md frontmatter: name = "sif-search" / "sif-get"
3. Update descriptions: "DocSift" → "SIF"
4. Update all CLI command examples: `docsift search` → `sif search`, `docsift get` → `sif get`
5. Update argument-hint fields
6. Update all process steps that build bash commands
7. Update error handling messages
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename skill directories and update skill file contents</name>
  <files>.claude/skills/docsift-search/ → .claude/skills/sif-search/, .claude/skills/docsift-get/ → .claude/skills/sif-get/</files>
  <read_first>
    - .claude/skills/docsift-search/SKILL.md
    - .claude/skills/docsift-get/SKILL.md
  </read_first>
  <action>
    Step 1: Rename directories using git mv:
    ```bash
    git mv .claude/skills/docsift-search .claude/skills/sif-search
    git mv .claude/skills/docsift-get .claude/skills/sif-get
    ```

    Step 2: Update sif-search/SKILL.md:
    - Change `name: docsift-search` to `name: sif-search`
    - Change description: "Search documents in DocSift index..." to "Search documents in SIF index..."
    - Change argument-hint: update if it mentions docsift
    - Change objective: "Search the user's DocSift document index..." to "Search the user's SIF document index..."
    - Change all CLI command examples:
      - `docsift -q search query` → `sif -q search query`
      - `docsift -q search search` → `sif -q search search`
      - `docsift -q search vsearch` → `sif -q search vsearch`
      - `docsift -q search query --json --limit 10 --line-numbers` → `sif -q search query --json --limit 10 --line-numbers`
    - Change error handling: "docsift update" → "sif update"
    - Change "Requires docsift CLI" → "Requires sif CLI"
    - Update example section commands

    Step 3: Update sif-get/SKILL.md:
    - Change `name: docsift-get` to `name: sif-get`
    - Change description: "Retrieve document content from DocSift index..." to "Retrieve document content from SIF index..."
    - Change objective: "Retrieve document content from the user's DocSift index." to "Retrieve document content from the user's SIF index."
    - Change all CLI command examples:
      - `docsift -q get get` → `sif -q get get`
      - `docsift -q get multi-get` → `sif -q get multi-get`
    - Change error handling: "docsift update" → "sif update"
    - Change "Requires docsift CLI" → "Requires sif CLI"
    - Update example section commands

    Use sed for bulk replacement in both files:
    ```bash
    for f in .claude/skills/sif-search/SKILL.md .claude/skills/sif-get/SKILL.md; do
      sed -i 's/name: docsift-search/name: sif-search/g' "$f"
      sed -i 's/name: docsift-get/name: sif-get/g' "$f"
      sed -i 's/DocSift/SIF/g' "$f"
      sed -i 's/docsift/sif/g' "$f"
    done
    ```

    After sed, manually review both files to ensure:
    - Frontmatter name field is correct
    - All bash command examples use sif
    - Error messages reference sif update
    - No over-replacement (e.g., in URLs or paths that shouldn't change)
  </action>
  <verify>
    <automated>
      test -d .claude/skills/sif-search &&
      test -d .claude/skills/sif-get &&
      test ! -d .claude/skills/docsift-search &&
      test ! -d .claude/skills/docsift-get &&
      grep -q "name: sif-search" .claude/skills/sif-search/SKILL.md &&
      grep -q "name: sif-get" .claude/skills/sif-get/SKILL.md &&
      grep -q "sif -q search query" .claude/skills/sif-search/SKILL.md &&
      grep -q "sif -q get get" .claude/skills/sif-get/SKILL.md &&
      ! grep -q "docsift" .claude/skills/sif-search/SKILL.md &&
      ! grep -q "docsift" .claude/skills/sif-get/SKILL.md &&
      echo "OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/sif-search/ directory exists
    - .claude/skills/sif-get/ directory exists
    - .claude/skills/docsift-search/ directory does NOT exist
    - .claude/skills/docsift-get/ directory does NOT exist
    - sif-search/SKILL.md contains `name: sif-search`
    - sif-get/SKILL.md contains `name: sif-get`
    - sif-search/SKILL.md contains `sif -q search query`
    - sif-get/SKILL.md contains `sif -q get get`
    - Neither SKILL.md contains "docsift"
  </acceptance_criteria>
  <done>
    - Skill directories renamed with git mv
    - Both SKILL.md files updated with sif commands
    - No docsift references in skill files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill → CLI | Skills invoke CLI commands; wrong name breaks functionality |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Denial of Service | Skill uses wrong CLI command | mitigate | Verify all bash examples use "sif" not "docsift" |
</threat_model>

<verification>
- `grep -c "docsift" .claude/skills/sif-search/SKILL.md` returns 0
- `grep -c "docsift" .claude/skills/sif-get/SKILL.md` returns 0
- `grep -c "sif " .claude/skills/sif-search/SKILL.md` returns >0 (commands present)
- `grep -c "sif " .claude/skills/sif-get/SKILL.md` returns >0 (commands present)
</verification>

<success_criteria>
- Skill directories renamed to sif-search and sif-get
- Both SKILL.md files use sif CLI commands
- No docsift references in skill files
- Skills reference SIF (not DocSift) in descriptions
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-rename-from-docsift-to-sif/08-07-SUMMARY.md`
</output>
