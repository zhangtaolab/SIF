---
phase: 08-project-rename-from-docsift-to-sif
plan: 04
type: execute
wave: 3
depends_on: ["08-03"]
files_modified:
  - src/sif/config/settings.py
  - src/sif/config/constants.py (APP_NAME already updated, verify no stale refs)
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings.env_prefix equals 'SIF_'"
    - "Settings docstring references SIF_ prefix"
    - "Settings.get_db_path() returns path ending in sif.db"
    - "Settings.get_cache_dir() returns path under sif/"
    - "No 'DOCSIFT_' strings remain in settings.py"
  artifacts:
    - path: "src/sif/config/settings.py"
      provides: "Updated Pydantic Settings with SIF_ prefix"
  key_links:
    - from: "src/sif/config/settings.py"
      to: "src/sif/config/constants.py"
      via: "import of APP_NAME"
      pattern: "from sif.config.constants import APP_NAME"
---

<objective>
Update the Pydantic Settings class to use SIF_ environment variable prefix per D-05, and verify all settings-related paths derive correctly from the updated constants.

Purpose: Environment variables are the primary configuration mechanism; the prefix change is a critical breaking change per D-03/D-05.
Output: Settings class uses SIF_ prefix, all paths resolve to sif directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-project-rename-from-docsift-to-sif/08-CONTEXT.md
@src/sif/config/settings.py
@src/sif/config/constants.py

## Current settings.py values that MUST change
- model_config env_prefix="DOCSIFT_" → env_prefix="SIF_"
- Docstring: "Environment variables should be prefixed with DOCSIFT_." → "Environment variables should be prefixed with SIF_."
- get_db_path() returns data_dir / "docsift.db" → data_dir / "sif.db"
- All `from docsift...` imports → `from sif...`

## Current constants.py (already updated in Plan 02)
- APP_NAME = "sif"
- DEFAULT_DB_PATH = "~/.local/share/sif/sif.db"
- DEFAULT_MODEL_PATH = "~/.local/share/sif/models"
- DEFAULT_CONFIG_PATH = "~/.config/sif"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update settings.py env_prefix and all docsift references</name>
  <files>src/sif/config/settings.py</files>
  <read_first>
    - src/sif/config/settings.py (read entire file)
    - src/sif/config/constants.py (verify APP_NAME = "sif")
  </read_first>
  <action>
    Update src/sif/config/settings.py with these EXACT changes:

    1. Change the module docstring from:
       """Configuration settings for DocSift."""
       to:
       """Configuration settings for SIF."""

    2. Change the Settings class docstring from:
       """Application settings using Pydantic Settings.

       Settings are loaded from environment variables and .env files.
       Environment variables should be prefixed with DOCSIFT_.
       """
       to:
       """Application settings using Pydantic Settings.

       Settings are loaded from environment variables and .env files.
       Environment variables should be prefixed with SIF_.
       """

    3. Change env_prefix in model_config from:
       env_prefix="DOCSIFT_",
       to:
       env_prefix="SIF_",

    4. Change get_db_path() return statement from:
       return data_dir / "docsift.db"
       to:
       return data_dir / "sif.db"

    5. Update all `from docsift...` imports to `from sif...`:
       - `from docsift.config.constants import APP_NAME, DEFAULT_CHUNK_OVERLAP, DEFAULT_CHUNK_SIZE`
         → `from sif.config.constants import APP_NAME, DEFAULT_CHUNK_OVERLAP, DEFAULT_CHUNK_SIZE`

    6. Verify no other "docsift" or "DOCSIFT" strings remain in the file.
  </action>
  <verify>
    <automated>
      grep -q 'env_prefix="SIF_"' src/sif/config/settings.py &&
      grep -q 'prefixed with SIF_' src/sif/config/settings.py &&
      grep -q 'return data_dir / "sif.db"' src/sif/config/settings.py &&
      grep -q "from sif.config.constants import" src/sif/config/settings.py &&
      ! grep -q 'DOCSIFT_' src/sif/config/settings.py &&
      ! grep -q "from docsift" src/sif/config/settings.py &&
      echo "OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - settings.py contains `env_prefix="SIF_"` (exact)
    - settings.py docstring contains "prefixed with SIF_" (not "DOCSIFT_")
    - settings.py get_db_path() returns `data_dir / "sif.db"` (not "docsift.db")
    - settings.py contains `from sif.config.constants import` (not `from docsift.config.constants`)
    - settings.py does NOT contain any "DOCSIFT_" string
    - settings.py does NOT contain any "from docsift" import
  </acceptance_criteria>
  <done>
    - env_prefix is "SIF_"
    - All docstrings reference SIF_ prefix
    - Database path returns sif.db
    - All imports use sif
    - No DOCSIFT_ references remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Settings paths resolve correctly</name>
  <files>src/sif/config/settings.py</files>
  <read_first>
    - src/sif/config/settings.py
    - src/sif/config/constants.py
  </read_first>
  <action>
    Create a quick verification script to confirm Settings paths resolve to sif directories:

    ```python
    # verify_settings.py
    import sys
    sys.path.insert(0, 'src')
    from sif.config.settings import Settings
    from sif.config.constants import APP_NAME

    s = Settings()
    db_path = s.get_db_path()
    cache_dir = s.get_cache_dir()

    print(f"APP_NAME: {APP_NAME}")
    print(f"DB path: {db_path}")
    print(f"Cache dir: {cache_dir}")

    assert APP_NAME == "sif", f"APP_NAME should be 'sif', got {APP_NAME}"
    assert "sif" in str(db_path).lower(), f"DB path should contain 'sif': {db_path}"
    assert str(db_path).endswith("sif.db"), f"DB path should end with 'sif.db': {db_path}"
    assert "sif" in str(cache_dir).lower(), f"Cache dir should contain 'sif': {cache_dir}"
    print("All assertions passed!")
    ```

    Run: `python verify_settings.py`
    Then delete the verification script.
  </action>
  <verify>
    <automated>
      python -c "
import sys; sys.path.insert(0, 'src')
from sif.config.settings import Settings
from sif.config.constants import APP_NAME
s = Settings()
db = s.get_db_path()
cache = s.get_cache_dir()
assert APP_NAME == 'sif', f'APP_NAME={APP_NAME}'
assert str(db).endswith('sif.db'), f'db={db}'
assert 'sif' in str(cache).lower(), f'cache={cache}'
print('OK')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - APP_NAME constant equals "sif"
    - Settings.get_db_path() returns a Path ending in "sif.db"
    - Settings.get_cache_dir() returns a Path containing "sif"
    - Verification script runs without assertion errors
  </acceptance_criteria>
  <done>
    - Settings paths verified to use sif directories
    - APP_NAME verified to be "sif"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings → filesystem | DB and cache paths determine where user data lives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of Service | Wrong env_prefix breaks user configs | accept | Per D-03/D-05, complete break — no backward compatibility. Documented in migration guide. |
</threat_model>

<verification>
- `grep -c "DOCSIFT_" src/sif/config/settings.py` returns 0
- `grep -c "from docsift" src/sif/config/settings.py` returns 0
- `python -c "from sif.config.settings import Settings; s=Settings(); print(s.get_db_path())"` outputs path ending in sif.db
</verification>

<success_criteria>
- Settings.env_prefix is "SIF_"
- Settings.get_db_path() returns ~/.local/share/sif/sif.db (or equivalent)
- Settings.get_cache_dir() returns path under sif/
- No DOCSIFT_ references in settings.py
- All imports use sif
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-rename-from-docsift-to-sif/08-04-SUMMARY.md`
</output>
