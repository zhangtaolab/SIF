---
phase: 08-project-rename-from-docsift-to-sif
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/docsift/ (renamed to src/sif/)
autonomous: true
requirements: []
must_haves:
  truths:
    - "Directory src/docsift/ no longer exists"
    - "Directory src/sif/ exists with all original source files"
    - "pyproject.toml references sif instead of docsift for package name, scripts, and tool configs"
  artifacts:
    - path: "src/sif/"
      provides: "Renamed Python package directory"
    - path: "pyproject.toml"
      provides: "Updated project metadata and tool configuration"
  key_links:
    - from: "pyproject.toml"
      to: "src/sif/"
      via: "tool.hatch.build.targets.wheel.packages"
      pattern: 'packages = \["src/sif"\]'
---

<objective>
Rename the Python package directory from src/docsift/ to src/sif/ and update pyproject.toml with the new package name, CLI script entry point, and tool configurations (ruff, mypy, pytest).

Purpose: This is the foundational infrastructure change. All subsequent plans depend on the directory and pyproject.toml being correct.
Output: src/sif/ directory exists, pyproject.toml fully updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-project-rename-from-docsift-to-sif/08-CONTEXT.md
@pyproject.toml

## Key values from current pyproject.toml

Current values that MUST change:
- name = "docsift" → name = "sif"
- description = "DocSift - Document indexing and search tool" → "SIF - Search / Index / Find - Local document search engine"
- authors = [{name = "DocSift Team"}] → [{name = "SIF Team"}]
- docsift = "docsift.cli.main:main" → sif = "sif.cli.main:main"
- packages = ["src/docsift"] → ["src/sif"]
- known-first-party = ["docsift"] → ["sif"]
- addopts = "--cov=src/docsift ..." → "--cov=src/sif ..."
- Homepage = "https://github.com/docsift/docsift" → "https://github.com/zhangtaolab/sif"
- Repository = "https://github.com/docsift/docsift" → "https://github.com/zhangtaolab/sif"
- keywords: add "sif"

Current values that stay the same:
- version = "0.1.0"
- requires-python = ">=3.9"
- All dependency lists
- All tool configurations except the specific fields above
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename src/docsift/ directory to src/sif/</name>
  <files>src/docsift/ → src/sif/</files>
  <read_first>
    - src/docsift/__init__.py (verify directory contents before rename)
  </read_first>
  <action>
    Use `git mv src/docsift src/sif` to rename the directory. This preserves git history.
    Verify the rename succeeded by checking that src/sif/__init__.py exists and src/docsift/ no longer exists.
    Do NOT modify any file contents in this task — only rename the directory.
  </action>
  <verify>
    <automated>test -d src/sif &amp;&amp; test ! -d src/docsift &amp;&amp; test -f src/sif/__init__.py &amp;&amp; echo "OK"</automated>
  </verify>
  <done>
    - Directory src/docsift/ no longer exists
    - Directory src/sif/ exists with all original files
    - Git tracks the rename (git status shows rename, not delete+add)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update pyproject.toml package metadata and tool configs</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current state — read the entire file)
  </read_first>
  <action>
    Update pyproject.toml with these EXACT changes:

    1. [project] section:
       - name = "sif"
       - description = "SIF - Search / Index / Find - Local document search engine"
       - authors = [{name = "SIF Team"}]
       - keywords = ["search", "documents", "indexing", "cli", "vector-search", "sif"]

    2. [project.scripts] section:
       - Change `docsift = "docsift.cli.main:main"` to `sif = "sif.cli.main:main"`

    3. [project.urls] section:
       - Homepage = "https://github.com/zhangtaolab/sif"
       - Documentation = "https://sif.readthedocs.io"
       - Repository = "https://github.com/zhangtaolab/sif"

    4. [tool.hatch.build.targets.wheel] section:
       - packages = ["src/sif"]

    5. [tool.ruff.lint.isort] section:
       - known-first-party = ["sif"]

    6. [tool.ruff.lint.per-file-ignores] section:
       - Change `"src/docsift/cli/*.py"` to `"src/sif/cli/*.py"`

    7. [tool.pytest.ini_options] section:
       - Change addopts from `--cov=src/docsift` to `--cov=src/sif`

    8. [tool.mypy] section:
       - No changes needed (mypy_path = ["src"] is already correct)

    Do NOT change: version, dependencies, optional-dependencies, classifiers (except if they reference docsift), black config, ruff lint rules, mypy strict settings.
  </action>
  <verify>
    <automated>
      grep -q '^name = "sif"$' pyproject.toml &amp;&amp;
      grep -q 'sif = "sif.cli.main:main"' pyproject.toml &amp;&amp;
      grep -q 'packages = \["src/sif"\]' pyproject.toml &amp;&amp;
      grep -q 'known-first-party = \["sif"\]' pyproject.toml &amp;&amp;
      grep -q 'src/sif/cli/\*.py' pyproject.toml &amp;&amp;
      grep -q 'cov=src/sif' pyproject.toml &amp;&amp;
      grep -q 'zhangtaolab/sif' pyproject.toml &amp;&amp;
      ! grep -q 'name = "docsift"' pyproject.toml &amp;&amp;
      echo "OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains `name = "sif"` (exact line)
    - pyproject.toml contains `sif = "sif.cli.main:main"` in [project.scripts]
    - pyproject.toml contains `packages = ["src/sif"]` in [tool.hatch.build.targets.wheel]
    - pyproject.toml contains `known-first-party = ["sif"]` in [tool.ruff.lint.isort]
    - pyproject.toml contains `src/sif/cli/*.py` in [tool.ruff.lint.per-file-ignores]
    - pyproject.toml contains `--cov=src/sif` in [tool.pytest.ini_options]
    - pyproject.toml contains `github.com/zhangtaolab/sif` in URLs
    - pyproject.toml does NOT contain `name = "docsift"`
    - pyproject.toml does NOT contain `docsift = "docsift.cli.main:main"`
    - pyproject.toml does NOT contain `packages = ["src/docsift"]`
    - pyproject.toml does NOT contain `known-first-party = ["docsift"]`
    - pyproject.toml does NOT contain `--cov=src/docsift`
  </acceptance_criteria>
  <done>
    - All pyproject.toml references updated from docsift to sif
    - No remaining docsift references in pyproject.toml
    - File passes `tomli` parse validation (Python: `import tomllib; tomllib.load(open("pyproject.toml","rb"))`)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system | Renaming directory could break imports if not followed by import updates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Denial of Service | Directory rename | mitigate | Use `git mv` to preserve history; verify rename before proceeding |
</threat_model>

<verification>
- `git status` shows rename of src/docsift/ → src/sif/
- `python -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` succeeds
- `grep -c "docsift" pyproject.toml` returns 0
</verification>

<success_criteria>
- src/sif/ directory exists with all 79 Python files
- src/docsift/ directory no longer exists
- pyproject.toml is valid TOML with all sif references
- No docsift strings remain in pyproject.toml
</success_criteria>

<output>
After completion, create `.planning/phases/08-project-rename-from-docsift-to-sif/08-01-SUMMARY.md`
</output>
