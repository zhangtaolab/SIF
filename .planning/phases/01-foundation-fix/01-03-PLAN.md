---
phase: 01-foundation-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/docsift/cli/commands/index.py, src/docsift/indexing/indexer.py, src/docsift/embedding/embedder.py]
autonomous: true
requirements: [FND-02, FND-04]
must_haves:
  truths:
    - "Document indexing correctly skips unchanged files and updates changed ones"
    - "Embedding commands use Settings model name instead of hardcoded path"
  artifacts:
    - path: "src/docsift/cli/commands/index.py"
      provides: "Correct checksum logic and Settings-driven model"
      contains: "existing.checksum == parsed.checksum"
    - path: "src/docsift/indexing/indexer.py"
      provides: "Correct checksum logic in indexer"
      contains: "existing.checksum == checksum"
    - path: "src/docsift/embedding/embedder.py"
      provides: "No hardcoded model path"
      pattern: "all-MiniLM-L6-v2"
---

<objective>
Fix the inverted checksum comparison in indexing and remove the hardcoded embedding model path per FND-02 and FND-04.

Purpose: The current logic skips changed documents instead of unchanged ones, breaking incremental indexing. The `embed` command and `SentenceTransformerEmbedder` also hardcode `BAAI/bge-small-zh-v1.5` instead of respecting `Settings.model_name`.
Output: Indexing correctly skips only unchanged files; embedding commands use the model name from Settings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-fix/01-CONTEXT.md
@.planning/phases/01-foundation-fix/01-RESEARCH.md
@src/docsift/cli/commands/index.py
@src/docsift/indexing/indexer.py
@src/docsift/embedding/embedder.py
@src/docsift/config/settings.py
</context>

<tasks>

<task type="auto">
  <files>src/docsift/cli/commands/index.py, src/docsift/indexing/indexer.py</files>
  <name>Task 1: Fix inverted checksum comparison in indexing</name>
  <read_first>
    src/docsift/cli/commands/index.py
    src/docsift/indexing/indexer.py
    .planning/phases/01-foundation-fix/01-RESEARCH.md (Pitfall 1: Inverted Checksum Condition)
  </read_first>
  <action>
    Fix the checksum logic in two locations so that unchanged documents are skipped and changed documents are updated.

    1. In `src/docsift/cli/commands/index.py` around line 108, change:
       ```python
       if existing.checksum != parsed.checksum and not force:
       ```
       to:
       ```python
       if existing.checksum == parsed.checksum and not force:
       ```
       Also update the log message from "Unchanged:" to remain "Unchanged:" (it is already correct).

    2. In `src/docsift/indexing/indexer.py` around line 163, the `_index_file` method currently has:
       ```python
       if existing:
           if existing.checksum == checksum:
               logger.debug(f"File unchanged, skipping: {file_path}")
               return IndexStatus.SKIPPED
       ```
       This appears correct at first glance, but verify it matches the intended logic: if checksums are equal and force is False, skip. If the code already looks like this, check whether a `and not force` guard is missing. If missing, add it:
       ```python
       if existing.checksum == checksum and not force:
       ```
       If the `not force` guard is already present, leave it as-is.
  </action>
  <verify>
    <automated>grep -n "existing.checksum == parsed.checksum and not force" src/docsift/cli/commands/index.py && grep -n "existing.checksum == checksum" src/docsift/indexing/indexer.py</automated>
  </verify>
  <done>
    - `src/docsift/cli/commands/index.py` contains `existing.checksum == parsed.checksum and not force`.
    - `src/docsift/indexing/indexer.py` contains `existing.checksum == checksum and not force` (or equivalent skip-unchanged logic).
  </done>
</task>

<task type="auto">
  <files>src/docsift/cli/commands/index.py, src/docsift/embedding/embedder.py</files>
  <name>Task 2: Remove hardcoded model path from embed_cmd and SentenceTransformerEmbedder</name>
  <read_first>
    src/docsift/cli/commands/index.py
    src/docsift/embedding/embedder.py
    src/docsift/config/settings.py
  </read_first>
  <action>
    Replace the hardcoded `BAAI/bge-small-zh-v1.5` model path with `Settings.model_name` in two locations.

    1. In `src/docsift/cli/commands/index.py` in the `embed_cmd` function (around line 185), the code currently does:
       ```python
       embedder = SentenceTransformer("BAAI/bge-small-zh-v1.5")
       ```
       Change it to load settings first and use the configured model name:
       ```python
       from docsift.config.settings import get_settings
       settings = get_settings()
       embedder = SentenceTransformer(settings.model_name)
       ```
       Add the import at the top of the function or at module level (module level is fine if it does not cause circular imports; inside the function is safer).

    2. In `src/docsift/embedding/embedder.py`, the `SentenceTransformerEmbedder` class has a default parameter:
       ```python
       model_name: str = "BAAI/bge-small-zh-v1.5",
       ```
       Change the default to:
       ```python
       model_name: str = "all-MiniLM-L6-v2",
       ```
       This aligns with the default in `Settings.model_name`.
  </action>
  <verify>
    <automated>grep -n "BAAI/bge-small-zh-v1.5" src/docsift/cli/commands/index.py src/docsift/embedding/embedder.py || echo "PASS: no hardcoded path found"</automated>
  </verify>
  <done>
    - No file contains the string `BAAI/bge-small-zh-v1.5`.
    - `embed_cmd` uses `settings.model_name` (or `get_settings().model_name`) when constructing `SentenceTransformer`.
    - `SentenceTransformerEmbedder` default `model_name` is `"all-MiniLM-L6-v2"`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → indexer | File contents and paths enter the indexing pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | indexer.py checksum comparison | mitigate | Fix inverted condition so attackers cannot prevent document updates by modifying files |
| T-03-02 | Information Disclosure | embed_cmd model path logging | accept | Model name is not sensitive; logging it is standard behavior |
</threat_model>

<verification>
- `python -c "from docsift.indexing.indexer import DocumentIndexer; print('OK')"` succeeds.
- `python -c "from docsift.cli.commands.index import embed_cmd; print('OK')"` succeeds.
- `python -c "from docsift.embedding.embedder import SentenceTransformerEmbedder; print('OK')"` succeeds.
</verification>

<success_criteria>
- Indexing skips only files whose checksum matches the existing record.
- Changed files are updated even without `--force`.
- `embed_cmd` and `SentenceTransformerEmbedder` no longer hardcode `BAAI/bge-small-zh-v1.5`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fix/01-03-SUMMARY.md`
</output>
