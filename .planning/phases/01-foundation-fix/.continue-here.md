---
context: phase
phase: 01-foundation-fix
task: 1
total_tasks: 6
status: ready_to_execute
last_updated: 2026-04-14T15:08:39.216Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

_If no constraints have been identified yet, remove this section._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Phase 1: Foundation Fix planning is complete. Six executable plans have been created and verified, but no execution has started yet. The user paused work after updating GSD from v1.34.2 to v1.36.0.
</current_state>

<completed_work>

Completed Tasks:
- Phase 1 planning — Done. 6 plans created covering all 8 requirements (FND-01 through FND-08).
- GSD update — Done. Updated to v1.36.0.
</completed_work>

<remaining_work>

- Plan 01-01: Fix missing runtime dependencies in pyproject.toml
- Plan 01-02: Fix FTS5 synchronization and consolidate repository files
- Plan 01-03: Fix inverted checksum comparison and remove hardcoded model path
- Plan 01-04: Replace placeholder embeddings with real ST and GGUF implementations
- Plan 01-05: Remove vector search fallback and make vsearch fail fast
- Plan 01-06: Improve SQLite connection safety for async/multi-threaded contexts
</remaining_work>

<decisions_made>

- FTS5 synchronization will use SQLite triggers on external content tables (D-01).
- `sqlite_repository.py` will be deleted and all access consolidated into `repositories.py` (D-02).
- CLI keeps single `Database` connection; async HTTP contexts use connection-per-request (D-04, D-05).
- Vector search will fail fast with a clear `RuntimeError` when `sqlite-vec` is unavailable (D-06, D-07).
- Heavy ML libraries (`sentence-transformers`, `llama-cpp-python`) remain optional extras (D-09).
</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)

1. `.planning/phases/01-foundation-fix/01-CONTEXT.md` — locked implementation decisions from discuss-phase
2. `.planning/phases/01-foundation-fix/01-RESEARCH.md` — technical research including FTS5 trigger patterns and embedding implementation notes
3. `.planning/phases/01-foundation-fix/01-01-PLAN.md` through `01-06-PLAN.md` — the executable plans
4. `.planning/ROADMAP.md` — phase goal and success criteria
5. `.planning/REQUIREMENTS.md` — FND-01 through FND-08 requirements

## Critical Anti-Patterns (do NOT repeat these)

- None discovered yet — this will be the first execution pass.

## Infrastructure State

- GSD version: 1.36.0 (just updated)
- Project: DocSift (Python 3.10+, SQLite, Click)
- Environment: No running servers or background processes

## Pre-Execution Critique Required

- Not applicable — plans have already passed the plan-checker verification gate.

<context>
The user completed Phase 1 planning, viewed the 6 existing plans, then ran a GSD update before deciding to pause. No execution has occurred. The STATE.md is slightly stale (still lists "awaiting roadmap approval" as the status) but the phase directory is the ground truth. The next logical step is to execute Phase 1 with `/gsd-execute-phase 1`.
</context>

<next_action>
Start with: Run `/gsd-execute-phase 1` to begin executing all 6 plans in Phase 1: Foundation Fix.
</next_action>
