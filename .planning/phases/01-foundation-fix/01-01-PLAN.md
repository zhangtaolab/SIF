---
phase: 01-foundation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
autonomous: true
requirements:
  - FND-01
user_setup: []
must_haves:
  truths:
    - "Project installs cleanly with all runtime dependencies declared"
    - "docsift --help runs without ModuleNotFoundError"
  artifacts:
    - path: "pyproject.toml"
      provides: "Complete dependency declarations"
      contains: "sqlite-vec>=0.1.9"
  key_links:
    - from: "pyproject.toml"
      to: "pip install"
      via: "dependencies array"
---

<objective>
Fix missing runtime dependencies in pyproject.toml so the project installs cleanly and all imports succeed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-fix/01-CONTEXT.md
@.planning/phases/01-foundation-fix/01-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing runtime dependencies to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
    - .planning/phases/01-foundation-fix/01-RESEARCH.md
  </read_first>
  <action>
    Edit the [project] dependencies list in pyproject.toml. Replace the current dependencies array with:

    dependencies = [
        "click>=8.0.0",
        "rich>=13.0.0",
        "python-frontmatter>=1.0.0",
        "sqlite-vec>=0.1.9",
        "platformdirs>=3.0.0",
        "pydantic>=2.0.0",
        "pydantic-settings>=2.0.0",
        "fastapi>=0.100.0",
        "uvicorn>=0.20.0",
        "watchdog>=3.0.0",
        "numpy>=1.24.0",
    ]

    Do NOT add structlog (it is not imported anywhere per research). Keep the existing [project.optional-dependencies] sections exactly as they are (dev, embed, all). Do not change any tool configuration sections.
  </action>
  <verify>
    <automated>grep -q 'sqlite-vec>=0.1.9' pyproject.toml && grep -q 'watchdog>=3.0.0' pyproject.toml && grep -q 'numpy>=1.24.0' pyproject.toml && ! grep -q 'structlog' pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains "sqlite-vec>=0.1.9" in the dependencies list
    - pyproject.toml contains "watchdog>=3.0.0" in the dependencies list
    - pyproject.toml contains "numpy>=1.24.0" in the dependencies list
    - pyproject.toml does NOT contain "structlog"
    - The existing [project.optional-dependencies] sections remain unchanged
  </acceptance_criteria>
  <done>All missing runtime dependencies are added and no extraneous dependencies are present.</done>
</task>

<task type="auto">
  <name>Task 2: Verify installation and basic import</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml
  </read_first>
  <action>
    Run pip install in editable mode, then verify the CLI entry point works:

    pip install -e "."
    python -m docsift --help

    If any import errors occur, diagnose and fix the root cause (which should only be missing dependencies).
  </action>
  <verify>
    <automated>python -m docsift --help | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Command `python -m docsift --help` exits with code 0
    - Output contains "Usage:" or "docsift"
  </acceptance_criteria>
  <done>CLI launches without ModuleNotFoundError.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pyproject.toml -> pip install | Dependency declarations affect what code is downloaded and executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pyproject.toml dependencies | accept | Dependencies are from PyPI with version pinning; no additional mitigation needed for local tool |
</threat_model>

<verification>
- `grep 'sqlite-vec>=0.1.9' pyproject.toml` succeeds
- `python -m docsift --help` runs without errors
</verification>

<success_criteria>
- All missing runtime dependencies are declared in pyproject.toml
- `pip install -e .` succeeds
- `docsift --help` executes without import errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fix/01-01-SUMMARY.md`
</output>
