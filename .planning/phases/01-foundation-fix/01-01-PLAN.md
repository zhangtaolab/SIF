---
phase: 01-foundation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml]
autonomous: true
requirements: [FND-01]
must_haves:
  truths:
    - "Project installs cleanly with all runtime dependencies declared in pyproject.toml"
    - "Heavy ML libraries remain optional extras"
  artifacts:
    - path: "pyproject.toml"
      provides: "Complete dependency declarations"
      contains: "sqlite-vec"
  key_links: []
---

<objective>
Fix missing runtime dependencies in pyproject.toml per D-08 and D-09.

Purpose: The package currently fails to install all required runtime dependencies, causing `ModuleNotFoundError` for `sqlite-vec`, `pydantic-settings`, `platformdirs`, and others.
Output: Updated pyproject.toml with complete `[project] dependencies` and restructured `[project.optional-dependencies]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-fix/01-CONTEXT.md
@.planning/phases/01-foundation-fix/01-RESEARCH.md
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <files>pyproject.toml</files>
  <name>Task 1: Add missing runtime dependencies to pyproject.toml</name>
  <read_first>
    pyproject.toml
    .planning/phases/01-foundation-fix/01-RESEARCH.md (Standard Stack section)
  </read_first>
  <action>
    Update the `[project] dependencies` list in pyproject.toml to include all missing runtime dependencies per D-08. The current list only has `click>=8.0.0`, `rich>=13.0.0`, and `python-frontmatter>=1.0.0`. Append the following packages with the exact version constraints:
    - `sqlite-vec>=0.1.9`
    - `structlog>=25.0.0`
    - `platformdirs>=4.0.0`
    - `pydantic>=2.0`
    - `pydantic-settings>=2.0`
    - `watchdog>=6.0.0`
    - `fastapi>=0.131.0`
    - `uvicorn>=0.41.0`
    - `numpy>=1.24.0`
    
    Keep the existing three dependencies. Do NOT add `sentence-transformers` or `llama-cpp-python` to the main dependencies list (those go in optional extras per D-09).
  </action>
  <verify>
    <automated>grep -E "sqlite-vec|structlog|platformdirs|pydantic-settings|watchdog|fastapi|uvicorn|numpy" pyproject.toml</automated>
  </verify>
  <done>
    pyproject.toml `[project] dependencies` contains all 12 required packages with correct version constraints.
  </done>
</task>

<task type="auto">
  <files>pyproject.toml</files>
  <name>Task 2: Restructure optional dependencies</name>
  <read_first>
    pyproject.toml
  </read_first>
  <action>
    Restructure `[project.optional-dependencies]` in pyproject.toml per D-09:
    
    1. Keep the `dev` group exactly as-is.
    2. Rename or keep `[embed]` with:
       - `sentence-transformers>=2.2.0`
       - `numpy>=1.24.0`
    3. Add a new `[llm]` group with:
       - `llama-cpp-python>=0.3.20`
    4. Update `[all]` to use references (if supported by hatchling/pep 621, use the string form) or duplicate the packages so that `pip install -e ".[all]"` gets both embedding and LLM backends. The standard hatchling-compatible way is:
       ```toml
       all = [
           "sentence-transformers>=2.2.0",
           "numpy>=1.24.0",
           "llama-cpp-python>=0.3.20",
       ]
       ```
    
    Remove the old minimal `[all]` if it only duplicated `[embed]`.
  </action>
  <verify>
    <automated>grep -A 20 "\[project.optional-dependencies\]" pyproject.toml</automated>
  </verify>
  <done>
    `[project.optional-dependencies]` contains `dev`, `embed`, `llm`, and `all` groups. `all` includes both `sentence-transformers` and `llama-cpp-python`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package index → local install | Untrusted PyPI packages enter the local environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | pyproject.toml dependencies | accept | Version pins prevent accidental major-version upgrades; supply-chain risk is standard for pip installs |
| T-01-02 | Denial of Service | Optional heavy extras | accept | Users explicitly opt-in to `[embed]` and `[llm]`; no automatic download of large models |
</threat_model>

<verification>
- `pyproject.toml` passes `pip install -e .` without `ModuleNotFoundError` for core imports (`docsift.config.settings`, `docsift.database.connection`, `docsift.search.vector`).
- `pip install -e ".[embed,llm]"` resolves all extras.
</verification>

<success_criteria>
- `pyproject.toml` declares all runtime dependencies under `[project] dependencies`.
- Heavy ML libraries (`sentence-transformers`, `llama-cpp-python`) are only in optional extras.
- `pip install -e .` succeeds in the current environment.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-fix/01-01-SUMMARY.md`
</output>
