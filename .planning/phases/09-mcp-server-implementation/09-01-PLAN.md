---
phase: 09-mcp-server-implementation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sif/mcp/__init__.py
  - src/sif/mcp/protocol.py
  - src/sif/mcp/server.py
  - src/sif/mcp/handlers.py
  - src/sif/mcp/backend.py
  - src/sif/mcp/transports/__init__.py
  - src/sif/mcp/cli.py
  - src/sif/cli/commands/mcp.py
autonomous: true
requirements:
  - MCP-01
must_haves:
  truths:
    - "src/sif/mcp_server/ directory no longer exists"
    - "src/sif/mcp/ contains only unified implementation files"
    - "Legacy mock implementations are removed"
    - "New package structure matches D-03 directory layout"
  artifacts:
    - path: "src/sif/mcp/__init__.py"
      provides: "Package exports for unified MCP"
    - path: "src/sif/mcp/protocol.py"
      provides: "Reused Pydantic models per D-01"
    - path: "src/sif/mcp/server.py"
      provides: "Async MCPServer with state machine"
    - path: "src/sif/mcp/handlers.py"
      provides: "ToolHandler ABC per D-02"
    - path: "src/sif/mcp/backend.py"
      provides: "SearchBackend class (skeleton for now)"
    - path: "src/sif/mcp/transports/__init__.py"
      provides: "Transport subpackage exports"
    - path: "src/sif/mcp/cli.py"
      provides: "CLI integration helpers"
  key_links:
    - from: "src/sif/cli/commands/mcp.py"
      to: "src/sif/mcp/"
      via: "import from unified package"
---

## Objective

Unify the dual legacy/refactored MCP implementations into a single `src/sif/mcp/` package. Delete `src/sif/mcp_server/` and all old `mcp/` files, then create the new unified package structure with async server skeleton, ToolHandler ABC, and SearchBackend placeholder.

Purpose: Eliminate divergence; create one source of truth for MCP implementation.
Output: Clean `src/sif/mcp/` directory with 7 files + updated CLI imports.

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-mcp-server-implementation/09-CONTEXT.md
@.planning/phases/09-mcp-server-implementation/09-RESEARCH.md
@.planning/phases/09-mcp-server-implementation/09-PATTERNS.md

### Interfaces

From `src/sif/mcp/protocol.py` (keep exactly as-is):
```python
class JsonRpcRequest(BaseModel):
    jsonrpc: Literal["2.0"] = "2.0"
    id: Optional[str | int] = None
    method: str
    params: Optional[dict[str, Any]] = None

class JsonRpcResponse(BaseModel):
    jsonrpc: Literal["2.0"] = "2.0"
    id: Optional[str | int] = None
    result: Optional[Any] = None
    error: Optional[JsonRpcError] = None

class MCPErrorCode(int, Enum):
    PARSE_ERROR = -32700
    INVALID_REQUEST = -32600
    METHOD_NOT_FOUND = -32601
    INVALID_PARAMS = -32602
    INTERNAL_ERROR = -32603
    SERVER_NOT_INITIALIZED = -32002

class ToolsCallResult(BaseModel):
    content: list[ToolContentItem] = Field(default_factory=list)
    isError: bool = False

def create_success_response(id: str | int | None, result: Any) -> JsonRpcResponse
def create_error_response(id: str | int | None, code: int, message: str, data: Any = None) -> JsonRpcResponse
```

From `src/sif/mcp_server/handlers.py` (adopt pattern):
```python
class ToolHandler(ABC):
    @property @abstractmethod def name(self) -> str: ...
    @property @abstractmethod def description(self) -> str: ...
    @property @abstractmethod def input_schema(self) -> dict[str, Any]: ...
    @abstractmethod def handle(self, params: dict[str, Any]) -> dict[str, Any]: ...
```

From `src/sif/cli/commands/mcp.py` (current imports to update):
- `from sif.mcp.server import run_stdio_server` -> `from sif.mcp.transports.stdio import run_stdio_server`
- `from sif.mcp.server_http import run_http_server` -> `from sif.mcp.transports.http import run_http_server`

## Tasks

<task type="auto">
  <name>Task 1: Delete old implementations and create new package structure</name>
  <files>
    src/sif/mcp_server/ (delete)
    src/sif/mcp/transport_stdio.py (delete)
    src/sif/mcp/transport_http.py (delete)
    src/sif/mcp/server_http.py (delete)
    src/sif/mcp/tools.py (delete)
    src/sif/mcp/test_mcp.py (delete)
    src/sif/mcp/server.py (rewrite)
    src/sif/mcp/__init__.py (rewrite)
    src/sif/mcp/handlers.py (create)
    src/sif/mcp/backend.py (create)
    src/sif/mcp/transports/__init__.py (create)
    src/sif/mcp/cli.py (rewrite)
  </files>
  <read_first>
    src/sif/mcp/protocol.py
    src/sif/mcp_server/handlers.py
    src/sif/mcp_server/server.py
    src/sif/mcp/__init__.py
    src/sif/cli/commands/mcp.py
  </read_first>
  <action>
    1. Delete the entire `src/sif/mcp_server/` directory and all its contents.
    2. Delete these legacy files from `src/sif/mcp/`: `transport_stdio.py`, `transport_http.py`, `server_http.py`, `tools.py`, `test_mcp.py`.
    3. Keep `src/sif/mcp/protocol.py` exactly as-is (per D-01).
    4. Rewrite `src/sif/mcp/__init__.py` to export: `MCPServer`, `SearchBackend`, `ToolHandler`, `run_stdio_server`, `run_http_server`.
    5. Create `src/sif/mcp/handlers.py` with the `ToolHandler` ABC copied from `mcp_server/handlers.py`, BUT change `handle()` signature to `async def handle(self, params: dict[str, Any], backend: SearchBackend) -> ToolsCallResult` per D-02. Do NOT add ResourceHandler ABC — resources/read will be implemented inline in server.py per the unified dispatch pattern (avoids unused abstraction).
    6. Create `src/sif/mcp/backend.py` with a `SearchBackend` class skeleton. It should have `__init__(self, db_path: str, settings: Settings | None = None)` and these async method stubs: `hybrid_search`, `get_document`, `get_documents_by_pattern`, `get_status`. Each stub should `raise NotImplementedError` with a clear message. Import `Settings` from `sif.config.settings` and `get_settings`.
    7. Create `src/sif/mcp/transports/__init__.py` with no exports yet (transports will be added in later plans).
    8. Rewrite `src/sif/mcp/cli.py` as a thin module with two async helper functions: `run_stdio_server(db_path: str)` and `run_http_server(db_path: str, host: str, port: int, cors_origins: list[str] | None)` that raise `NotImplementedError` with message "Implemented in plan 09-04/09-05".
    9. Rewrite `src/sif/mcp/server.py` with an async `MCPServer` class containing:
       - `ServerState` enum: `CREATED`, `INITIALIZED`, `SHUTDOWN`
       - `__init__(self, backend: SearchBackend)` stores backend and sets state to `CREATED`
       - `_tools: dict[str, ToolHandler] = {}` registry
       - `register_tool(self, handler: ToolHandler) -> None` adds to registry
       - `async def handle_request(self, request: JsonRpcRequest) -> JsonRpcResponse` with state machine logic:
         - If method == "initialize": set state to INITIALIZED, return `InitializeResult`
         - If state != INITIALIZED: return `SERVER_NOT_INITIALIZED` error
         - If method == "tools/list": return `ToolsListResult` with registered tools
         - If method == "tools/call": dispatch to handler, return `ToolsCallResult`
         - If method == "resources/read": return error "Resources not yet implemented"
         - Else: return `METHOD_NOT_FOUND` error
       - `async def handle_notification(self, notification: JsonRpcNotification) -> None` that logs and handles `notifications/initialized` (no-op) and `notifications/cancelled` (no-op for now)
    10. Update `src/sif/cli/commands/mcp.py` imports to point to new unified package:
        - `from sif.mcp.cli import run_stdio_server, run_http_server`
        - Keep command signatures unchanged
        - Add `--cors-origins` option to `mcp_http_cmd` with default `None` (will resolve to secure defaults in transport)
  </action>
  <acceptance_criteria>
    - src/sif/mcp_server/ directory does not exist after deletion
    - src/sif/mcp/transport_stdio.py, transport_http.py, server_http.py, tools.py, test_mcp.py do not exist
    - src/sif/mcp/protocol.py is unchanged (byte-identical to before plan execution)
    - src/sif/mcp/__init__.py exports MCPServer, SearchBackend, ToolHandler, run_stdio_server, run_http_server
    - src/sif/mcp/server.py contains ServerState enum with CREATED, INITIALIZED, SHUTDOWN
    - src/sif/mcp/handlers.py contains ToolHandler ABC with async def handle(self, params, backend)
    - src/sif/mcp/backend.py contains SearchBackend class with async method stubs
    - src/sif/cli/commands/mcp.py imports from sif.mcp.cli
  </acceptance_criteria>
  <verify>
    <automated>
      test -d /Users/forrest/GitHub/SIF/src/sif/mcp_server/ && echo "FAIL: mcp_server still exists" || echo "PASS: mcp_server deleted"
      test -f /Users/forrest/GitHub/SIF/src/sif/mcp/transport_stdio.py && echo "FAIL: transport_stdio.py still exists" || echo "PASS: transport_stdio.py deleted"
      test -f /Users/forrest/GitHub/SIF/src/sif/mcp/transport_http.py && echo "FAIL: transport_http.py still exists" || echo "PASS: transport_http.py deleted"
      test -f /Users/forrest/GitHub/SIF/src/sif/mcp/tools.py && echo "FAIL: tools.py still exists" || echo "PASS: tools.py deleted"
      python -c "from sif.mcp import MCPServer, SearchBackend, ToolHandler; print('PASS: imports work')"
      python -c "from sif.mcp.server import ServerState; print('PASS: ServerState import works')"
      python -c "from sif.cli.commands.mcp import mcp_group; print('PASS: CLI imports work')"
    </automated>
  </verify>
  <done>
    - src/sif/mcp_server/ directory does not exist
    - src/sif/mcp/ contains only: __init__.py, protocol.py, server.py, handlers.py, backend.py, cli.py, transports/__init__.py
    - All imports from sif.mcp.* succeed
    - CLI commands import from new unified package without error
    - MCPServer has state machine (CREATED -> INITIALIZED) and tool registry
    - ToolHandler ABC has async handle signature with backend parameter
  </done>
</task>

<task type="auto">
  <name>Task 2: Add type stubs and verify package integrity</name>
  <files>
    src/sif/mcp/__init__.py
    src/sif/mcp/server.py
    src/sif/mcp/handlers.py
    src/sif/mcp/backend.py
  </files>
  <read_first>
    src/sif/mcp/__init__.py
    src/sif/mcp/server.py
    src/sif/mcp/handlers.py
    src/sif/mcp/backend.py
    src/sif/mcp/protocol.py
  </read_first>
  <action>
    1. Ensure all files have proper `from __future__ import annotations`.
    2. Ensure all files use `list[str] | None` union syntax (Python 3.9+).
    3. Add type annotations to all function signatures (`disallow_untyped_defs = true` per project convention).
    4. In `server.py`, add the `InitializeResult`, `ServerCapabilities`, `ToolsListResult` imports from `protocol.py`.
    5. In `backend.py`, add `from __future__ import annotations` and fix any import issues.
    6. Run `ruff check src/sif/mcp/` to verify zero errors.
    7. Run `python -m py_compile src/sif/mcp/*.py src/sif/mcp/transports/*.py` to verify syntax.
  </action>
  <acceptance_criteria>
    - Every .py file in src/sif/mcp/ has 'from __future__ import annotations' as first import
    - ruff check src/sif/mcp/ reports zero errors
    - python -m py_compile succeeds on all src/sif/mcp/*.py and src/sif/mcp/transports/*.py
    - No mypy untyped-def errors in src/sif/mcp/
  </acceptance_criteria>
  <verify>
    <automated>
      ruff check src/sif/mcp/ && echo "PASS: ruff clean"
      python -m py_compile src/sif/mcp/__init__.py src/sif/mcp/server.py src/sif/mcp/handlers.py src/sif/mcp/backend.py src/sif/mcp/cli.py src/sif/mcp/transports/__init__.py && echo "PASS: syntax valid"
    </automated>
  </verify>
  <done>
    - ruff check src/sif/mcp/ passes with zero errors
    - All Python files compile without syntax errors
    - All type annotations present (no untyped defs)
    - Proper import paths throughout
  </done>
</task>

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Deletion of old files | accept | Old implementations contained no secrets; deletion is cleanup |

## Verification

- `ls src/sif/mcp_server/` returns "No such file or directory"
- `python -c "from sif.mcp import MCPServer, SearchBackend, ToolHandler; print('ok')"` succeeds
- `ruff check src/sif/mcp/` passes

## Success Criteria

- Single unified `src/sif/mcp/` package exists
- `src/sif/mcp_server/` is fully deleted
- All legacy mock/stub files removed
- New async server skeleton with state machine compiles
- ToolHandler ABC has correct async signature
- CLI imports updated to new package

## Output

After completion, create `.planning/phases/09-mcp-server-implementation/09-01-SUMMARY.md`
