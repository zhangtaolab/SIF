---
context: phase
phase: 09-mcp-server-implementation
task: 3
total_tasks: 4
status: in_progress
last_updated: 2026-05-12
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] **CONSTRAINT: MCP 2025-11-25 Streamable HTTP replaces HTTP+SSE** — The new standard uses a single `/mcp` endpoint with POST/GET, not separate `/mcp/v1/sse` + `/mcp/v1/messages`. Old endpoints must NOT be implemented.
- [x] **CONSTRAINT: SQLite DDL triggers implicit commit** — Creating tables inside a transaction that later rolls back will NOT roll back the DDL. Never assert DDL rollback in SQLite tests.
- [x] **CONSTRAINT: stdout must never contain non-JSON output in stdio mode** — All logs must go to stderr only. Any non-JSON stdout will break MCP clients.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Long-lived DB connections | Holding SQLite connection across tool calls causes concurrency issues | advisory | New DatabaseConnection per tool call (D-04) |
| Wildcard CORS defaults | `cors_origins=["*"]` is insecure per MCP spec | advisory | Default to `["http://localhost:3000", "http://127.0.0.1:3000"]` (D-14) |
| Blocking stdout in stdio mode | print() or logging to stdout corrupts JSON-RPC stream | blocking | All logging goes to stderr; stdout is JSON-RPC only |

<current_state>
Phase 9 planning is complete. 6 executable plans have been created, verified, and committed.

- SPEC.md: 8 locked requirements (updated to match Streamable HTTP)
- CONTEXT.md: 14 implementation decisions
- RESEARCH.md: Complete with resolved open questions
- PATTERNS.md: 17 files classified, 14 analogs found
- VALIDATION.md: Nyquist validation strategy created
- 6 PLAN.md files created and committed (hash fdbc2b9)

Ready to execute Wave 1 (09-01).
</current_state>

<completed_work>

Completed Tasks:
- Task 1: spec-phase — wrote 09-SPEC.md with 8 locked requirements (commit a83df34)
- Task 2: discuss-phase — gathered context, 14 decisions, wrote 09-CONTEXT.md + 09-DISCUSSION-LOG.md (commit 694254b)
- Task 3: plan-phase — created 6 executable plans, verified by plan-checker, committed (commit fdbc2b9)
</completed_work>

<remaining_work>

- Task 4: execute-phase — execute all 6 plans across 4 waves
  - Wave 1: 09-01 — Unify dual MCP implementations into single src/sif/mcp/ package
  - Wave 2: 09-02 + 09-03 — SearchBackend + Tool handlers (query, get, multi_get, status)
  - Wave 3: 09-04 + 09-05 — stdio transport + HTTP transport (Streamable HTTP)
  - Wave 4: 09-06 — Integration tests + full quality suite
</remaining_work>

<decisions_made>

- **D-01:** Reuse legacy `mcp/protocol.py` Pydantic models
- **D-02:** Use OOP `ToolHandler` ABC pattern — each tool is a class with name/desc/schema/handle
- **D-03:** Directory structure A (functional layering) — handlers.py + backend.py + transports/
- **D-04:** New `DatabaseConnection` per tool call — safety first, no concurrency issues
- **D-05:** `SearchBackend` methods are `async` — matches async transport semantics
- **D-06:** Cache embedder via `EmbeddingManager` singleton — balances safety with performance
- **D-07:** Full MCP lifecycle (initialize → initialized → tools/list → tools/call)
- **D-08:** Tiered error handling — fatal→exit, recoverable→JSON-RPC error response
- **D-09:** Full notification support — initialized, progress, cancelled
- **D-10:** Streamable HTTP (2025-11-25) — single `/mcp` endpoint, POST/GET, session-aware
- **D-11:** No backwards compatibility with old HTTP+SSE transport
- **D-12:** Default bind to `127.0.0.1`
- **D-13:** Validate `Origin` header, reject invalid with 403
- **D-14:** CORS defaults non-wildcard
- **D-15:** Resource URI: `docs://{doc_id}` with optional `?from_line=N&max_lines=M`
</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)

1. `.planning/phases/09-mcp-server-implementation/09-SPEC.md` — locked requirements and acceptance criteria
2. `.planning/phases/09-mcp-server-implementation/09-CONTEXT.md` — implementation decisions
3. `.planning/phases/09-mcp-server-implementation/09-01-PLAN.md` — next plan to execute (Wave 1)
4. `src/sif/mcp/protocol.py` — Pydantic models to reuse

## Infrastructure State
- SIF CLI installed and working (`sif --version` → 0.1.1)
- Working tree is clean (planning artifacts committed)
- No running servers or background processes
- Model cached: Qwen/Qwen3-Embedding-0.6B at `/Users/forrest/Library/Caches/sif/`

<context>
Phase 9 is at the execution boundary. All design work is complete. The next step is to run /gsd-execute-phase 9 to begin executing Wave 1 (09-01).

Key architectural decisions are locked and verified:
- Reuse protocol.py models
- OOP ToolHandler pattern
- Async SearchBackend with per-call DB connections
- Full MCP lifecycle for stdio
- Streamable HTTP for HTTP transport

Auto-advance is enabled — after planning completed, the system was about to auto-advance to execute-phase when context warning paused work.
</context>

<next_action>
Run `/gsd-execute-phase 9` to begin executing Wave 1 (09-01: Unify dual MCP implementations)
</next_action>
