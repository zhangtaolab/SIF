---
context: phase
phase: 09-mcp-server-implementation
task: 4
total_tasks: 4
status: in_progress
last_updated: 2026-05-12
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [x] **CONSTRAINT: MCP 2025-11-25 Streamable HTTP replaces HTTP+SSE** — The new standard uses a single `/mcp` endpoint with POST/GET, not separate `/mcp/v1/sse` + `/mcp/v1/messages`. Old endpoints must NOT be implemented.
- [x] **CONSTRAINT: SQLite DDL triggers implicit commit** — Creating tables inside a transaction that later rolls back will NOT roll back the DDL. Never assert DDL rollback in SQLite tests.
- [x] **CONSTRAINT: stdout must never contain non-JSON output in stdio mode** — All logs must go to stderr only. Any non-JSON stdout will break MCP clients.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Long-lived DB connections | Holding SQLite connection across tool calls causes concurrency issues | advisory | New DatabaseConnection per tool call (D-04) |
| Wildcard CORS defaults | `cors_origins=["*"]` is insecure per MCP spec | advisory | Default to `["http://localhost:3000", "http://127.0.0.1:3000"]` (D-14) |
| Blocking stdout in stdio mode | print() or logging to stdout corrupts JSON-RPC stream | blocking | All logging goes to stderr; stdout is JSON-RPC only |

<current_state>
Phase 9 Waves 1-3 complete. Wave 4 (integration tests + quality suite) in progress.

- 09-01 (Wave 1): COMPLETE — Unified MCP package structure, deleted mcp_server/
- 09-02 (Wave 2): COMPLETE — SearchBackend with real DB connectivity
- 09-03 (Wave 2): COMPLETE — 4 tool handlers + server dispatch
- 09-04 (Wave 3): COMPLETE — stdio transport with 10 tests
- 09-05 (Wave 3): COMPLETE — HTTP transport with 12 tests

Current MCP test coverage: ~79% (just under 80% target)
- server.py: 26% covered — main gap
- handlers.py: 69% covered
- http.py: 76% covered
- stdio.py: 84% covered
- backend.py: 95% covered

All 409 project tests pass, 11 skipped.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: 09-01 — Unified MCP package (commit 965c095)
- Task 2: 09-02 — SearchBackend implementation (commit e84e372)
- Task 3: 09-03 — Tool handlers + server dispatch (commit e84e372)
- Task 4: 09-04 — stdio transport (commit b935567)
- Task 5: 09-05 — HTTP transport (commit b935567)
- Task 6: Summaries for 09-04, 09-05 (commit b30f299)
</completed_work>

<remaining_work>

- Task 7: 09-06 — Integration tests + full quality suite
  - Create `tests/unit/mcp/test_integration.py` with:
    - TestStdioIntegration: full lifecycle (initialize → tools/list → tools/call)
    - TestStdioIntegration: reject before initialize (error -32002)
    - TestStdioIntegration: notification handling
    - TestHTTPIntegration: full lifecycle via TestClient
    - TestHTTPIntegration: CORS secure defaults
    - TestHTTPIntegration: origin rejection
    - TestHTTPIntegration: SSE stream
  - Verify MCP coverage >= 80%
  - Run full quality suite: ruff check + ruff format + pytest
  - Verify CLI commands: `sif mcp stdio --help`, `sif mcp http --help`
</remaining_work>

<decisions_made>

- **D-01:** Reuse legacy `mcp/protocol.py` Pydantic models
- **D-02:** Use OOP `ToolHandler` ABC pattern
- **D-03:** Directory structure A (functional layering) — handlers.py + backend.py + transports/
- **D-04:** New `DatabaseConnection` per tool call
- **D-05:** `SearchBackend` methods are `async`
- **D-06:** Cache embedder via `EmbeddingManager` singleton
- **D-07:** Full MCP lifecycle (initialize → initialized → tools/list → tools/call)
- **D-08:** Tiered error handling — fatal→exit, recoverable→JSON-RPC error response
- **D-09:** Full notification support
- **D-10:** Streamable HTTP (2025-11-25) — single `/mcp` endpoint, POST/GET, session-aware
- **D-11:** No backwards compatibility with old HTTP+SSE transport
- **D-12:** Default bind to `127.0.0.1`
- **D-13:** Validate `Origin` header, reject invalid with 403
- **D-14:** CORS defaults non-wildcard
- **D-15:** Resource URI: `docs://{doc_id}` with optional `?from_line=N&max_lines=M`
</decisions_made>

<blockers>
- None
</blockers>

## Required Reading (in order)

1. `.planning/phases/09-mcp-server-implementation/09-06-PLAN.md` — remaining plan
2. `.planning/phases/09-mcp-server-implementation/09-04-SUMMARY.md` — stdio transport summary
3. `.planning/phases/09-mcp-server-implementation/09-05-SUMMARY.md` — HTTP transport summary

## Infrastructure State
- SIF CLI installed and working
- Working tree has uncommitted changes (integration tests pending)
- No running servers or background processes
- Model cached: Qwen/Qwen3-Embedding-0.6B

<context>
Phase 9 is at Wave 4 boundary. All transport and handler code is implemented and tested. The next step is to write integration tests to reach 80% MCP coverage, then run the full quality suite.

Key files to work with:
- `src/sif/mcp/server.py` — needs coverage (currently 26%)
- `src/sif/mcp/handlers.py` — needs coverage (currently 69%)
- `tests/unit/mcp/test_integration.py` — to be created
</context>

<next_action>
Create `tests/unit/mcp/test_integration.py` with end-to-end stdio and HTTP integration tests, then run full quality suite.
</next_action>
