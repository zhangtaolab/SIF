---
phase: 09-mcp-server-implementation
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-04", "09-05"]
files_modified:
  - tests/unit/mcp/test_integration.py
  - src/sif/mcp/__init__.py
  - src/sif/mcp/protocol.py
  - src/sif/mcp/server.py
  - src/sif/mcp/handlers.py
  - src/sif/mcp/backend.py
  - src/sif/mcp/transports/stdio.py
  - src/sif/mcp/transports/http.py
  - src/sif/cli/commands/mcp.py
  - pyproject.toml
autonomous: true
requirements:
  - MCP-01
  - MCP-02
  - MCP-03
  - MCP-04
must_haves:
  truths:
    - "pytest tests/unit/mcp/ passes with >=80% coverage of src/sif/mcp/"
    - "ruff check src/sif/mcp/ passes with zero errors"
    - "ruff format --check src tests passes"
    - "Full pytest suite passes (all existing tests still green)"
    - "sif mcp stdio --help works without ImportError"
    - "sif mcp http --help works without ImportError"
    - "Integration test verifies end-to-end stdio lifecycle"
    - "Integration test verifies end-to-end HTTP /mcp endpoint"
  artifacts:
    - path: "tests/unit/mcp/test_integration.py"
      provides: "End-to-end integration tests"
    - path: "tests/unit/mcp/"
      provides: "Complete MCP test suite"
  key_links:
    - from: "test_integration.py"
      to: "src/sif/mcp/"
      via: "imports and exercises all MCP components"
---

## Objective

Write integration tests for the unified MCP implementation and run the full quality suite. Verify end-to-end stdio and HTTP flows, ensure all tests pass, and confirm the implementation meets coverage and lint requirements.

Purpose: Validate the complete MCP server implementation and ensure no regressions.
Output: Passing integration tests and green quality suite.

## Execution Context

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## Context

@.planning/phases/09-mcp-server-implementation/09-CONTEXT.md
@.planning/phases/09-mcp-server-implementation/09-RESEARCH.md
@.planning/phases/09-mcp-server-implementation/09-SPEC.md
@.planning/phases/09-mcp-server-implementation/09-01-SUMMARY.md
@.planning/phases/09-mcp-server-implementation/09-02-SUMMARY.md
@.planning/phases/09-mcp-server-implementation/09-03-SUMMARY.md
@.planning/phases/09-mcp-server-implementation/09-04-SUMMARY.md
@.planning/phases/09-mcp-server-implementation/09-05-SUMMARY.md

### Interfaces

From `src/sif/mcp/transports/stdio.py`:
```python
class StdioTransport:
    def __init__(self, server: MCPServer, stdin: TextIO | None = None, stdout: TextIO | None = None) -> None
    async def run(self) -> None

def run_stdio_server_sync(db_path: str) -> None
```

From `src/sif/mcp/transports/http.py`:
```python
def create_app(server: MCPServer, cors_origins: list[str] | None = None) -> FastAPI
def run_http_server(db_path: str, host: str = "127.0.0.1", port: int = 3000, cors_origins: list[str] | None = None) -> None
```

From `src/sif/mcp/server.py`:
```python
class MCPServer:
    def __init__(self, backend: SearchBackend) -> None
    def register_tools(self, tools: list[ToolHandler]) -> None
    async def handle_request(self, request: JsonRpcRequest) -> JsonRpcResponse
```

## Tasks

<task type="auto">
  <name>Task 1: Write integration tests for end-to-end MCP flows</name>
  <files>
    tests/unit/mcp/test_integration.py
  </files>
  <read_first>
    src/sif/mcp/transports/stdio.py
    src/sif/mcp/transports/http.py
    src/sif/mcp/server.py
    src/sif/mcp/protocol.py
    src/sif/mcp/handlers.py
    src/sif/mcp/backend.py
    tests/unit/mcp/test_server.py
    tests/unit/mcp/test_transports_http.py
  </read_first>
  <action>
    1. Create `tests/unit/mcp/test_integration.py` with two integration test classes:

    **TestStdioIntegration:**
    - `test_stdio_full_lifecycle`: Create StringIO stdin with sequence of messages:
      1. {"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05"}}
      2. {"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}
      3. {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"status","arguments":{}}}
      4. EOF (empty StringIO after messages)
      - Create StringIO stdout, mock backend with AsyncMock methods
      - Create MCPServer, register default tools, create StdioTransport, run it
      - Read all lines from stdout, parse each as JSON
      - Verify: initialize response has result.protocolVersion == "2024-11-05"
      - Verify: tools/list response has result.tools with 4 items
      - Verify: tools/call response has result.content array
      - Verify: no error responses
    - `test_stdio_reject_before_initialize`: Send tools/list before initialize, verify error code -32002 (SERVER_NOT_INITIALIZED)
    - `test_stdio_handles_notification`: Send notifications/initialized, verify no response written (no id)

    **TestHTTPIntegration:**
    - `test_http_full_lifecycle`: Use TestClient with create_app:
      1. POST /mcp with initialize request, verify 200
      2. POST /mcp with tools/list, verify 200 and 4 tools
      3. POST /mcp with tools/call for "status", verify 200 and content
    - `test_http_cors_secure_by_default`: POST without --cors-origins (use default app), verify Access-Control-Allow-Origin is http://localhost:3000, not "*"
    - `test_http_origin_rejection`: POST with Origin: http://evil.com, verify 403
    - `test_http_sse_stream`: GET /mcp, read first event, verify contains data: {"ping": true}

    2. All tests use pytest.mark.asyncio where needed.
    3. Use MagicMock/AsyncMock for SearchBackend to avoid requiring real database.
    4. Use StringIO for stdio tests, TestClient for HTTP tests.
  </action>
  <acceptance_criteria>
    - tests/unit/mcp/test_integration.py has >= 6 tests
    - TestStdioIntegration.test_stdio_full_lifecycle verifies initialize -> tools/list -> tools/call sequence
    - TestStdioIntegration.test_stdio_reject_before_initialize verifies error code -32002
    - TestHTTPIntegration.test_http_full_lifecycle verifies POST initialize, tools/list, tools/call
    - TestHTTPIntegration.test_http_cors_secure_by_default verifies no wildcard in CORS
    - All integration tests use mocked SearchBackend (no real DB required)
  </acceptance_criteria>
  <verify>
    <automated>
      pytest tests/unit/mcp/test_integration.py -x && echo "PASS: integration tests"
    </automated>
  </verify>
  <done>
    - tests/unit/mcp/test_integration.py has >= 6 integration tests
    - Stdio integration covers full lifecycle: initialize -> tools/list -> tools/call
    - HTTP integration covers full lifecycle via TestClient
    - CORS security tested (default origins, rejection)
    - SSE stream tested
    - All tests pass without real database
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full MCP test suite with coverage and fix any issues</name>
  <files>
    tests/unit/mcp/
    src/sif/mcp/
  </files>
  <read_first>
    tests/unit/mcp/test_backend.py
    tests/unit/mcp/test_handlers.py
    tests/unit/mcp/test_server.py
    tests/unit/mcp/test_transports_stdio.py
    tests/unit/mcp/test_transports_http.py
    tests/unit/mcp/test_integration.py
  </read_first>
  <action>
    1. Run the full MCP test suite: `pytest tests/unit/mcp/ -x --cov=src/sif/mcp --cov-report=term-missing`
    2. If coverage is below 80%, identify uncovered lines and add tests.
    3. Fix any failing tests:
       - Check for import errors
       - Check for async/await mismatches
       - Check for mock configuration issues
       - Check for Pydantic validation errors
    4. Run `ruff check src/sif/mcp/` and fix any lint errors.
    5. Run `ruff format --check src tests` and fix any formatting issues.
    6. Common issues to watch for:
       - Missing `from __future__ import annotations` in test files
       - Wrong mock patch targets (use full module paths)
       - AsyncMock vs MagicMock confusion
       - pytest-asyncio not configured (check pyproject.toml for asyncio_mode)
  </action>
  <acceptance_criteria>
    - pytest tests/unit/mcp/ passes with >= 80% line coverage of src/sif/mcp/
    - ruff check src/sif/mcp/ passes with zero errors
    - ruff format --check src tests passes
    - No async tests missing @pytest.mark.asyncio decorator
  </acceptance_criteria>
  <verify>
    <automated>
      pytest tests/unit/mcp/ --cov=src/sif/mcp --cov-report=term-missing && echo "PASS: MCP tests with coverage"
      ruff check src/sif/mcp/ && echo "PASS: ruff check"
      ruff format --check src tests && echo "PASS: ruff format"
    </automated>
  </verify>
  <done>
    - pytest tests/unit/mcp/ passes with >=80% coverage of src/sif/mcp/
    - ruff check src/sif/mcp/ passes with zero errors
    - ruff format --check src tests passes
    - All test files use proper async decorators
    - No ImportError or ModuleNotFoundError
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full project quality suite and verify CLI commands</name>
  <files>
    src/sif/cli/commands/mcp.py
    pyproject.toml
  </files>
  <read_first>
    src/sif/cli/commands/mcp.py
    pyproject.toml
  </read_first>
  <action>
    1. Run full test suite: `pytest -x`
    2. If any existing tests fail due to MCP changes, fix them:
       - Old imports from sif.mcp_server may need updating in other test files
       - Old imports from sif.mcp.server or sif.mcp.server_http may need updating
       - Check for any tests that import deleted modules
    3. Run `ruff check src tests` and fix all errors.
    4. Run `ruff format --check src tests` and fix all formatting.
    5. Run `mypy src/sif` and fix type errors (focus on MCP-related ones).
    6. Verify CLI commands work:
       - `python -m sif.cli.main mcp stdio --help` should show help without ImportError
       - `python -m sif.cli.main mcp http --help` should show help without ImportError
       - `sif mcp stdio --help` should work (if sif is installed)
    7. Check pyproject.toml for any MCP-related entry points or scripts that need updating.
  </action>
  <acceptance_criteria>
    - pytest -x passes with zero failures across entire project
    - ruff check src tests passes with zero errors
    - ruff format --check src tests passes
    - python -m sif.cli.main mcp stdio --help exits with code 0
    - python -m sif.cli.main mcp http --help exits with code 0
    - No grep matches for deleted modules (sif.mcp_server, sif.mcp.server_http, sif.mcp.transport_stdio)
  </acceptance_criteria>
  <verify>
    <automated>
      pytest -x && echo "PASS: full test suite"
      ruff check src tests && echo "PASS: ruff check all"
      ruff format --check src tests && echo "PASS: ruff format all"
      python -m sif.cli.main mcp stdio --help >/dev/null 2>&1 && echo "PASS: stdio help works" || echo "FAIL: stdio help failed"
      python -m sif.cli.main mcp http --help >/dev/null 2>&1 && echo "PASS: http help works" || echo "FAIL: http help failed"
    </automated>
  </verify>
  <done>
    - Full pytest suite passes with zero failures
    - ruff check src tests passes with zero errors
    - ruff format --check src tests passes
    - sif mcp stdio --help works without ImportError
    - sif mcp http --help works without ImportError
    - No references to deleted modules (sif.mcp_server, sif.mcp.server_http, etc.)
    - pyproject.toml entry points are correct
  </done>
</task>

## Threat Model

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Information Disclosure | Test mocks leak data | accept | Test environment only; no production impact |
| T-09-14 | Denial of Service | Test suite hangs on async tests | mitigate | pytest-asyncio with timeout; all async tests properly decorated |

## Verification

- `pytest tests/unit/mcp/ --cov=src/sif/mcp --cov-report=term-missing` shows >=80% coverage
- `pytest -x` passes (full suite)
- `ruff check src tests` passes
- `ruff format --check src tests` passes
- `python -m sif.cli.main mcp stdio --help` works

## Success Criteria

- Integration tests cover end-to-end stdio and HTTP flows
- MCP test suite has >=80% coverage
- Full project test suite passes
- All lint and format checks pass
- CLI commands work without ImportError
- No references to deleted legacy modules

## Output

After completion, create `.planning/phases/09-mcp-server-implementation/09-06-SUMMARY.md`
