---
context: phase
phase: 04-advanced-search-pipeline
task: 1
total_tasks: 2
status: paused
last_updated: 2026-04-17T15:39:48.043Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: Subagent worktrees fail when Bash requires explicit approval** — Executor subagents spawned via `Task` with `isolation="worktree"` cannot run `git commit` or `pytest` because the Bash tool requires user approval on each invocation. When the agent tries to commit or verify, it gets blocked waiting for permission that never comes. — **Structural mitigation required**: Execute plans inline (sequentially, without subagents) using direct tool calls. Skip worktree mode entirely for this project.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree execution with unapproved Bash | Agent spawns in worktree but cannot commit/verify because Bash calls require user approval | blocking | Always execute plans inline (direct tool calls) for this project. Verify with `pytest` before committing. |

## Current State

Phase 04 — Advanced Search Pipeline. Wave 2 (Plan 04-03) is COMPLETE and merged to main. Ready to begin Wave 3.

## Completed Work

**Plan 04-01** (3 commits):
- `32bdfc1`: Extended `SearchResult` (scores dict, snippet field) and `SearchOptions` (explain, candidate_limit, intent, snippet_max_length)
- `e474c53`: Added reranker settings to Settings (default model_type='gguf'); updated RRF to preserve bm25_score, vector_score, rrf_score
- `6a782d8`: Implemented `LlamaCppReranker` (primary) and `CrossEncoderReranker` (fallback) with `create_reranker()` factory

**Plan 04-02** (2 commits):
- `4acf4d3`: Implemented `QueryExpansion` with embedding-based PRF returning `list[str]`
- `6473394`: Implemented `SmartSnippetExtractor` with sentence scoring and window extraction

**Plan 04-03** (5 commits):
- `56cb05b`: Rewrote `SearchPipeline` with prefix routing (`lex:`, `vec:`, `hyde:`, `expand:`), explainability, candidate capping, intent propagation
- `9d4874d`: Updated CLI `query_cmd` with `--explain`, `-C/--candidate-limit` (IntRange 1-200), `--intent`; `vsearch_cmd` with `--min-score`, `--full`
- `ca6df8d`: Fixed `test_hybrid.py` and `test_search_pipeline.py` imports; added SearchPipeline tests
- `aaa29ae`: Fixed CLI test mocks; added tests for new flags
- `dcc4b2d`: SUMMARY.md

## Remaining Work

**Wave 3 — Plan 04-04** (2 tasks):
- Task 1: Implement `SearchEvaluator` with precision@k, recall, MRR metrics
- Task 2: Implement `bench` CLI command with fixture loading and output formatting

**Wave 3 — Plan 04-05** (4 tasks):
- Task 1: Fix `test_bm25.py` broken imports
- Task 2: Fix `test_reranker.py` and `test_query_expander.py` remaining issues
- Task 3: Run pytest on all search/inference/cli tests
- Task 4: Run ruff check/format

## Decisions Made

- **Subagent worktrees are BLOCKED** — Execute all remaining plans inline (no worktrees, no subagents)

## Blockers

- None (wave 2 complete, wave 3 ready to start)

## Required Reading (in order)

1. `.planning/phases/04-advanced-search-pipeline/04-04-PLAN.md` — Next plan to execute
2. `.planning/phases/04-advanced-search-pipeline/04-05-PLAN.md` — Wave 3 plan 2 (test fixes)
3. `./CLAUDE.md` — Project coding conventions

## Infrastructure State

- Python 3.13.9 venv active
- sentence-transformers NOT installed (optional extra)
- llama-cpp-python NOT installed (optional extra)

## Next Action

Start with: Read `04-04-PLAN.md` and execute Task 1 (implement SearchEvaluator). Run inline — do NOT spawn subagents.
