---
phase: 04-advanced-search-pipeline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sif/search/hybrid.py
  - src/sif/cli/commands/search.py
  - tests/unit/search/test_hybrid.py
  - tests/unit/cli/test_search.py
  - tests/integration/test_search_pipeline.py
autonomous: true
gap_closure: true
gap_ids: [SC7-SRCH-07]
requirements: [SRCH-07]
estimate:
  tokens: 33000
  raw_tokens: 33000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Default `sif search query <terms>` on an indexed collection renders a Snippet column in the rich table showing the extracted snippet for matching results, with no --full flag passed (SC 7 / SRCH-07 display half; gap missing item 1)"
    - "SearchPipeline.search populates SearchResult.snippet on the default hybrid route when include_content is False, by fetching document content transiently for extraction (gap missing item 2; 04-VERIFICATION.md E2E smoke showed snippet=None without content)"
    - "The lex:, vec:, and hyde: prefix routes apply snippet extraction before returning when a snippet extractor is configured (gap missing item 3; 04-VERIFICATION.md E2E smoke showed snippet=None on lex: with content because the routes returned before the snippet stage)"
    - "The search_cmd (BM25) rich table shows a Snippet column that falls back to the first BM25 highlight when no snippet is present (gap missing item 1 fallback clause)"
    - "Real-sqlite integration, pipeline unit, and CLI unit tests assert snippet extraction and display end to end (gap missing item 4; no test currently asserts the snippet flow)"
    - statement: "On the maintainer's real personal index, the rendered snippet is judged to be the most relevant excerpt for its query — relevance is a human judgment; automated tests prove extraction mechanics (term-frequency window selection) only"
      verification: backstop
  artifacts:
    - "src/sif/search/hybrid.py defines SearchPipeline._apply_snippets(results, parsed_query) and routes the BM25/VECTOR/HYDE early returns plus the default-route tail through it; the snippet stage no longer requires result.content to be pre-populated"
    - "src/sif/cli/commands/search.py defines a module-level _display_snippet(r, max_len) helper and adds a default Snippet column to the query_cmd and search_cmd rich tables"
    - "tests/integration/test_search_pipeline.py gains a real-sqlite test class proving snippet extraction on the default route with include_content False, mirroring the 04-VERIFICATION.md E2E smoke"
    - "tests/unit/search/test_hybrid.py gains per-route snippet tests; tests/unit/cli/test_search.py gains a TestSnippetDisplay class covering rendering and the highlights fallback"
  key_links:
    - "SearchPipeline._apply_snippets → HybridSearcher._get_document_content(document_id) → SmartSnippetExtractor.extract(text, query_terms) → SearchResult.snippet — the content feed that was missing when --full was not passed"
    - "query_cmd and search_cmd rich tables → _display_snippet(r) → r.snippet else r.highlights[0] — the render target that never existed (r.snippet was serialized only by to_dict() into --json)"
    - "lex:/vec:/hyde: early-return routes → _apply_snippets before return — the routes currently return at hybrid.py:209-221, before the snippet stage at hybrid.py:267-272"
  prohibitions:
    - statement: "MUST NOT assign the fetched document text into SearchResult.content when include_content is False — the fetch feeds snippet extraction only; --full stays the only path that returns full document content, so the --json output for default searches is unchanged (CLI-07 contract preserved)"
      status: unverified
      flagged: true
    - statement: "MUST NOT interpret snippet or highlight text as Rich console markup — bracket sequences inside document text must render literally in Snippet cells, not as console markup tags"
      status: unverified
      flagged: true
    - statement: "MUST NOT alter the --files, --quiet, --json, --csv, --md, --xml output branches, nor the routing, scoring, or dedup behavior of any search route — the Snippet column is a rich-table addition only (04-REVIEW CR-03 vec: dedup stays advisory and out of gap scope)"
      status: unverified
      flagged: true
---

<objective>
Close the single Phase 04 verification gap (SC 7 / SRCH-07, 04-VERIFICATION.md status gaps_found,
7/8 truths): "Search results show the most relevant snippet extracted from each chunk."

The extraction engine is done — SmartSnippetExtractor is implemented, unit-tested (10 tests), and wired
into SearchPipeline's default hybrid path. The last mile is missing, per the gap's three artifacts:

1. src/sif/cli/commands/search.py: r.snippet is rendered nowhere in human-facing output — the rich
   tables in query_cmd (lines 553-575) and search_cmd (lines 208-230) have no Snippet column; the field
   only reaches users via SearchResult.to_dict() into --json.
2. src/sif/search/hybrid.py: the snippet loop (lines 267-272) requires result.content, which is only
   fetched when include_content/--full is set — so default searches never extract a snippet.
3. src/sif/search/hybrid.py: the lex:/vec:/hyde: routes return early (lines 209-221) before the snippet
   stage — empirically, lex: with content returned snippet=None.

Corroborated independently by 04-UI-REVIEW finding 3 ("snippets are never shown", "SRCH-07 is invisible
to table users").

Purpose: make SC 7 true end to end so Phase 04 can close without an overrides entry.
Output: pipeline content-feed fix, prefix-route snippet coverage, Snippet column in query/search tables,
and pipeline + CLI + real-sqlite integration tests asserting the flow.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-advanced-search-pipeline/04-VERIFICATION.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end snippet on the default query path — pipeline content feed plus CLI Snippet column, no --full required</name>
  <files>src/sif/search/hybrid.py, src/sif/cli/commands/search.py, tests/integration/test_search_pipeline.py</files>
  <read_first>
    - src/sif/search/hybrid.py (SearchPipeline.search lines 183-274; HybridSearcher._get_document_content line 148; the snippet loop at 267-272 being replaced)
    - src/sif/cli/commands/search.py (query_cmd table construction lines 553-575; truncation constants _TITLE_MAX_LEN/_CONTENT_MAX_LEN lines 22-24; existing import block)
    - src/sif/search/snippets.py (SmartSnippetExtractor.extract(text, query_terms) contract)
    - src/sif/core/models.py (SearchResult.snippet/content/highlights fields, lines 207-237; SearchOptions.include_content/snippet_max_length, lines 240-254)
    - tests/integration/test_embed_idempotency.py (the proven real-sqlite seeding pattern: Database(path) + init_schema + CollectionRepository/DocumentRepository inside db.connection)
    - tests/unit/cli/test_search.py (TestQueryNewFlags._make_pipeline_mock and the patch scaffold for Database/CollectionRepository/SearchPipeline/EmbeddingManager.from_settings/get_settings; the table-assertion pattern "Content" in result.output at test_query_line_numbers_flag)
    - tests/integration/test_search_pipeline.py (existing classes; note none construct a snippet extractor)
    - CLAUDE.md (code style: 3.10+ unions, line length 100, strict annotations)
  </read_first>
  <behavior>
    - RED first. Add to tests/integration/test_search_pipeline.py a new class (e.g. TestSnippetExtractionIntegration) that seeds a real temp sqlite database with one collection and one multi-sentence document containing a distinctive term (pattern: "Python decorators wrap functions. Decorators use the @ syntax. ..."), constructs SearchPipeline(db.connection, snippet_extractor=SmartSnippetExtractor()) with no embedder, and asserts after pipeline.search over the distinctive term with SearchOptions(limit=5) (include_content defaults False): results[0].snippet is not None, the snippet string contains the query term, and results[0].content is None (transient fetch, CLI-07 semantics preserved).
    - Same fixture with include_content=True: snippet also set and content populated (current behavior preserved).
    - CLI: new test in tests/unit/cli/test_search.py asserting the default query_cmd table output contains the literal header "Snippet" and a distinctive snippet substring from a mocked pipeline result (mock result carries snippet="unique snippet marker text").
  </behavior>
  <action>
    Pipeline half (src/sif/search/hybrid.py): extract the snippet loop at lines 267-272 into a new private method _apply_snippets(self, results: list[SearchResult], parsed_query: str) -> list[SearchResult] and call it from the default-route tail (before the final self.hybrid._attach_contexts(results) return, preserving current ordering after the rerank stage). Inside the method: return results unchanged when self.snippet_extractor is None; for each result whose snippet is already set, skip; otherwise obtain the extraction text as result.content when truthy, else the return value of self.hybrid._get_document_content(result.document_id) (existing method at hybrid.py:148) — the fetch is transient, the fetched value goes only into a local variable feeding self.snippet_extractor.extract(text, parsed_query.lower().split()) which is assigned into result.snippet when the text is truthy. Per gap missing item 2 this removes the include_content dependency; per must_haves.prohibitions entry 1 the fetched value is never written into result.content. Keep the # noqa: SLF001 convention already used for _attach_contexts access.
    CLI half (src/sif/cli/commands/search.py): add a module-level helper _display_snippet(r, max_len: int = _CONTENT_MAX_LEN) -> str that returns r.snippet truncated to max_len with a trailing "..." when longer; when snippet is absent or empty, returns the first entry of r.highlights with the same truncation; returns the empty string when neither is available (guard highlights with getattr for dict-shaped rows). Import SearchResult from sif.core.models alongside the existing SearchOptions import for the annotation. In query_cmd's rich table (lines 553-575), add table.add_column("Snippet", style="white") after the Collection column, and in the row loop append the snippet cell before the conditional --line-numbers Content cell so column order matches row order. Wrap the cell value with rich.markup.escape (from rich.markup import escape) so bracket sequences inside document-derived text render literally per must_haves.prohibitions entry 2. Apply escaping only to the new Snippet cell; existing title/content cells predate this phase and stay as they are.
    Leave every other output branch of query_cmd untouched: --files, --quiet, --json, --csv, --md, --xml, and the --explain footer.
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/integration/test_search_pipeline.py -k snippet -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output, or the new snippet tests appear in a failure/summary line with counts other than all-passed</fails_when>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/cli/test_search.py -k snippet -x</automated>
    <fails_when>non-zero exit, or "no tests ran" in the output</fails_when>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/search/test_hybrid.py tests/unit/cli/test_search.py tests/integration/test_search_pipeline.py -x</automated>
    <fails_when>non-zero exit, or a pytest summary line reporting any failed or errored test</fails_when>
    <automated>grep -q "_apply_snippets" src/sif/search/hybrid.py &amp;&amp; grep -q "_display_snippet" src/sif/cli/commands/search.py</automated>
    <fails_when>non-zero exit (either symbol absent from its file)</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/integration/test_search_pipeline.py contains an assertion sequence matching "snippet is not None" inside a real-sqlite test that passes SearchOptions without include_content
    - src/sif/search/hybrid.py contains the symbol _apply_snippets and references _get_document_content inside it; the include_content guard no longer gates snippet extraction
    - src/sif/cli/commands/search.py contains the literal "Snippet" as an added table column in query_cmd and the symbol _display_snippet, and imports escape from rich.markup
    - env -u FORCE_COLOR NO_COLOR=1 pytest tests/integration/test_search_pipeline.py -k snippet -x exits 0
    - env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/cli/test_search.py -k snippet -x exits 0 with the Snippet header assertion passing
    - In the real-sqlite test with include_content False: results[0].content is None while results[0].snippet is a non-empty string (transient-fetch contract)
  </acceptance_criteria>
  <done>The default hybrid query path extracts and displays a snippet end to end without --full, proven against a real sqlite index and in the CLI table, and committed.</done>
  <reversibility rating="reversible">Additive pipeline helper plus one table column; both trivially removable.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prefix-route snippet coverage — lex:/vec:/hyde: run the snippet stage before returning</name>
  <files>src/sif/search/hybrid.py, tests/unit/search/test_hybrid.py</files>
  <read_first>
    - src/sif/search/hybrid.py (SearchPipeline.search early-return routes lines 209-221; _apply_snippets from Task 1)
    - tests/unit/search/test_hybrid.py (TestSearchPipelinePrefixRouting fixtures and the MagicMock patterns at test_pipeline_prefix_lex_routes_to_bm25, test_pipeline_prefix_vec_raises_without_embedder, test_pipeline_prefix_hyde_raises_without_generate)
    - src/sif/core/models.py (SearchResult fields)
  </read_first>
  <behavior>
    - lex: route with a snippet extractor configured: the BM25-returned result comes back with snippet populated from fetched content, and content stays None when include_content is False.
    - vec: route with embedder plus vector mock: same assertion.
    - hyde: route with an embedder whose generate() returns a short document: same assertion.
    - A result that already carries a snippet passes through without re-invoking the extractor (idempotent skip).
    - A pipeline constructed with no snippet extractor leaves all three routes byte-behaviorally unchanged (stage is a no-op).
  </behavior>
  <action>
    First add the route tests above to tests/unit/search/test_hybrid.py (new class, e.g. TestPipelineSnippetRoutes): stub pipeline.hybrid.bm25.search / pipeline.hybrid.vector.search to return SearchResult objects with content None, and stub the content feed by assigning pipeline.hybrid._get_document_content = MagicMock(return_value=<multi-sentence text containing a query term>) so no real database is needed; for hyde:, give the mock embedder a generate method returning a sentence. Run to see them fail.
    Then change the three early returns in SearchPipeline.search (SearchType.BM25, SearchType.VECTOR, SearchType.HYDE at hybrid.py lines 209-221): assign each route's searcher output to a local results variable and return it via self._apply_snippets(results, parsed_query) instead of returning the searcher output directly. Keep everything else about these routes exactly as-is — routing targets, parsed_query handling, RuntimeError guards, returned ordering — and keep the routes free of context attachment and dedup changes (04-REVIEW CR-03 remains advisory, out of gap scope per must_haves.prohibitions entry 3).
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/search/test_hybrid.py -x</automated>
    <fails_when>non-zero exit, or a pytest summary line reporting any failed or errored test</fails_when>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/search/ tests/integration/test_search_pipeline.py -x</automated>
    <fails_when>non-zero exit, or a pytest summary line reporting any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/unit/search/test_hybrid.py contains snippet assertions for each of the three prefixes lex:, vec:, hyde: (grep -c "snippet" on the file grows by at least the new test count)
    - Each new route test asserts the returned result has snippet set from the stubbed _get_document_content text and content None when include_content is False
    - The three early-return branches in src/sif/search/hybrid.py route through _apply_snippets (source assertion: the return statements for SearchType.BM25/VECTOR/HYDE no longer return searcher output directly)
    - env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/search/test_hybrid.py -x exits 0
  </acceptance_criteria>
  <done>All three prefix modes return snippet-bearing results when an extractor is configured, with unit tests proving each route; default and no-extractor paths unchanged.</done>
  <reversibility rating="reversible">Early returns re-routed through an additive helper; revert restores prior behavior.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: search_cmd Snippet column, highlights fallback tests, and full quality gate</name>
  <files>src/sif/cli/commands/search.py, tests/unit/cli/test_search.py</files>
  <read_first>
    - src/sif/cli/commands/search.py (search_cmd table construction lines 208-230; _display_snippet from Task 1; the output branch order at lines 163-235)
    - tests/unit/cli/test_search.py (the search_cmd test scaffold — patch targets sif.cli.commands.search.Database and sif.cli.commands.search.BM25Searcher; TestQueryNewFlags for query_cmd mocks; _make_search_result helper)
    - CLAUDE.md (post-plan full quality suite requirement)
  </read_first>
  <behavior>
    - query_cmd table with a mocked result whose snippet is None and highlights non-empty: output shows the first highlight text (fallback path of _display_snippet).
    - search_cmd (BM25) table with a mocked result carrying highlights only: output contains the literal header "Snippet" and the highlight text.
    - query_cmd --files with a mocked result: output shows the result path and is free of table headers (the path-only branch keeps its shape).
    - Full quality suite passes: ruff check, ruff format --check, pytest.
  </behavior>
  <action>
    First add a TestSnippetDisplay class to tests/unit/cli/test_search.py with the three CLI behaviors above, following the existing patch scaffold (for search_cmd patch sif.cli.commands.search.BM25Searcher; for query_cmd patch sif.cli.commands.search.SearchPipeline; mock results built like _make_pipeline_mock with explicit snippet and highlights fields; for the --files case assert the path string is present and assert against the absence of table markers using a wording the test chooses, e.g. checking the output has no "Score" header).
    Then mirror the query_cmd change in search_cmd's rich table (lines 208-230): table.add_column("Snippet", style="white") after the Collection column, and in the row loop append escape(_display_snippet(r)) before the conditional --line-numbers Content cell so column order matches row order. Reuse the existing _display_snippet helper — search_cmd results carry BM25 highlights (include_highlights is already True in its SearchOptions) so the fallback clause of gap missing item 1 is what renders here.
    Leave the --files, --quiet, --json, --csv, --md, --xml branches of search_cmd untouched, and make no changes to vsearch_cmd (the gap's stated scope is the query/search tables; vsearch has no pipeline snippet path).
    Finish by running the full quality suite per CLAUDE.md.
  </action>
  <verify>
    <automated>env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/cli/test_search.py -x</automated>
    <fails_when>non-zero exit, or a pytest summary line reporting any failed or errored test</fails_when>
    <automated>ruff check src tests &amp;&amp; ruff format --check src tests &amp;&amp; env -u FORCE_COLOR NO_COLOR=1 pytest</automated>
    <fails_when>any of the three commands exits non-zero, or the pytest summary reports any failed, errored, or unexpectedly-skipped count change beyond the documented 11 pre-existing skips</fails_when>
  </verify>
  <acceptance_criteria>
    - tests/unit/cli/test_search.py contains a TestSnippetDisplay class with a query fallback test, a search_cmd column test, and a --files shape test
    - src/sif/cli/commands/search.py search_cmd table adds the "Snippet" column and its row loop calls _display_snippet (second call site after query_cmd)
    - ruff check src tests exits 0 and ruff format --check src tests exits 0
    - env -u FORCE_COLOR NO_COLOR=1 pytest exits 0 with no new failures or skips versus the pre-plan baseline (554 passed, 11 skipped per STATE.md)
  </acceptance_criteria>
  <done>Both human-facing rich tables show snippets with a highlights fallback, output modes are untouched, and the CLAUDE.md quality gate is green.</done>
  <reversibility rating="reversible">One mirrored table-column change plus tests.</reversibility>
</task>

</tasks>

## Flagged Assumptions (spec-less edge probe — unresolved, surfaced per #1110)

The deterministic edge probe over this phase's 10 requirement IDs (SRCH-01..08, CLI-06, CLI-07) returned
`unclassified` for every applicable row (13 rows, 0 resolved). Per the spec-less fallback protocol these
stay unresolved and are surfaced as flagged assumptions — never auto-resolved, never silently dropped,
and not re-planned wholesale here: 04-VERIFICATION.md already carries verified evidence for 7/8
roadmapped success criteria; the only replanned truth is SC 7 / SRCH-07.

- SRCH-01 (reranker): quality-of-ranking edges assumed covered by existing tests + human verification item 1.
- SRCH-02 (query expansion): expansion-quality edges assumed covered by existing tests.
- SRCH-03 (prefix syntax): routing edges assumed covered; CR-03 duplicate-document advisory remains open.
- SRCH-04 (explain traces): WR-09 score-provenance advisory remains open.
- SRCH-05 (candidate limit): assumed covered.
- SRCH-06 (intent): WR-03 literal-prepend design remains the accepted design.
- SRCH-07 (snippets): the one gap this plan closes; backstop truth above carries the human relevance judgment.
- SRCH-08 (bench): WR-07 dead flag and WR-08 ignored fixture field remain advisory.
- CLI-06 / CLI-07 (min-score, full): assumed covered; this plan explicitly preserves the CLI-07 --full contract.

## Artifacts this phase produces

New symbols created by this plan (gap closure 04-06):

- `SearchPipeline._apply_snippets(results, parsed_query)` — private method, src/sif/search/hybrid.py
- `_display_snippet(r, max_len)` — module-level function, src/sif/cli/commands/search.py
- `escape` import from `rich.markup` in src/sif/cli/commands/search.py (used on Snippet cells)
- `Snippet` column — default rich-table column added to query_cmd and search_cmd (UI element, no CLI flag introduced)
- `TestSnippetExtractionIntegration` (name exemplary) — real-sqlite test class, tests/integration/test_search_pipeline.py
- `TestPipelineSnippetRoutes` (name exemplary) — test class, tests/unit/search/test_hybrid.py
- `TestSnippetDisplay` (name exemplary) — test class, tests/unit/cli/test_search.py

No new CLI flags, no Settings fields, no schema changes, no new files outside tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local SQLite index → CLI terminal | Document text stored in the user's local index crosses into terminal rendering via the new Snippet cells |
| document text → rich table cell | Arbitrary bracket sequences in note content reach Table.add_row, which interprets console markup in plain strings by default |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure | query/search rich tables | low | accept | Snippet cells surface up to ~200 chars of the user's own local documents in the user's own terminal — that is SC 7 working as designed; --json already serializes the full snippet today |
| T-04-06-02 | Tampering | _display_snippet → Table.add_row | low | mitigate | Wrap Snippet cells in rich.markup.escape so document-derived bracket sequences render literally instead of being parsed as console markup; pre-existing title/content cells are out of gap scope and unchanged |
| T-04-06-03 | DoS | _apply_snippets content fetch | low | accept | One SELECT per snippet-less result, bounded by options.limit (default 10); results that already carry snippets are skipped; local personal-scale indexes make this negligible |
</threat_model>

<verification>
- Targeted: env -u FORCE_COLOR NO_COLOR=1 pytest tests/unit/search/test_hybrid.py tests/unit/cli/test_search.py tests/integration/test_search_pipeline.py -x (fails when any test fails)
- Full gate (CLAUDE.md): ruff check src tests && ruff format --check src tests && env -u FORCE_COLOR NO_COLOR=1 pytest — must exit 0 with the 554-passed/11-skipped baseline plus the new tests
- Environment note from 04-VERIFICATION.md: this host family exports FORCE_COLOR=3, which Rich honors over NO_COLOR; every plain-text CLI assertion above must run with FORCE_COLOR unset (hence the env -u FORCE_COLOR prefix) — treat a bare pytest run that fails only the new output assertions under FORCE_COLOR as an environment artifact, not a code regression
- Gap-specific evidence inversion of the two failed E2E smokes: default hybrid without --full now returns a non-None snippet, and lex: with content now returns a non-None snippet
- Human verification (unchanged from 04-VERIFICATION.md, plus the backstop truth): on a real personal index, confirm the rendered snippet reads as the most relevant excerpt
</verification>

<success_criteria>
- SC 7 holds end to end: extraction (all four routes, with or without --full) and display (query and search rich tables, JSON unchanged) are both delivered and test-covered
- The two empirical failures recorded in 04-VERIFICATION.md (hybrid-without-content snippet=None; lex:-with-content snippet=None) are inverted by automated tests
- 04-VERIFICATION.md can be re-run by /gsd-execute-phase 04 --gaps-only and score 8/8 with no overrides entry needed for SC 7
</success_criteria>

<output>
Create `.planning/phases/04-advanced-search-pipeline/04-06-SUMMARY.md` when done
</output>
