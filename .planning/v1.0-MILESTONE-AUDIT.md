---
milestone: v1.0
audited: 2026-09-14T05:10:00Z
status: gaps_found
scores:
  requirements: 35/39
  phases: 7/9
  integration: 2 blockers, 7 warnings
  flows: 5/6
gaps:
  requirements:
    - id: DOC-02
      status: partial
      phase: "6"
      claimed_by_plans: ["06-02-SUMMARY.md"]
      completed_by_plans: ["06-02-SUMMARY.md"]
      verification_status: missing
      evidence: "Phase 06 has no VERIFICATION.md. Integration Finding 7 live-contradicts docs accuracy: docs/development.md:438-444 uses nonexistent `sif collection create` / `sif index add` / bare `sif search`."
    - id: DOC-03
      status: partial
      phase: "6"
      claimed_by_plans: ["06-03-SUMMARY.md"]
      completed_by_plans: ["06-03-SUMMARY.md"]
      verification_status: missing
      evidence: "No VERIFICATION.md for phase 06."
    - id: DOC-04
      status: partial
      phase: "6"
      claimed_by_plans: ["06-04-SUMMARY.md (per SUMMARY frontmatter)"]
      completed_by_plans: ["06-04-SUMMARY.md"]
      verification_status: missing
      evidence: "No VERIFICATION.md. Skills carry stale `sif update` hints (actual command: `sif index update`) — CLI main.py:107, bench.py:64, search.py:145,294,447, both skills."
    - id: DOC-05
      status: partial
      phase: "6"
      claimed_by_plans: ["06-05-SUMMARY.md (per SUMMARY frontmatter)"]
      completed_by_plans: ["06-05-SUMMARY.md"]
      verification_status: missing
      evidence: "No VERIFICATION.md for phase 06."
  integration:
    - severity: blocker
      finding: "All-collections-excluded returns ALL documents instead of none"
      where: "src/sif/cli/commands/search.py:163,329,468 sets collection_ids=[] when list_enabled() is empty; src/sif/search/bm25.py:54 treats empty list as no-filter"
      affects: ["CLI-04", "CLI-05"]
      evidence: "Live-reproduced by integration checker: sole collection excluded (include_by_default=0, 'Default ✗' in collection list) yet search returned the doc. Same pattern likely in vector/hybrid."
    - severity: blocker
      finding: "Distribution still named `docsif`; install hints reference nonexistent `sif[...]` extras"
      where: "pyproject.toml:6 name=\"docsif\"; duplicate docsif console script (line 69); cli/commands/mcp.py:39,72 and search.py:509 tell users `pip install sif[mcp]`/`sif[http]`/`sif[embed]`"
      affects: ["MCP-01", "Phase 8 goal (rename)"]
      evidence: "Extras exist only under the name `docsif`; publishing as SIF v1.0 would ship the wrong package name and every self-referential install hint is broken."
  flows:
    - flow: "collection exclude-all → search"
      breaks_at: "searcher filter — empty collection_ids treated as unfiltered"
      affects: ["CLI-04", "CLI-05"]
nyquist:
  compliant_phases: ["03", "04", "05"]
  partial_phases: []
  not_validated_phases: ["01", "02", "08", "09"]
  missing_phases: ["06", "07"]
  overall: "NOT-VALIDATED ×4 (status: draft — coverage TODO per #2117, not a compliance failure) + MISSING ×2; run /gsd-validate-phase on each flagged phase to promote to a real verdict"
traceability_staleness:
  last_updated: "2026-04-14 (predates phases 6-9)"
  stale_pending_checkboxes: ["FND-01", "FND-03", "FND-05", "FND-06", "FND-07", "FND-08", "CLI-03", "CLI-04", "CLI-08"]
  untracked_requirement_ids: ["DOC-02", "DOC-03", "DOC-04", "DOC-05", "MCP-01", "MCP-02", "MCP-03", "MCP-04"]
  note: "9 Pending checkboxes are proven SATISFIED by phase VERIFICATIONs (update them); DOC-*/MCP-* are claimed and verified by phases 6/9 but absent from the traceability table. Orphan detection proper (tracked-but-never-verified): none."
tech_debt:
  - phase: 03-embedding-vector-search
    items:
      - "mypy strict-mode typing debt: 139 errors in 35 files (open deferred item, disposition pending)"
      - "FTS trigger corruption on direct UPDATE of external-content FTS5 (pre-existing, Phase 1 origin) — 'database disk image is malformed'"
  - phase: 04-advanced-search-pipeline
    items:
      - "CR-03: vec:/hyde: chunk duplicates degrade vector-route quality"
      - "WR-03: intent prepended as literal query text (feeds snippet terms)"
      - "WR-04/07/08: csv/xml escaping, dead bench -C flag, ignored fixture collections field"
      - "WR-06: snippet extract can exceed max_length — reaches --json only (table render bounded)"
      - "WR-09: expand: multi-list provenance mislabeling"
  - phase: integration (cross-phase, 2026-09-14 checker)
    items:
      - "lex: prefix eagerly loads embedder — BM25-only escape hatch dead on minimal installs (SRCH-01/02)"
      - "MCP query ignores collection default-inclusion state — CLI/MCP semantic divergence (MCP-02 vs CLI-04)"
      - "Context descriptions never reach MCP clients — CTX-02/03 CLI-only (MCP-02)"
      - "MCP get path normalization asymmetry: /tmp/... fails, /private/tmp/... works on macOS (MCP-03)"
      - "ls tree nests collection root under full resolved path (cosmetic, CLI-03)"
      - "Collection/global-scope contexts write-only for search (documented decision D-07 — by design, note only)"
  - phase: 05-agent-context-experience
    items:
      - "context list/prune UX: no ID column, prune has no preview/confirmation (05-UI-REVIEW advisories)"
      - "MCP field carry-forward (05-VERIFICATION info item)"
  - phase: windows-ledger
    items:
      - "HuggingFace factory stub raises NotImplementedError (WINDOWS #1, open)"
      - "Live OpenAI-compatible endpoint check requires user API key (WINDOWS #2, open)"
      - "2 pre-existing caplog test failures in test_openai_embedder.py — setup_logging propagate=False pollution (WINDOWS #3/#4)"
---

# Milestone v1.0 Audit Report — SIF (formerly DocSift)

**Audited:** 2026-09-14 · **Verdict: `gaps_found`** (2 integration blockers + 2 unverified phases force the FAIL gate)

## Method

Three-source requirements cross-reference (REQUIREMENTS.md traceability × phase VERIFICATION.md tables × SUMMARY frontmatter) + live integration check (temp index, MCP stdio handshake, 346 tests passed) + Nyquist VALIDATION.md scan. Basis: current codebase at `9abea51`+.

## Requirements (35/39 satisfied)

| Group | IDs | Status |
|---|---|---|
| Foundation (P1) | FND-01..08 | ✓ satisfied (01-VERIFICATION passed; live wiring confirmed) |
| CLI core (P2) | CLI-01..05, 08 | ✓ satisfied — **but CLI-04/05 edge-case integration blocker below** |
| Embedding/vector (P3) | VEC-01..04 | ✓ satisfied (03-VERIFICATION passed + 2026-09-04/09-11 UAT live evidence) |
| Search pipeline (P4) | SRCH-01..08, CLI-06, 07 | ✓ satisfied |
| Context (P5) | CTX-01..03 | ✓ satisfied (path scope live-verified; collection/global write-only by decision D-07) |
| Docs (P6) | DOC-02..05 | ⚠ **partial — no VERIFICATION.md; DOC-02/04 live-contradicted by stale command hints** |
| Skills (P7) | (no REQ-IDs) | ✓ wired — skill flag examples verified live (stale `sif update` hints excepted) |
| Rename (P8) | (goal-level) | ⚠ **distribution name still `docsif`** — blocker below |
| MCP (P9) | MCP-01..04 | ✓ satisfied per 09-VERIFICATION (live stdio handshake) — install-path and semantic caveats below |

**Traceability staleness:** REQUIREMENTS.md predates phases 6–9 — DOC-*/MCP-* never tracked; 9 "Pending" checkboxes proven satisfied. No true orphans (all 31 tracked IDs verified).

## Phases (7/9 verified)

| Phase | VERIFICATION | VALIDATION (Nyquist) |
|---|---|---|
| 01–05 | passed | 01/02 draft (TODO); 03/04/05 compliant |
| 06 | **MISSING — unverified phase (blocker)** | MISSING |
| 07 | **MISSING — unverified phase (blocker)** | MISSING |
| 08 | passed | draft (TODO) |
| 09 | passed | draft (TODO) |

## Integration (2 blockers, 7 warnings)

Live E2E confirmed working: index→embed→search→get→ls core pipeline; partial collection exclusion; context descriptions in CLI JSON output; prefix routing + explain + snippets; MCP stdio handshake against the CLI-built index (4/4 tools); skills' flag accuracy.

### BLOCKER 1 — exclude-all returns everything (CLI-04/05)
`search.py:163,329,468` emits `collection_ids=[]` when every collection is excluded; `bm25.py:54` reads empty as "no filter". Live-reproduced. Same pattern likely in vector/hybrid.

### BLOCKER 2 — distribution named `docsif` (P8, MCP-01)
`pyproject.toml` name + duplicate console script still `docsif`; CLI error messages point users at `sif[mcp]`/`sif[http]`/`sif[embed]` extras that exist only under `docsif`. Every self-referential install hint is broken; publishing v1.0 would ship the wrong name.

### Warnings (recommended hardening before or shortly after ship)
1. `lex:` eagerly loads embedder — dead BM25-only escape hatch on minimal installs (SRCH-01/02)
2. MCP `query` ignores `include_by_default` — CLI/MCP divergence (MCP-02 vs CLI-04)
3. Context descriptions absent from MCP results — headline P5 feature is CLI-only (CTX-02/03, MCP-02)
4. MCP `get` exact-match path asymmetry on macOS `/tmp` vs `/private/tmp` (MCP-03)
5. Stale `sif update` hints in CLI + skills; broken `docs/development.md` examples (DOC-02/04)
6. `ls` cosmetic nesting under full resolved path (CLI-03)
7. Collection/global contexts write-only (D-07, by design — recorded, not a defect)

## Flows (5/6 clean)

Core pipeline, partial exclusion, context retrieval, MCP round-trip, skills accuracy: **pass**. Exclude-all degenerate case: **inverted semantics** (returns all).

## Recommended closure set (before /gsd-complete-milestone)

1. **Fix BLOCKER 1** (sentinel value for exclude-all across bm25/vector/hybrid) — small, surgical
2. **Fix BLOCKER 2** (rename distribution to `sif`, dedupe console script, align extras + install hints) — small
3. **Verify phases 06/07** (`/gsd-verify-work 6`, `/gsd-verify-work 7`) or accept as override
4. **Fix stale command hints + docs examples** (DOC-02/04) — trivial edits
5. **Pay down typing debt** (139 mypy errors — already user-committed)
6. Refresh REQUIREMENTS.md traceability (9 checkboxes + DOC-*/MCP-* rows) at archive time
